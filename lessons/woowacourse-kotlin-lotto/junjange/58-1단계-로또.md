# PR #58 — [에디] 1단계 로또

- **Reviewee:** [@junjange](https://github.com/junjange)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/58
- **단계:** 1단계
- **리뷰어:** @vagabond95
- **라인 코멘트:** 31

## 핵심 지적

### 1. Controller에서 `,` split

```kotlin
val lottoNumbers = winningNumber.split(",").map { LottoNumber(it.trim()) }
val winningLotto = Lotto(lottoNumbers)
```

> "," 로 split 된다는 내용은 view 의 요구사항인데요. view 의 요구사항이 바뀔 때 controller 에 변경이 발생하는 것을 지양해야할 것 같습니다!

→ "," 구분은 View 책임. Controller는 도메인 객체 받기만.

### 2. 출력 문자열 상수 미분리

### 3. Rank 순회로 출력 vs lottoResult 직접 출력

> Rank 를 순회하면서 출력하기보다, lottoResult 의 결과를 바로 출력하는 것은 어떨까요?

## 얻은 교훈

- **Controller가 split = View 책임 침범**. 입력 형식 변경이 Controller까지.
- **결과 객체가 자기 출력 표현** — Rank 순회는 *View의 일*이 아닌 *결과 객체의 일*.
