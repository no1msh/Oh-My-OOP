# PR #81 — [로또] 에디 2단계 미션

- **Reviewee:** [@junjange](https://github.com/junjange)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/81
- **단계:** 2단계
- **리뷰어:** @vagabond95
- **라인 코멘트:** 32

## 핵심 지적

### 1. 함수명에 `And` — 두 책임 신호

```kotlin
fun printErrorAndExit(...) { ... }
```

> 네이밍에 And 가 들어간다는 것은 해당 함수가 2가지 이상의 역할을 수행한다는 신호인데요. 함수당 한가지 역할만 수행하도록 개선 가능할까요?

### 2. `ErrorCode` enum + `ExceptionsHandler` (칭찬)

> 에러코드 정리 👍 반복적인 작업을 공통화하는 시도 👍 그런데, 서비스가 커질수록 모든 에러코드를 한 곳에서 관리했을 때 생길 수 있는 단점도 추후에는 같이 고려가 되면 좋을 것 같아요!

→ 중앙 ErrorCode = 도메인 god-folder 위험.

### 3. `sealed class GameState` 활용 (칭찬)

## 얻은 교훈

- **함수명에 `And` = 두 책임 신호**.
- **중앙 ErrorCode = 미래 god-folder**. 도메인별 분리도 가능.
- **`sealed class`로 상태 표현** = OOP의 OCP 패턴.
