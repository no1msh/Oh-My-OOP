# PR #153 — [로또 2단계] 이든 미션

- **Reviewee:** [@devfeijoa](https://github.com/devfeijoa)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/153
- **단계:** 2단계 (최신 PR)
- **리뷰어:** @malibinYun
- **라인 코멘트:** 22

## 핵심 지적

### 1. `ManualLottoGenerator`의 count 불일치 위험

```kotlin
class ManualLottoGenerator(val manualNumbersList: List<List<Int>>) {
    fun generate(count: Int): List<Lotto> = ...  // ⚠ count와 numbersList size 불일치?
}
```

> ManualLottoGenerator 객체를 활용하는 개발자는 manualNumbersList의 count와 generate의 count가 같아야한다는 사실을 알 수 있을까요?

→ 묵시적 invariant = 동료 혼란. 시그니처로 명시.

### 2. Lotto가 당첨 티켓 관심?

> 로또 티켓 자체는 당첨 티켓에 대한 관심을 가져야할지 고민해보세요!

### 3. 부 생성자 vs 팩토리

> 이 부분을 생성자로 만들지 않은 이유가 궁금하네요

### 4. 에러 메시지에 입력값

## 얻은 교훈

- **묵시적 invariant 회피** — 시그니처로 명시. List<List<Int>>의 size = count는 *암묵 약속*. 차라리 List만 받기.
- **에러 메시지에 *입력값*** = 디버깅 비용 ↓.
