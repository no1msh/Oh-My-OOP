# PR #74 — [로또] 올리브 1단계 미션

- **Reviewee:** [@kimhm0728](https://github.com/kimhm0728)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/74
- **단계:** 1단계
- **리뷰어:** @laco-dev
- **라인 코멘트:** 46

## 못한 점 + 핵심 지적

### 1. lint 실패

```
ModelTest.kt:13:1 Wildcard import (cannot be auto-corrected)
ModelTest.kt:34:27 Newline expected after opening parenthesis
```

### 2. `LottoConstants` — 상수 분리 기준 자가 의문

```kotlin
object LottoConstants {. }
```

리뷰어:

> 상수로 값이 분리되는 기준은 무엇일까요?? 2군데 이상에서 같이 사용되면 상수에 포함이 되는 것일까요? 그렇다면 향후 다시 1군데에서만 사용된다면 상수가 아니게 되는 것일까요? 클래스가 수백개 수천개가 된다면 2개 이상 사용하는지 어떻게 검사해야 할까요.. 각 상수에 대한 값을 표현하기에 적절한 객체는.

→ "2군데 이상" 기준은 *유지 불가능*. 도메인 객체가 자기 상수 소유.

### 3. LottoPrize 정의된 값을 또 하드코딩

```kotlin
enum class LottoPrize(val matchingCount: Int,.) {
    FIRST(6,.),
.
}

if (matched == 6) {. }  // ⚠ LottoPrize.FIRST.matchingCount 안 씀
```

> 하드코딩 대신 LottoPrize에 정의된 matchingCount 값을 활용해 볼 수 있지 않을까요?

### 4. 복합 조건문 괄호 누락

→ 가독성 + 우선순위 명시.

## 얻은 교훈

- **상수 분리 기준 = *2군데 이상*은 불안정**. 도메인 객체 소유가 안정적.
- **enum 데이터 = enum 자신이 활용**. 외부에서 매직넘버 다시 쓰면 enum 가치 잃음.
