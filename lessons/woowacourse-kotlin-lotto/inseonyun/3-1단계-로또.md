# PR #3 — [써니] 1단계 로또 (초창기 PR)

- **Reviewee:** [@inseonyun](https://github.com/inseonyun)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/3
- **단계:** 1단계
- **리뷰어:** @namjackson
- **라인 코멘트:** 39

## 못한 점 + 핵심 지적

### 1. `World` Controller — DYGames PR #2와 동일 안티패턴

→ "World"는 god-naming. *책임이 무엇이든* 들어갈 수 있음.

### 2. 함수 순서 — 코드 읽기 흐름

> 함수 순서에 대한 이야기입니다! 개발자들은 보통 코드를 작성하는 시간보다 작성을 위해 코드를 읽는 시간이 훨씬 많다고해요! 코드를 읽는 사람의 가독성을 위해, 위에서 아래로 흐름이 흐르도록 순서를 바꿔보면 어떨까요?

→ public → 호출 순서대로 → private. Kotlin 컨벤션.

### 3. `Lotto`의 1-45 검증 + 중복 검증 누락

```kotlin
class Lotto(val numbers: List<Int>) { ... }  // ⚠ 범위/중복 검증 없음
```

> numbers에 1~45 범위가 아닌수가 들어올수 있는 구조네요!
> numbers에 중복된 숫자가 들어오는 케이스도 고려해보면 좋을것 같아요!

### 4. Controller에서 로또 장수 결정

> 정해진 Money에서 몇장의 로또를 살지를 결정하는 것또한 모델의 있어야하는 로직이 아닐까요? (View와 도메인을 연결하는 작업 이외의 로직은 최소화로 해보면 어떨까요?)

## 얻은 교훈

- **"World" 같은 god-naming** = lotto 초창기 PR (#2, #3) 공통 패턴.
- **함수 순서 = 가독성 = 호출 순서대로**.
- **`Lotto` 도메인 invariant 검증 = 범위 + 중복 + 개수** 셋 다 init.
