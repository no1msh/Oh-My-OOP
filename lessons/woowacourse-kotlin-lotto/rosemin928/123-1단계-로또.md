# PR #123 — [로또 1단계] 비비 미션

- **Reviewee:** [@rosemin928](https://github.com/rosemin928)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/123
- **단계:** 1단계
- **리뷰어:** @Gyuil-Hwnag
- **라인 코멘트:** 45

## 잘한 점

- **`LottoGenerator` 인터페이스** — 칭찬.

## 못한 점 + 핵심 지적

### 1. `main()` 이름 모호 (다게임 확장)

### 2. 하드코딩 문구 다수

> 하드 코딩된 문구들에 대해서, 상수화를 해보는 것은 어떨까 라는 생각이 듭니다

### 3. `LottoResult` 결과 wrapping 안 함

```kotlin
fun calculateResult(...): Map<Rank, Int> = ...
```

> Map으로 리턴되는 값들을 WinningXXX or Lottos 등으로 묶어보는 것은 어떨까 라는 생각이 듭니다 그렇다면 OutputView.printResult() 내의 로직등 여러가지 책임을 가질 수 있겠다는 생각이 드네요!

### 4. `Profit` 원시값 노출

```kotlin
val profit: Double = ...
```

> 원시값 포장을 한다면, Profit 이라는 객체도 책임을 가지게 해볼 수도 있겠군요!

## 얻은 교훈

- **`Map<Rank, Int>` wrapping** = `WinningStatistic` 객체로. View가 자료구조 직접 다룸 회피.
- **`Profit`/`ProfitRate` 원시값 포장** — Double을 그대로 노출하면 *계산식*이 호출자에. 객체가 *수익률 계산*까지.
