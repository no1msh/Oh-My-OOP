# PR #140 — [로또 2단계] 비비 미션

- **Reviewee:** [@rosemin928](https://github.com/rosemin928)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/140
- **단계:** 2단계
- **리뷰어:** @Gyuil-Hwnag
- **라인 코멘트:** 30

## 핵심 지적

### 1. 자동/수동 변수명 모호

> 자동 로또와 수동 로또를 구분해주셨군요~ 자동로또라는 네이밍이 드러나게 변수명을 지어봐도 좋을 것 같다는 생각이 듭니다!

→ `autoLottos`/`manualLottos` 명시.

### 2. `LottoTicketCounter` — manualCount, autoCount 분리 권장

```kotlin
class LottoTicketCounter {
    fun count(): List<Int> = listOf(manualCount, autoCount)
}
```

> 해당값을 리스트로 만들어야 할 이유가 있을까? 라는 생각이 듭니다! fun manualCount(), fun autoCount() 이렇게 두개로 나눠보는 것은 어떨까 라는 생각이 듭니다

### 3. `LottoTicketCounter` vs `LottoStore` 합치기 논의

학생:

> LottoTicketCounter는 로또 개수를 세는 역할, LottoStore는 로또를 발행하는 역할이라고 생각해서 따로 두었는데 이를 합치면 더 좋을까요?

## 얻은 교훈

- **`List<Int>`로 두 의미 값 합치기 ≠ 추상화**. 명시적 메서드가 의미 명확.
- **분리 정당화 = 책임 다름**. "세기" + "발행" 책임이 다르면 분리.
