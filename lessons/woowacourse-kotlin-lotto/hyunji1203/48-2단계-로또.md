# PR #48 — [뽀또] 2단계 로또

- **Reviewee:** [@hyunji1203](https://github.com/hyunji1203)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/48
- **단계:** 2단계
- **리뷰어:** @laco-dev
- **라인 코멘트:** 34

## 핵심 지적

### 1. `LottoMaker` — 협력 없이 단독 생성

```kotlin
class LottoMaker {
    fun make(): Lotto = Lotto((1..45).shuffled().take(6))  // ⚠ 단독
}
```

> 1. LottoMaker는 아무런 협력이 없이, 단순히 로또를 만들고만 있어요
> 2. 도메인 로직이 다시 RandomLottoGenerator()라는 불확실성과 의존성을 가지게 되었어요.
> 로또를만든다 → 로또 제너레이터에게 로또를 요청한다 →

→ 협력 없는 객체 = 인스턴스 의미 약함.

### 2. Controller 검증 중복

> 이 검증 로직은 도메인 모델에서 수행하고 있기 때문에 중복이라고 볼 수 있지 않을까요?

### 3. Rank fallback에 null 반환

> 일치하지 않는 Rank가 있는 경우, MISS 타입을 반환해야 하지 않을까요??

### 4. `Lottos` 위임 활용 (칭찬)

> 위임을 이용해 Lottos가 리스트 타입이 되도록 한 점은 잘 해주셨습니다.

### 5. 파라미터 = 불변 `List`

> 파라미터를 받을 때에는 가급적 불변 객체인 List 타입을 받도록 합니다

## 얻은 교훈

- **단독 생성 객체 = 협력 없는 객체**. 협력자가 있어야 OOP의 의미.
- **파라미터 = `List<T>` (불변)**, `MutableList`를 외부 인터페이스에 노출 X.
