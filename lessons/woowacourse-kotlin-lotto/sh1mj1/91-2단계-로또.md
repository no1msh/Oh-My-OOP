# PR #91 — [로또] 심지 2단계 미션

- **Reviewee:** [@sh1mj1](https://github.com/sh1mj1)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/91
- **단계:** 2단계
- **리뷰어:** @malibinYun
- **라인 코멘트:** 42

## 잘한 점

- **`LotteryMachine` 인터페이스** + `RandomLotteryMachine`/`ManualLotteryMachine` 두 구현체.
- **`LotterySeller` 역할 분리 시도** — 지불 → 수량.
- **자가 의문 PR에 풍부히 명시** — 원시값 포장 경계 등.

## 학생의 자가 학습

학생 PR:

> 설계 중에, LottoterySeller가 LotteryMachine에 접근할 수 있고, Controller에서는 LotteryMachine에 대한 의존성을 가지지 않도록 만들고 싶었습니다.

> step 1 이후 원시값 포장에 대해 많은 고민을 해보았습니다. 제가 도달한 생각은 아래와 같습니다.

→ 자가 설계 + 회고 → 다음 라운드 토론 출발점.

## 얻은 교훈

- **인터페이스 분리 (LotteryMachine) → 두 구현체 (Random/Manual)** = lotto 2단계의 표준 패턴. Strategy.
- **Seller가 Machine 의존 + Controller는 Seller만 의존** = Clean Architecture 패턴. Controller → Service → Domain.
- **자가 설계 회고 PR** — 학습이 *코드만이 아닌 설계 사고 흐름까지* 기록됨.
