# PR #75 — [로또] 심지 1단계 미션

- **Reviewee:** [@sh1mj1](https://github.com/sh1mj1)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/75
- **단계:** 1단계
- **리뷰어:** *(self-discussion)*
- **라인 코멘트:** 15

## 학생 자가 의문

> `LotteryRankEvaluator` 클래스를 구현할 때도 고민이 많았습니다. 위 `LotteryGenerator` 클래스에 대한 고민과도 마찬가지로 클래스 이름과 메서드 `evaluate` 가 중복됩니다.

→ `evaluator.evaluate()` 같은 동어반복.

> 제가 이 Money 클래스를 data class 로 선언하지 않고 직접 equals, hashCode, toString 메서드를 override 한 이유는...

## 얻은 교훈

- **`Evaluator.evaluate()` 같은 동어반복 회피** = 짧고 명확한 이름 (`Ranker.of()`).
- **data class vs 직접 override** — racingcar에서 같은 학생 (sh1mj1)의 깊은 고민. lotto에서도 지속.
