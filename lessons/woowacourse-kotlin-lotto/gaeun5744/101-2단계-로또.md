# PR #101 — [로또] 벼리 2단계 미션

- **Reviewee:** [@gaeun5744](https://github.com/gaeun5744)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/101
- **단계:** 2단계
- **리뷰어:** *(self-discussion)*
- **라인 코멘트:** 14

## 학생 자가 변경 이유

> 이전에서는 제가 문자열은 따로 companion object에서 const val로 선언함으로써 한곳에 모아두었지만, 이번 pr에서는 문자열을 그대로 넣었습니다.

> 기존에는 `InputView`에 모든 로직을 두었지만, 이번 pr에서는 연관된 함수들을 묶어서 `domain` 객체를 생성한 후에 Controller에 전달하는 방식으로 로직을 세...

> `Ticket`이 `lottos`를 발행한다는 상황이 어색해보여서, `lottoMachine`으로 수정했습니다!!

## 얻은 교훈

- **상수 분리 vs 인라인 = 컨텍스트에 따라**. 1단계에서 모은 상수를 2단계에서 인라인으로 되돌리는 결정도 *왜*가 명확하면 OK.
- **InputView 분리** — 연관 함수를 도메인 객체로 묶기.
- **이름이 어색하면 변경** — `Ticket`보다 `LottoMachine`이 *발행* 행위 어울림.
