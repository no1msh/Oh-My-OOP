# PR #149 — [로또 2단계] 밀러 미션

- **Reviewee:** [@oungsi2000](https://github.com/oungsi2000)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/149
- **단계:** 2단계
- **리뷰어:** @malibinYun
- **라인 코멘트:** 41

## 못한 점 + 핵심 지적

### 1. 1단계 피드백 미반영 (racingcar 랜덤 테스트 패턴)

> 이전 피드백을 먼저 반영해주세요 로또에서 랜덤 테스트를 구성하지 못한다는 의미는, 자동차 경주에서의 랜덤 테스트를 가능하게 하는 방법에 대해.

→ racingcar에서 배운 NumberGenerator 추상화 패턴 미적용.

### 2. object → class 변경 이유?

```kotlin
class LottoGenerator {. }  // 이전엔 object
```

> 갑자기 object -> class 로 변경된 이유는 무엇인가요?

### 3. for + mutableList 패턴

→ `List(count) { generate() }` 또는 `(1..count).map { generate() }`.

### 4. `associate/group` 컬렉션 함수 활용

```kotlin
val map = mutableMapOf<Rank, Int>()
lottos.forEach {. map[rank] = (map[rank] ?: 0) + 1. }
```

→ `lottos.groupingBy { it.match(winning) }.eachCount()`.

### 5. 수동 로또가 LottoGenerator를 안 쓰는 분기

```kotlin
val manual = Lotto(.)  // ⚠ Generator 우회
```

> LottoGenerator 라는 객체가 있는데, 수동 로또는 생성기를 통하지 않네요. 수동 로또를 어디서 만드는 지 과연 다른 개발자들을 짐작할 수 있을까요?

→ 자동/수동 모두 Generator 인터페이스 구현체.

### 6. `UserInput`의 메서드 — View 전용?

> 이 메서드들이 꼭 View에서 사용해야하는 메서드인가요?

## 얻은 교훈

- **이전 피드백 흡수 = 다음 단계의 기반**. 1단계 안 받았으면 2단계도 무너짐.
- **`groupingBy { }.eachCount()`** = Map 집계의 표준. lotto 통계에 자연.
- **자동/수동 통합 = Strategy/Generator 인터페이스 둘 다 거치게**. 일관성.
