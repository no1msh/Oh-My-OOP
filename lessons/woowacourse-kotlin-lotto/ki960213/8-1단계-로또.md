# PR #8 — [토마스] 1단계 로또

- **Reviewee:** [@ki960213](https://github.com/ki960213)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/8
- **단계:** 1단계
- **리뷰어:** @galcyurio
- **라인 코멘트:** 15

## 핵심 지적

### 1. 1, 45 하드코딩 → 상수 참조

> 위의 상수에 따라 정책이 변경될 수도 있으니 1, 45를 하드코딩하는 대신 상수를 참조해주세요.

### 2. 인터페이스 이름 + 메서드 명 중복

> 인터페이스 이름이 `NumberGenerator`이므로 `generate()`로도 충분해보여요.

### 3. 일급 컬렉션이 List 타입 구현 — 도메인 함수만 노출

> 일급컬렉션에서 컬렉션 타입을 구현하도록 되어있는데 이번 미션에서는 모두 지워보고 도메인에 어울리는 이름으로 함수를 만들어보시면 좋겠습니다. 그리고 get(), set(), con...

### 4. 방어적 복사 패턴

## 얻은 교훈

- **인터페이스 이름 + 메서드 명 = 컨텍스트 안 겹치기**. `NumberGenerator.generate()` 충분.
- **일급 컬렉션 = 도메인 메서드만 노출**. `List<T>` 구현은 *전체 인터페이스 노출* — 일급 컬렉션 의도와 충돌.
