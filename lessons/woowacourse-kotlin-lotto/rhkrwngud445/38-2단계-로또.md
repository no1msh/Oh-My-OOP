# PR #38 — [코비] 2단계 로또

- **Reviewee:** [@rhkrwngud445](https://github.com/rhkrwngud445)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/38
- **단계:** 2단계
- **리뷰어:** @BeokBeok
- **라인 코멘트:** 20

## 핵심 지적

### 1. List 반환이지만 `as MutableList`로 캐스팅 가능

```kotlin
fun getLottos(): List<Lotto> = lottos  // ⚠ 사실은 MutableList
// 호출 측에서
val mutable = getLottos() as MutableList  // ⚠ 원본 훼손
```

> mutableList를 함수의 리턴타입을 활용하여, List 타입으로 전달하도록 구현해주셨네요. 하지만, `as` 를 통해 Mutable로 변환이 가능하며, 이로 인하여 원본을 훼손시킬 수 있습니다. "방어...

→ 방어적 복사 (`toList()`) 또는 immutable 자료구조.

### 2. 함수명 너무 김

> 함수명이 너무 길어서 잘 읽히지가 않네요. 함수명의 단어의 수를 조금 줄여보면 어떨까요?

## 얻은 교훈

- **`List as MutableList` 캐스팅 가능 = 안전성 잃음**. `toList()`로 방어적 복사.
- **함수명 길이 = 책임이 많은 신호**. `printWinningStatisticsAndProfitRate` 같이 길면 두 함수.
