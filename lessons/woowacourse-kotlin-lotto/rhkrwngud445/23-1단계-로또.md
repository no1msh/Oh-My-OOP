# PR #23 — [코비] 1단계 로또

- **Reviewee:** [@rhkrwngud445](https://github.com/rhkrwngud445)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/23
- **단계:** 1단계
- **리뷰어:** @BeokBeok
- **라인 코멘트:** 11

## 핵심 지적

### 1. 멤버 변수 vs 생성자 주입

> 멤버 변수는 모든 함수가 공통으로 접근해서 사용할 수 있는 장점이 있지만, 멤버 변수의 값에 따라 함수의 호출 결괏값이 달라.

### 2. 함수 시그니처 단순화

```kotlin
fun initWinningLottoNumbers(lottoNumbers: List<Int>, bonusNumber: Int) {. }
```

→ 도메인 객체로 묶어 시그니처 단순화.

## 얻은 교훈

- **멤버 변수 → 생성자 주입** = 함수 결과 예측 가능성 ↑.
- **여러 파라미터 = 도메인 객체로 묶기** (`WinningLottoInput`).
