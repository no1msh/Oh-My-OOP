# PR #105 — [로또 1단계] 뭉치 미션

- **Reviewee:** [@m6z1](https://github.com/m6z1)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/105
- **단계:** 1단계
- **리뷰어:** @malibinYun
- **라인 코멘트:** 46

## 못한 점 + 핵심 지적

### 1. Lotto에 Set 미적용 (List + 중복 검증)

```kotlin
class Lotto(val numbers: List<LottoNumber>) {
    init { require(numbers.toSet().size == numbers.size) }  // ⚠ 매번 변환
}
```

> set을 활용해보는 건 어떨까요?

→ 도메인 자체에 *중복 없음 invariant*가 있다면 Set으로 저장.

### 2. LottoMachine — 테스트 어려운 구조

```kotlin
class LottoMachine {
    fun generate(count: Int): List<Lotto> {
        repeat(count) {. Random.nextInt(.). }  // ⚠ Random 직접
    }
}
```

> 이 클래스는 테스트하기 매우 어려운 구조네요. 랜덤 요소를 분리해내서 테스트할 수 있도록 구성해보는 건 어떨까요?

### 3. `mutableListOf + add` 패턴

```kotlin
val lottos = mutableListOf<Lotto>()
repeat(count) { lottos.add(generateOne()) }
return lottos
```

> 코틀린 Collections 확장함수들을 활용한다면 지금처럼 mutableList를 만들어서 add 하는 형태의 코드를 작성하지 않아도 된답니다!

→ `List(count) { generateOne() }`.

### 4. `LottoNumber` 직접 equals/hashCode

```kotlin
class LottoNumber(val value: Int) {
    override fun equals(.): Boolean =.
    override fun hashCode(): Int =.
}
```

> 직접 equals & hashCode를 구현하신 이유는 무엇인가요? data class 와 class의 차이점에 대해 알아보세요!

→ data class 또는 value class가 자동 생성.

### 5. LottoNumber 캐싱 미적용

## 얻은 교훈

- **도메인 invariant 표현 = 자료구조 선택**. *중복 없음*은 Set으로.
- **`List(count) {. }` Kotlin builder** — mutableListOf + repeat 패턴 대체.
- **data class / value class > 직접 equals/hashCode**.
- **flyweight** — 1억 티켓도 LottoNumber 45개 재활용.
