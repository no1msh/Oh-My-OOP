# PR #24 — [스캇] 1단계 로또

- **Reviewee:** [@chws0508](https://github.com/chws0508)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/24
- **단계:** 1단계
- **리뷰어:** @BeokBeok
- **라인 코멘트:** 21

## 핵심 지적

### 1. `check~` 함수명 모호

> 함수명만 봤을 때에는 무엇을 "체크"하는지 알기가 어렵네요. 함수가 하는 일에 대해서 좀 더 명확하게 작성해보면 어떨까요?

### 2. 매직넘버 + 매직리터럴

```kotlin
if (count == 5) return 1_500_000  // ⚠ 5, 1_500_000
```

> 숫자5와 1_500_000과 같은 것은 "매직리터럴"이라고 합니다. 이 숫자들은 자세히 보시면, Enum에 정의된 값과 동일합니다. 해당 값을 가지고 있는 Enum을 활용해보면 어떨까요?

### 3. Rank 변경 시 OutputView도 변경 필요

## 얻은 교훈

- **`check*` = 검증/예외 행위 시사. 반환 있으면 다른 이름** (`has*`, `count*`).
- **매직리터럴 = enum 활용으로 제거**.
