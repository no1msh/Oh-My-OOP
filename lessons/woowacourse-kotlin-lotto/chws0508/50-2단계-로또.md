# PR #50 — [스캇] 로또 미션 2단계

- **Reviewee:** [@chws0508](https://github.com/chws0508)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/50
- **단계:** 2단계
- **리뷰어:** @BeokBeok
- **라인 코멘트:** 23

## 핵심 지적

### 1. `BonusNumberValidation`이 검증 + 변환 (두 일)

```kotlin
fun validateBonus(input: String): Int {  // ⚠ 검증 + 변환
    require(...)
    return input.toInt()
}
```

> 함수명만 봤을 때에는 "보너스볼 유효성을 체크한다" 라는 의미로 보여요. 하지만 함수 구현부를 보면, 유효성을 체크할 뿐만 아니라 다른 값으로 변환해서 리턴을 해주고 있네요. 하나의 함수에서는 한 가지 일만 하도...

### 2. 예외 처리 두 종류

> 코틀린에서 예외를 처리하는 방법은 크게 두 종류가 있습니다.
> - 오류가 예상되는 경우
> - 오류가 예상되지 않는 경우

## 얻은 교훈

- **`validate*` 함수가 변환까지 = 두 책임**. `validateAndConvert` 또는 두 함수로.
- **예외 처리 두 분류** — 예상 vs 예상 외. 처리 방법 다름.
