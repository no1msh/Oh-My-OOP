# PR #35 — [하티] 2단계 로또

- **Reviewee:** [@sujin9](https://github.com/sujin9)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/35
- **단계:** 2단계
- **리뷰어:** @galcyurio
- **라인 코멘트:** 24

## 학생 자가 의문

> 다만 고민 하나가 생겼는데요, `money`와 같은 값도 객체로 감싸줄 필요가 있을까? - 라는 고민이 있었어요. 객체가 결국 money라는 int값만 가지고 있는 상태라면, 굳이 감싸줄 필요가 없지...

→ 원시값 포장 *과잉*에 대한 합리적 의문. 객체가 *행위*까지 가지면 정당화, 단순 wrapper면 과설계.

## 핵심 지적

### 1. junit valueOf — 직접 `Rank` 타입 가능

> junit에서 내부적으로 `valueOf()`를 사용하기 때문에 `result: Rank`로 곧바로 선언하셔도 됩니다.

### 2. given/when/then 패턴 권장

## 얻은 교훈

- **원시값 포장의 정당화 = 행위 부여**. 단순 wrapping은 가치 약함.
- **junit `valueOf` 자동 변환** — Kotlin enum 타입 직접 선언 가능.
