# PR #22 — [부나] 1단계 로또

- **Reviewee:** [@tmdgh1592](https://github.com/tmdgh1592)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/22
- **단계:** 1단계
- **리뷰어:** @vagabond95
- **라인 코멘트:** 13

## 핵심 지적

### 1. `.editorconfig` 같은 IDE 파일 — `.gitignore` 권장

### 2. `divideByThousand` 확장 함수

```kotlin
fun Int.divideByThousand(): Int = this / 1000
```

> 이 부분은 money 에 대한 원시값을 포장하고, 해당 클래스 내부에서 처리되는 것이 더 적절하지 않을까요?

→ 확장 함수가 *공용 유틸*인데 *Money 도메인 지식*을 가짐. Money 객체로.

### 3. `divideByThousand` 이름 일반적인데 메시지는 도메인 특화

> 오류 메세지는 '입력값' 이라는 내용을 내포하고 있는 부분 자체가 common 한 케이스를 다루지 않는...

## 얻은 교훈

- **`util/common` 확장 함수에 도메인 지식 누수 회피**. 도메인 객체로 흡수.
- **함수 이름과 에러 메시지 일관성** — `divideByThousand`가 일반 유틸이면 도메인 메시지 X.
