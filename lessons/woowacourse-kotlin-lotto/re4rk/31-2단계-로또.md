# PR #31 — [아크] 2단계 로또

- **Reviewee:** [@re4rk](https://github.com/re4rk)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/31
- **단계:** 2단계
- **리뷰어:** @malibinYun
- **라인 코멘트:** 28

## 핵심 지적

### 1. 남은 돈 계산에 *수동 티켓 전체* 불필요

```kotlin
fun remainingMoney(manual: Lottos): Money = money - manual.size * LOTTO_PRICE
```

> 남은 돈을 구하기 위해서 수동 티켓 전체가 필요할까요?

→ `size`만 필요.

### 2. `LottoStore` — Lotto로 변환 책임?

> 이미 Lotto가 파라미터로 들어오고 있어 큰 의미를 부여하기 어려워 보입니다. Lotto로 변환하는 역할을 이 객체에 위임해보는 것은 어떨까요?

### 3. `LottoFactory` + `LottoStore` 중복

> LottoFactory와 LottoStore는 하는 역할이 겹쳐보이네요. 또, 자동 티켓을 만들 때마다 LottoFactory 객체를 생성하는 것은 불필요해보입니다 객체 간 역할을 다시 생각해보시고, 어느 한 쪽을 통폐합 하면 좋겠네요

### 4. `Rank.values()` 먼저 채움

> Rank.values() 로 먼저 값을 채우지 않고 시도해보세요

## 얻은 교훈

- **메서드 시그니처 = *최소 필요 정보***. size만 쓰면 size만 받기.
- **두 객체 책임 겹침 = 통합/재분배 시점**. Factory + Store 같은 *비슷한 추상*.
