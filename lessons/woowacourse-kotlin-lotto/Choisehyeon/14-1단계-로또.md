# PR #14 — [로피] 1단계 로또

- **Reviewee:** [@Choisehyeon](https://github.com/Choisehyeon)
- **PR:** https://github.com/woowacourse/kotlin-lotto/pull/14
- **단계:** 1단계
- **리뷰어:** @malibinYun
- **라인 코멘트:** 36

## 핵심 지적

### 1. LottoNumber 미추출

### 2. `matchLottos`가 `Lotto`와 `Lottos` 양쪽에 (중복)

```kotlin
class Lotto { fun matchLottos(...) = ... }
class Lottos { fun matchLottos(...) = ... }
```

> winningLotto와 bonusNumber를 묶어서 당첨 티켓이란 객체를 만들어보는 것은 어떨까요?

→ `WinningLotto(lotto, bonusNumber)`가 매칭 책임.

### 3. WinningResult 빈 껍데기 + 채우기

```kotlin
val result = WinningResult()
lottos.forEach { ... result.add(rank) ... }  // ⚠ 외부에서 채움
```

> 빈 껍대기의 WinningResult를 만들어서 값을 채우기보다, WinningResult를 직접 만들어서 반환하는 것은 어떨까요?

→ `winningLotto.match(lottos): WinningResult` 한 호출.

### 4. Rank에 View 로직

## 얻은 교훈

- **빈 객체 + 외부 채우기 = invariant 시간차 깨짐**. 생성 시점에 완성된 객체 반환.
- **`WinningLotto(lotto, bonus)` 조합** = lotto의 표준 매칭 진입점.
