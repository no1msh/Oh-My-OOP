# PR #113 — [블랙잭 1단계] 포르 미션

- **Reviewee:** [@jiyuneel](https://github.com/jiyuneel)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/113
- **단계:** 1단계
- **리뷰어:** [@KwonDae](https://github.com/KwonDae) (제이든) — 2025 코호트 전담 reviewer (PR #117·#126·#129·#138·#139·#142·#143·#109·#113 분석 reviewer)
- **라인 코멘트:** 22 스레드 — *2025 코호트 셀프 체크리스트 PR 본문 + 모든 22 스레드 학생 자가 답변 + commit SHA + 학생 자가 후속 의문 5+ 회 (Player.playGame 추상화 의문 + 인자 폭증 의문 + Controller 책임 의문) + Hit/Stay Boolean vs Action enum (PR #117·#126 결과 반대 시점) + Deck null vs 예외 + companion → 인스턴스 + 고차함수 활용 + canHit 추상화 자가 학습 + Result enum 도메인 vs 뷰 + abstract vs open vs interface 자가 학습 + Participants 일급 컬렉션 자가 적용 + playGame 추상화 → Participants 역할 (reviewer 자가 수정)*
- **신규 reviewee** (2025년 코호트 — 모바일 안드로이드 7기, PR #117·#129·#130·#126·#138·#142·#143·#109·#113 = **2025 코호트 9 PR 코퍼스 성장**).

## 잘한 점 (설계 관점)

### 1. PR 본문 자가 의문 + 셀프 체크리스트 + 학생 자가 입장 명시 (2025 코호트 정점)

PR 본문 + 라인 자가 입장 (Thread 2 인용):

> *"`controller`를 게임의 중재자 또는 카지노라고 생각했습니다. 참가자가 처음에 뽑아야 하는 카드의 수(2장)는 게임의 룰에 해당하며, 이를 `controller`가 관리하는 것이 가장 바람직하다고 판단했습니다..!*
>
> *참가자에게 카드를 뽑으라는 메시지를 두 번씩 던져서 참가자는 카드 뽑기의 주체가 되지만, 몇 번 뽑아야 하는지는 게임의 흐름을 조율하는 `controller`가 결정하는 것이 적절하다고 생각했는데 어떻게 생각하시나요..?!"*

→ **학생 자가 입장 명시 + reviewer 의견 요청** = PR #35·#126 (hyemdooly·doabletuple *학생 자가 입장 명시*) 결 합류.

reviewer 후속 (Thread 15):

> *"BlackJackGame 이라는 도메인 모델이 존재한다면 보다 명확해질 수 있을까요?"*

학생 자가 적용 → *BlackjackGame 도메인 객체 자가 생성*. 

**MCP 룰 `controller-as-game-mediator-vs-domain-game-object` 후보** — *Controller 가 게임 흐름 vs BlackjackGame 도메인 객체* 트레이드오프.

### 2. `Hit/Stay Boolean` vs `Action enum` — PR #117·#126 결과 반대 시점

reviewer (Thread 1):

> *"Hit or Stay를 Boolean으로 구분할 수 있지 않을까요? 처음에 Action 이라는 키워드를 보고 어떤 의미인지 판단하기 쉽지 않을 것 같습니다."*

학생 자가 적용: `readPlayerHit(player): Boolean`.

→ **`Action enum` (PR #117·#126) vs `Boolean` (PR #113)** = **2 reviewer 의 *반대 시점 자가 적용*** = *MCP `oop_propose_alternatives` 핵심 가치 합류*:

- **PR #117·#126** (reviewer KwonDae·woowahan-dino): *String → Action enum (Stringly-typed 회피)*.
- **PR #113** (reviewer KwonDae): *Action → Boolean (의미 직관)*.

→ **같은 reviewer (KwonDae) 의 *다른 학생에 다른 권유*** = *학생 컨텍스트 별 트레이드오프*.

**MCP 룰 `enum-vs-boolean-context-dependent` 후보** — *enum vs Boolean = 의미 명시 vs 직관성 트레이드오프*.

### 3. `BlackjackGame` 도메인 객체 자가 적용 + 룰 응집

reviewer (Thread 15): *"BlackJackGame 이라는 도메인 모델이 존재한다면 보다 명확해질 수 있을까요?"*

학생 자가 적용:

```kotlin
fun distributeInitialCards() {
    repeat(INITIAL_CARD_COUNT) {
        participants.drawCard(deck)
    }
}
```

→ **게임 룰 도메인 응집 + Controller 가벼움** = PR #129·#126·#142 결 합류.

### 4. 학생 자가 *현실 메타포 = Controller = 게임 중재자/카지노* (Thread 2)

§1 인용. 학생이 자가 *Controller = 카지노* 메타포 사용 → reviewer *Controller 가 흐름 조율 vs 참가자 자가 뽑기* 권유.

→ **현실 메타포 양가성** = PR #117·#129·#130·#143·#109 결의 *현실 메타포 정합* 어휘 누적 12+ PR ⭐⭐⭐.

다만 *PR #129 reviewer = 참가자 자가 / PR #113 학생 자가 = Controller 중재자* = **메타포 양가**.

### 5. `abstract class Participant` + canHit/getResult 추상 메서드 자가 학습

학생 자가 답변 (Thread 16):

> *"`interface`는 상태를 가질 수 없으며, `open class`는 하위 클래스에서 공통 메서드를 구현하도록 강제할 수 없습니다. 딜러와 플레이어의 공통된 상태와 역할을 제공하면서, 일부 구체적인 로직은 각 클래스에서 구현하도록 하기 위해 `abstract class`를 사용했습니다!"*

→ **abstract class vs open vs interface 3 분류 학생 자가 학습** = PR #129·#126·#138·#142·#143 결 합류 누적 6+ PR.

### 6. 학생 자가 후속 의문 + reviewer 자가 권유 수정 (Thread 20·21)

학생 자가 의문 (Thread 20):

> *"플레이어는 'y'를 입력하면 카드를 추가로 뽑을 수 있고, 딜러는 점수가 16 이하이면 카드를 추가로 뽑을 수 있습니다. 이처럼 카드를 뽑는 공통 역할이 있어 `Player`와 `Dealer`의 `playGame`를 추상화하고 싶었습니다.*
>
> *하지만 각 클래스의 `playGame`에서 필요로 하는 파라미터가 달라 다소 복잡해졌는데, 제이든이 보시기에는 어떠신지, 더 나은 추상화 방법이 있는지 피드백 부탁드립니다..! 🙏🏻"*

reviewer 자가 수정 (Thread 20):

> *"이 부분은 수정할게요. playGame의 내용은 각 참가자별로 게임을 진행하는 로직으로 Participant로의 추상화 보다는 Participants에서 역할을 할 수 있지 않을까 생각이 들었습니다. 현재 참가자의 결과를 계산하는 `getPlayerResults` 처럼 playGame은 Player 와 Dealer의 추상화 보다는 이미 추상화된 `canHit(), drawCard()` 를 사용하면 지금 하고 계신 고민들에 도움이 될 것 같아요"*

→ **시그니처 차이 추상화 한계 + reviewer 자가 권유 수정 + 이미 추상화된 메서드 활용 권유** = PR #126 (abstract-method-blocked-by-signature-divergence) 결 강화 + PR #142·#109 (reviewer 자가 한계 인정/수정) 결 합류.

**MCP 룰 `reviewer-self-suggestion-revision` 후보** — *reviewer 가 자가 권유 수정 + 더 나은 안 제시*.

### 7. 학생 자가 ShuffledCardGenerator + Deck(cards) DI 자가 적용

reviewer (Thread 8): *"Deck이 항상 랜덤으로 생성될 경우 해당 도메인은 테스트가 어려워집니다."*

학생 자가 적용:

```kotlin
class ShuffledCardGenerator {
    fun generate(): List<Card> = ...
}

class Deck(cards: List<Card>) {
    val cards: ArrayDeque<Card> = ArrayDeque(cards)
    fun pick(): Card = cards.removeLast()
}
```

→ **DI + 카드 생성 분리** = PR #138·#143 (Deck DI) 결 합류. **MCP 룰 `interface-for-test-substitution` 강화** 누적 6+ PR.

### 8. 추가 칭찬

- Thread 19: *"공통 로직 추상화 잘해주셨어요 👍"*.
- Thread 22: *"참가자들을 관리하여 역할을 분리할 수 있게 되었네요! 👍"*.

## 못한 점 (설계 관점)

### 1. `Deck.pick(): Card?` nullable 반환 — null vs 예외

reviewer (Thread 4): *"만약 덱이 비어있으면 어떤 일이 발생할까요?"*

학생 자가 답변:

> *"덱이 비어있는 경우에 예외를 던질지 고민했는데, 카드가 없는 것도 정상적인 게임 상황이라고 판단하고 게임을 결과를 출력하도록 했습니다! 덱이 비어있으면 null을 반환하고, `controller`에서 null을 확인하여 프로그램을 종료하고 결과를 출력합니다."*

→ **null 반환 정공** = PR #35 (galcyurio Kotlin 예외 처리 4 원칙) 결의 *논리 오류 X = null 반환* 정공. **MCP 룰 `kotlin-exception-handling-strategy` 강화** 누적 4+ PR (#129·#138·#35·#113).

### 2. Deck companion → 인스턴스 변경 (학생 자가 자가 정정)

reviewer (Thread 5): *"Deck의 변수와 함수들을 모두 companion object에 구현한 배경은 무엇이었을까요? 🧐"*

학생: *"여기저기서 `Deck`에 접근할 수 있을 것 같아 현재는 인스턴스를 만들어 사용하도록 변경했습니다!"*

→ **Singleton 자가 회피 + 인스턴스 정공** = PR #130·#138·#143 (Singleton Deck 안티패턴) 결 누적 20+ PR ⭐⭐⭐.

### 3. Players 일급 컬렉션 사용자 입력 한계 학생 자가 의문

reviewer (Thread 7): *"만들어주신 Players 일급컬렉션의 역할을 제대로 하고 있는걸까요?"*

학생 자가 의문:

> *"`Players` 일급컬렉션을 활용하는 방법을 고민해보았지만, 플레이어는 매번 카드를 뽑을지 여부를 view에서 입력받아야 해서 해당 로직을 도메인으로 옮기는 것이 쉽지 않았습니다. 😢 현재 구조에서는 사용자의 입력이 필요한 부분이 존재하여 `Players` 내부에서 구현하기에 한계가 있는데, 이를 해결할 수 있는 방법이 있을까요..?"*

reviewer 답변 (Thread 14): *"입력받는 로직을 domain으로 옮겨야만 해결이 가능할까요? hint. kotlin에는 [고차함수](https://kotlinlang.org/docs/lambdas.html)라는게 존재합니다."*

학생 자가 적용 + 자가 한계 인정:

> *"고차함수를 활용하여 참가자들이 각자 게임을 진행하도록 변경해봤습니다! 다만, 아직 고차함수가 익숙하지 않아 적절하게 사용하였는지는 잘 모르겠습니다..! 😢"*

→ **사용자 입력 vs 도메인 = 고차함수 정공 + 학생 자가 학습 한계 인정** = PR #87·#129·#138·#126 결.

### 4. Result enum 도메인 vs 뷰 (PR #126·#143 결과 같은 어휘)

reviewer (Thread 10): *"승, 패, 무 는 도메인 로직일까요?"*

학생 자가 답변:

> *"해당 값은 출력을 할 때만 필요한 정보이므로 뷰 로직이라고 생각합니다! `OutputView`에서 처리하도록 수정했습니다."*

→ **String 출력 = View 책임** = PR #126·#130·#143 결 강화 누적 4+ PR.

### 5. Rank 기호 표시 = 도메인 vs 뷰 (변경 가능성 지표)

reviewer (Thread 12): *"만약 A 대신 기호로 표시해달라는 요구사항이 온다면 도메인 로직을 수정해야 할까요?"*

→ **변경 가능성 = View 책임 지표** = PR #126 (KwonDae 동일 어휘) 결 강화 누적 2+ PR.

### 6. 인자 폭증 (4 람다 인자) — 학생 자가 의문

학생 코드 (Thread 21):

```kotlin
fun playTurns(
    onPlayerResponse: (Player) -> Boolean,
    onPlayerDraw: (Player) -> Unit,
    onDealerDraw: (Dealer) -> Unit,
) {
    participants.playGame(deck, onPlayerResponse, onPlayerDraw, onDealerDraw)
}
```

학생 자가 의문:

> *"`participants`에게 메세지를 던지는 방식으로 변경했습니다! 다만, 이렇게 변경하니 인자를 너무 많이 넘겨주는 건 아닌가 싶긴 하네요.. 😅"*

→ **콜백 람다 폭증 학생 자가 인지** = PR #36·#142·#35 (`callback-lambda-explosion`) 결 합류. 

### 7. 결과 계산 책임 분배 + 학생 자가 의문 2 가지 (Thread 11)

학생 자가 의문:

> *"1. 플레이어는 `승 or 패`의 결과가 나오는 반면, 딜러는 모든 플레이어와 비교하여 `n승 n패` 형식의 결과가 나옵니다. 그렇기에 controller에서 딜러의 결과를 계산하는 로직은 아직 남아있습니다.*
>
> *2. 변경되기 전의 코드는 모든 플레이어의 결과를 계산한 후, 그 결과를 바탕으로 딜러의 결과를 계산했습니다. 플레이어의 결과에 따라서 딜러의 결과가 결정된다고 생각해서 이렇게 구현했었습니다. 다른 참가자와 비교하여 결과를 계산하도록 변경하면서 동일한 비교를"*

→ **Player 1대1 결과 vs Dealer n승n패 형식 비대칭 + 동일 비교 반복** = PR #142 (doabletuple 자가 코드 예시 + 자가 의문) 결 합류. **MCP 룰 `symmetric-compare-bidirectional-call` 강화** 누적 3+ PR (#142·#143·#113).

### 8. `Players.players` 2 depth 일급 컬렉션 → Participants 자가 적용

reviewer (Thread 17): *"2 depth로 들어가는 `players.players` 으로도 일급컬렉션의 용도가 생기는 부분 같아요..!"*

학생 자가 적용: *Participants 클래스 추가 (딜러 + 플레이어 관리)*.

→ **PR #129 (compile-time-singleton + Participants 생성자 타입 분리) 결 합류**.

### 9. `Hand` 생성자 vs emptyList 기본값

reviewer (Thread 3): *"현재 생성자가 사용되는곳은 Participant 뿐이어서 생성자로 받지 않아도 차이는 없어보이네요!"*

학생 자가 적용: `Hand` 기본 `emptyList()`.

→ **불필요한 생성자 인자 제거** — PR #109·#138 (test fixture 생성자) 결과 같은 어휘. 

### 10. 확장 함수 위치 (Controller 내부 vs Domain)

reviewer (Thread 18): *"Participant 확장함수가 Controller 내부에 있는게 자연스러울까요? 🧐"*

학생: *"덱에 카드가 비어있으면 다시 카드를 넣어주도록 변경하여 해당 확장함수는 삭제했습니다!"*

→ **확장 함수 위치 = 도메인 위치 정공**. **MCP 룰 `extension-function-locator` 후보**.

### 11. `Dealer.canHit() = score <= 16` + `Player.canHit() = score <= 21`

```kotlin
abstract fun canHit(): Boolean
```

→ **각자 다른 기준 자가 적용** = PR #129·#126·#138·#142·#143 결.

다만 *Player canHit `score <= 21`* — *21 시 hit 가능* = PR #129·#138·#139·#126·#130·#142·#143 결과 동일 (21 시 hit 가능 모델). PR #35 (21 시 stop) 반대 시점.

### 12. `getResult` 추상 메서드 + Player/Dealer override

```kotlin
abstract fun getResult(other: Participant): Result
```

→ **Tell-Don't-Ask 정공 + 양쪽 자가 비교** = PR #142·#143·#109 결.

### 13. 한 도메인 객체 (`BlackjackGame`) 다른 도메인 객체 (`Participants`) 동시 보유 — 책임 중첩

학생 코드: *BlackjackGame + Participants 동시 존재* = PR #142·#143 (Game/Scoreboard 분리) 결 합류. *Controller → BlackjackGame → Participants 3 단계 메시지*.

### 14. 모든 22 스레드 학생 자가 답변 + commit SHA + 자가 후속 의문 5+ 회 — 2025 코호트 정점

## 리뷰어 의견 요지

- **Hit/Stay Boolean 권유 (PR #117·#126 반대 시점)**.
- **Controller 흐름 vs 참가자 자가 분배 (학생 자가 입장 + reviewer 양방향)**.
- **Hand 생성자 제거 + emptyList 기본값**.
- **Deck null 반환 vs 예외**.
- **Deck companion → 인스턴스 변경**.
- **canHit 공통 로직 자가 학습**.
- **Players 일급 컬렉션 사용자 입력 한계 학생 자가 의문 + 고차함수 권유**.
- **테스트 가능성 = Deck DI + ShuffledCardGenerator 분리**.
- **Result enum 도메인 vs 뷰**.
- **getResult 추상 메서드 + 학생 자가 의문 2가지 (Player 1대1 vs Dealer n승n패)**.
- **Rank 기호 표시 = 도메인 vs 뷰 (변경 가능성)**.
- **고차함수 입력받기 + 학생 자가 한계 인정**.
- **BlackjackGame 도메인 객체 권유 + 학생 자가 적용**.
- **abstract vs open vs interface 자가 학습**.
- **Players.players 2 depth + Participants 일급 컬렉션 자가 적용**.
- **playGame 추상화 → Participants 역할 (reviewer 자가 수정)**.
- **확장 함수 위치 (Controller vs Domain)**.

## 대표 인용

> [KwonDae Thread 1]
> *"Hit or Stay를 Boolean으로 구분할 수 있지 않을까요? 처음에 Action 이라는 키워드를 보고 어떤 의미인지 판단하기 쉽지 않을 것 같습니다."*

→ **enum vs Boolean = 의미 명시 vs 직관성** — *PR #117·#126 정공 (enum) 반대 시점*.

> [jiyuneel Thread 2]
> *"`controller`를 게임의 중재자 또는 카지노라고 생각했습니다. 참가자가 처음에 뽑아야 하는 카드의 수(2장)는 게임의 룰에 해당하며, 이를 `controller`가 관리하는 것이 가장 바람직하다고 판단했습니다..!"*

→ **학생 자가 *Controller = 카지노* 현실 메타포** — *PR #129 (참가자 자가) 반대 시점 양가*.

> [KwonDae Thread 20]
> *"이 부분은 수정할게요. (...) playGame은 Player 와 Dealer의 추상화 보다는 이미 추상화된 `canHit(), drawCard()` 를 사용하면 지금 하고 계신 고민들에 도움이 될 것 같아요"*

→ **reviewer 자가 권유 수정 + 이미 추상화 활용** — *PR #142·#109 결 합류*.

> [jiyuneel Thread 21]
> *"`participants`에게 메세지를 던지는 방식으로 변경했습니다! 다만, 이렇게 변경하니 인자를 너무 많이 넘겨주는 건 아닌가 싶긴 하네요.. 😅"*

→ **학생 자가 콜백 람다 폭증 인지** — *PR #36·#142 결 합류*.

## 얻은 교훈 (보편)

1. **enum vs Boolean = 의미 명시 vs 직관성 트레이드오프 (PR #117·#126 반대 시점)** — **MCP 룰 `enum-vs-boolean-context-dependent` 후보**.

2. **Controller = 게임 중재자/카지노 vs 참가자 자가 (현실 메타포 양가)** — *PR #129 결과 반대 시점*. **MCP 룰 `controller-as-game-mediator-vs-domain-game-object` 후보**.

3. **reviewer 자가 권유 수정 + 이미 추상화 활용** — **MCP 룰 `reviewer-self-suggestion-revision` 후보**.

4. **사용자 입력 vs 도메인 = 고차함수 정공 + 학생 자가 한계 인정** — PR #87·#129·#138·#126 결.

5. **결과 비대칭 (Player 1대1 vs Dealer n승n패) + 동일 비교 반복** — **MCP 룰 `symmetric-compare-bidirectional-call` 강화** 누적 3+ PR.

6. **Players 일급 컬렉션 사용자 입력 한계 학생 자가 의문 + 고차함수 답변** — *학생 자가 학습 정점*.

7. **null 반환 = 정상 게임 상황 (Kotlin 예외 처리 4 원칙)** — **MCP 룰 `kotlin-exception-handling-strategy` 강화** 누적 4+ PR.

8. **현실 메타포 양가 (Controller 카지노 vs 참가자 자가)** — **MCP 룰 `metaphor-fidelity-locator` 강화** 누적 12+ PR ⭐⭐⭐.

9. **abstract vs open vs interface 학생 자가 3 분류** — *PR #129·#126·#138·#142·#143·#113 결 합류 누적 6+ PR*.

10. **확장 함수 위치 (Controller vs Domain)** — **MCP 룰 `extension-function-locator` 후보**.

11. **변경 가능성 = View 책임 지표 (PR #126 결)** — *Rank 기호 표시*.

12. **모든 22 스레드 학생 자가 답변 + commit SHA + 자가 후속 의문 5+ 회** — *2025 코호트 정점 유지*.

## 얻은 교훈 (도메인)

1. **Dealer canHit `score <= 16` + Player canHit `score <= 21`** — *각자 기준 자가 적용*.

2. **카드 N장 뽑기 = 도메인 룰 (학생 자가 *카지노* 메타포 + reviewer *참가자 자가* 메타포 양가)**.

3. **Player 1대1 결과 vs Dealer n승n패 결과 비대칭** — *학생 자가 의문*.

## MCP 룰 후보 신규 + 강화 (PR #113)

| 룰 | 신규 / 강화 | 누적 PR |
|---|---|---|
| `enum-vs-boolean-context-dependent` | 신규 | 1 (#113) |
| `controller-as-game-mediator-vs-domain-game-object` | 신규 | 1 (#113) |
| `reviewer-self-suggestion-revision` | 신규 | 1 (#113) |
| `symmetric-compare-bidirectional-call` | 강화 | 3+ (#142·#143·#113) |
| `metaphor-fidelity-locator` | 강화 | 12+ ⭐⭐⭐ |
| `kotlin-exception-handling-strategy` | 강화 | 4+ |
| `extension-function-locator` | 신규 | 1 (#113) |
| `interface-for-test-substitution` | 강화 | 6+ |
| `string-formatting-view-responsibility` | 강화 | 4+ |
| `singleton-deck-via-companion` | 강화 | 20+ ⭐⭐⭐ |
| `abstract-without-abstract-method` | 강화 | 6+ |

## 메타 어휘 (14 reviewer + MCP 핵심 가치 합류 정점 유지)

- ***학생 자가 *Controller = 카지노* 현실 메타포*** — jiyuneel PR #113 — *현실 메타포 양가성*.
- ***reviewer 자가 권유 수정 + 이미 추상화 활용*** — KwonDae PR #113 — *PR #142·#109 결 합류*.
- ***Hit/Stay enum vs Boolean = 같은 reviewer 의 다른 학생 다른 권유*** — KwonDae PR #117·#126 (enum) vs PR #113 (Boolean) — *학생 컨텍스트 별 트레이드오프*.
- ***학생 자가 콜백 람다 폭증 인지*** — jiyuneel PR #113 — *PR #36·#142 결 합류*.

→ **14 reviewer + MCP 핵심 가치 어휘 합류 정점 유지**. **2025 코호트 9 PR 코퍼스 성장**.
