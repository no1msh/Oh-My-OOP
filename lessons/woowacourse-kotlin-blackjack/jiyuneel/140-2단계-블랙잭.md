# PR #140 — [블랙잭 2단계] 포르 미션

- **Reviewee:** [@jiyuneel](https://github.com/jiyuneel)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/140
- **단계:** 2단계
- **리뷰어:** [@KwonDae](https://github.com/KwonDae) (제이든) — 1단계 #113 동일 reviewer
- **라인 코멘트:** 16 스레드 — *PR 본문 자가 의문 + 학생 자가 의문 5+ 회 + 람다 4개 인자 폭증 자가 의문 (PR #142 결과 동일) + 고차함수 = View 의존 X 학생 자가 학습 + 다형성 compare 추상화 학생 자가 코드 예시 + 수익률 = GameResult 책임 (PR #142 결) + abstract → sealed class 학생 자가 변경 + sealed 라이브러리 배포 시 제약 reviewer 메타 어휘 + Profit 팩토리 메서드 자가 적용 + thisScore 외부 의존 → 본인 점수 자가 (Tell-Don't-Ask) + tailrec 자가 적용*
- **양 단계 완성 reviewee** (1단계 #113 batch 21 분석 완료 + 2단계 #140 = batch 22 추가).

## 잘한 점 (설계 관점)

### 1. PR 본문 자가 학습 의식 + 학생 자가 의문 5+ 회

PR 본문:

> *"제이든, 안녕하세요 :) 2단계 미션을 진행하면서 고민한 부분이 많아서 조금 늦어진 것 같아요 😂 이전 피드백도 #113 모두 반영하고자 노력했습니다!"*

→ **학생 자가 학습 적극성 + 1단계 → 2단계 사이 학습 시간성** = PR #138·#142·#143 결 합류.

### 2. 람다 4개 인자 폭증 학생 자가 의문 (PR #142 결과 동일)

학생 자가 의문 (Thread 1):

> *"하지만 변경해보니 고차함수를 `controller` → `BlackjackGame` → `Participants`로 넘기게 되었고, 너무 많이 넘겨주는 건 아닌가 싶어 해결할 방법도 고민해봤지만 방법을 찾지 못했습니다.. 😢 또한 4개의 매개변수도 많지 않나 싶었지만 플레이어와 딜러가 게임을 하려면 모두 필요한 정보여서 이렇게 구현하게 되었습니다."*

reviewer 답변 (Thread 5):

> *"여러 가지 방법이 있을 것 같아요*
> *- onDraw 를 하나의 람다로 사용할 수 없을까?*
> *- 불가능하다면 람다 매개변수를 묶어서 활용해볼 수 있을까?"*

학생 자가 적용:

```kotlin
// onPlayerDraw + onDealerDraw 통합
when (participant) {
    is Player -> ...
    is Dealer -> ...
}
```

→ **람다 통합 = 타입 체크로 분기** + **`abstract class` → `sealed class` 학생 자가 변경 (when exhaustive)**. PR #142 (lambda-multiple-args-tuple-leak) 결 합류.

### 3. 고차함수 = View 의존 X 학생 자가 학습

reviewer (Thread 2):

> *"고차함수는 View에 대한 직접적인 의존성을 제거하고, 추상화된 동작(람다)만을 전달받아 실행합니다. 따라서 도메인은 View의 구체적인 구현을 알 필요가 없기에 의존한다고 볼 수 없습니다. 고차함수를 사용함으로써 어떤 이점이 있었을까요?*
> *- 특정 행동 추상화*
> *- 전달받은 action 만 실행하므로 실제 내부 구현은 관심 X*
> *- 테스트"*

학생 자가 답변:

> *"고차함수를 사용하면 도메인은 추상화된 행동만 전달받기 때문에 구체적인 구현을 알 수 없습니다! `Participant`의 입장에서는 단순히 `카드를 뽑았을 때 어떠한 행동을 해야 돼` 정도만 알 뿐, 그 행동이 어떤 것인지는 알지 못합니다. 또한, 테스트 코드에서는 `{ true }`와 같이 전달하면 되므로 view와 무관하게 테스트를 할 수 있고 테스트가 비교적 용이해집니다!"*

→ **고차함수 = 추상화된 행동 + 테스트 용이성** = PR #109 (5 라운드 양방향 토론 + reviewer 자가 정정) 결 합류. **MCP 룰 `higher-order-function-as-domain-view-decoupling` 후보**.

### 4. `abstract class` → `sealed class` 학생 자가 변경 (when exhaustive)

학생 자가 적용 (Thread 5):

> *"또한 이 과정에서 when 문에서 모든 하위 클래스를 처리할 수 있도록 `abstract class`였던 `Participant` 클래스를 `sealed class`로 변경했습니다."*

reviewer (Thread 7): 외부글 (codestates 다형성 + stackoverflow interface/abstract/sealed 비교).

학생 자가 답변:

> *"`sealed class`로 구현하면 `Participant`를 상속하는 새로운 타입이 추가되었을 때, 컴파일 타임에 모든 하위 타입을 확인할 수 있어 더 안전하다고 생각했습니다..! 특히 `when` 문에서 모든 타입을 처리하도록 강제하고 예상치 못한 타입을 방지할 수 있다는 장점이 있다고 생각했는데, 혹시 제가 고려하지 못한 점이 있을까요..? 😯"*

reviewer 메타 어휘 (Thread 13):

> *"맞아요 sealed class 는 compiler 가 나머지 분기에 대해 처리하지 않아도 된다는 장점이 존재합니다. 그렇다면 포르가 이 블랙잭 미션을 라이브러리로 배포한다고 했을 때, 사용자들에게 Paricipant를 확장할 수 있도록 제공한다고 가정해본다면 어떤 일이 발생할 수 있을지 고민해볼까요?*
>
> *hint. 직접 패키지를 새로 만들어 Participant 를 확장해보기"*

→ **sealed class 라이브러리 배포 시 사용자 확장 제약** = *sealed = 모듈 한정 = 외부 확장 X*. **MCP 룰 `sealed-class-library-extension-constraint` 후보**.

PR #129·#126·#138·#142·#143·#113 결의 *sealed vs abstract vs open* 결 합류 정점.

### 5. `Profit` 팩토리 메서드 + 자가 계산 (PR #142 cashOut 결 합류)

```kotlin
fun from(bettingAmount: BettingAmount, gameResult: GameResult): Profit =
    Profit(bettingAmount * gameResult.rate)
```

→ **Profit 자가 계산 + 팩토리 메서드** = PR #142 `GameResult.cashOut(player)` 결 합류.

### 6. operator overloading `times` 자가 적용

```kotlin
operator fun times(rate: Double): Double = value * rate
```

→ **value class + operator times** = PR #142·#36 결 합류.

### 7. tailrec 자가 적용 + 학생 자가 어휘

학생 자가 답변 (Thread 10):

> *"새로 알게 된 `tailrec` 키워드를 활용하여 변경해보았습니다! 재귀로 호출하여 코드의 가독성을 높이면서, 컴파일 과정에서 while 문으로 변환하여 성능도 최적화할 수 있어 좋은 것 같습니다 👍🏻"*

```kotlin
tailrec fun playGame(draw: () -> Card, shouldContinue: ..., onDraw: ...) {
    if (canHit() && shouldContinue(this)) {
        receiveCard(draw())
        onDraw(this)
        playGame(draw, shouldContinue, onDraw)
    }
}
```

→ **`tailrec` 자가 학습 + 적용** = PR #138·#142 (rosemin928·doabletuple) 결 합류 누적 3+ PR.

### 8. compare 공통 로직 추상화 + 학생 자가 코드 예시 (Thread 3)

```kotlin
// Participant.kt
protected fun compare(thisScore: Score, otherScore: Score): GameResult =
    when {
        thisScore > otherScore -> GameResult.WIN
        thisScore < otherScore -> GameResult.LOSE
        else -> GameResult.DRAW
    }
```

→ **공통 비교 로직 추출** = PR #142 (super 키워드) 결 합류.

### 9. 추가 칭찬

- Thread 8: *"중복 로직이 깔끔하게 없어졌네요 👍"*.
- Thread 14: *"연산자 오버로딩을 잘 활용해주셨어요 👍"*.
- Thread 15: *"입력받는 곳에서도 재귀를 잘 활용해주셨습니다 👍"*.
- 모든 16 스레드 학생 자가 답변 + commit SHA.

## 못한 점 (설계 관점)

### 1. `thisScore` 외부 의존 → 본인 점수 자가 (Tell-Don't-Ask)

reviewer (Thread 12):

> *"thisScore 라는 값을 외부로 부터 받아야 할까요? 참가자는 본인의 점수를 알고 있습니다. '나'의 점수와 '상대방' 점수를 비교하면 되는 로직일거에요. 그렇다면 나의 점수도 외부 값에 의존해야 할까요? 또는 비교해야 할 대상이 늘어난다면 그만큼 함수 또는 파라미터가 늘어나야 할까요?.?*
>
> *자식클래스는 다른 참가자를 받는데 왜 부모클래스는 두 참가자를 받아야만 하는걸까요?"*

→ **부모 클래스 두 점수 vs 본인 점수 자가** = *Tell-Don't-Ask 정공*. **MCP 룰 `parameter-vs-self-state-access` 후보**.

### 2. sealed class 라이브러리 확장 제약 — §4 참조

reviewer 의 메타 어휘 (Thread 13) = *sealed class 의 라이브러리 외부 확장 X 제약 = 사용자 확장 가능성 의식*.

→ **라이브러리 사용자 시점 어휘** = PR #87·#76·#107·#82 (maintainer perspective locator) 결 + **사용자 확장 의식** 어휘 합류.

**MCP 룰 `sealed-class-library-extension-constraint` 후보** — *sealed = 모듈 내부 + 라이브러리 배포 시 사용자 확장 X*.

### 3. else 분기 처리 = 논리 오류 (Thread 6)

reviewer (Thread 6):

> *"현재 else 분기처리로 들어온다는 것은 어떤 오류에 해당할까요? 또한 어떤 방법으로 오류 대응을 할 수 있을까요?"*

학생: *"추상 클래스를 사용했을 때 `else` 분기처리로 들어오는 상황은 예상하지 못한 타입이 들어왔을 때 발생하는 논리적 오류입니다. 이때 `IllegalArgumentException`와 같은 예외를 던져 대응할 수 있을 것입니다."*

→ **else 분기 = 논리 오류** = PR #35 (Kotlin 예외 처리 4 원칙 - 논리 오류 시 예외) 결 합류.

### 4. 테스트 범위 (Thread 16) — 역할 중심

학생 자가 의문:

> *"테스트 코드를 작성하는 과정에서, 어느 정도까지 테스트를 해야 하는지에 대한 의문이 생겼습니다. 현재 제 코드에서 게임 실행이 controller → BlackjackGame → Participants → Player 순서로 호출하여 진행됩니다. 테스트 코드를 전부 작성하지는 못했지만,, 각 클래스에서 게임 실행에 대한 테스트가 중복될 것 같은데, 어떤 범위까지 테스트를 작성해야 할지에 대한 조언을 구하고 싶습니다..!"*

reviewer 답변:

> *"먼저 각 클래스와 객체별로 역할을 중심으로 테스트를 작성하면 됩니다. (...) BlackjackGame: 게임 규칙과 진행을 관리하는 역할 → 게임 규칙과 주요 로직. Participants 및 Player: 개별 [...]"*

→ **테스트 = 역할 중심 (PR #87·#129·#142·#109 결 합류)** = *MCP `oh-my-oop` 핵심 가치 (역할·책임·협력) 어휘 정점*.

**MCP 룰 `test-by-role-not-by-flow` 후보** — *역할 중심 테스트 vs 흐름 중심 테스트*.

### 5. `super` 키워드 학생 자가 답변 (Thread 8)

reviewer: *"중복 로직이 깔끔하게 없어졌네요 👍 그렇다면 super 키워드에 대해 찾아보시면 더 도움이 될 것 같습니다 🙃"*

학생 자가 답변:

> *"부모 클래스인 `Participant`의 `compare` 함수는 두 참가자의 점수를 비교하는 연산을 하고, 자식 클래스의 `resultAgainst` 함수는 다른 참가자를 받아와 결과를 계산합니다. 두 함수의 함수 시그니처가 달라 super 키워드를 사용하지 않아도 되었던 것 같습니다..!"*

→ **시그니처 차이 = super 활용 불필요** + *PR #142 결의 *시그니처 차이 추상화 한계* 어휘 합류*.

### 6. `BlackjackResult` 클래스 - 게임 결과 + 수익 계산

```kotlin
// BlackjackResult - 수익 계산
```

→ **결과 통계 + 수익 계산 책임 분리** = PR #142 (Scoreboard) 결 합류. **MCP 룰 `result-aggregation-locator` 강화** 누적 3+ PR (#126·#142·#140).

### 7. 모든 16 스레드 학생 자가 답변 + commit SHA — 2025 코호트 정점 유지

## 대표 인용

> [KwonDae Thread 2]
> *"고차함수는 View에 대한 직접적인 의존성을 제거하고, 추상화된 동작(람다)만을 전달받아 실행합니다."*

→ **고차함수 = 도메인 ↔ View 의존 X** — PR #109 결과 합류.

> [KwonDae Thread 13]
> *"그렇다면 포르가 이 블랙잭 미션을 라이브러리로 배포한다고 했을 때, 사용자들에게 Paricipant를 확장할 수 있도록 제공한다고 가정해본다면 어떤 일이 발생할 수 있을지 고민해볼까요?"*

→ **라이브러리 배포 시점 메타 어휘** — *사용자 확장 의식*.

> [KwonDae Thread 12]
> *"thisScore 라는 값을 외부로 부터 받아야 할까요? 참가자는 본인의 점수를 알고 있습니다."*

→ **Tell-Don't-Ask 어휘**.

> [jiyuneel Thread 4]
> *"참가자는 각자의 승패를 판단하고, `BlackjackResult`는 게임 결과를 바탕으로 수익 계산을 함으로써 각 객체의 책임이 분리되었습니다!"*

→ **학생 자가 책임 분리 어휘**.

## 얻은 교훈 (보편)

1. **람다 4개 인자 폭증 학생 자가 의문 + reviewer 람다 통합 권유** — PR #142·#143 결 합류 + sealed class 변경.

2. **고차함수 = 도메인 ↔ View 의존 X** — **MCP 룰 `higher-order-function-as-domain-view-decoupling` 후보**.

3. **`abstract class` → `sealed class` 학생 자가 변경 (when exhaustive + 라이브러리 제약)** — **MCP 룰 `sealed-class-library-extension-constraint` 후보**.

4. **`thisScore` 외부 의존 → 본인 점수 자가 (Tell-Don't-Ask)** — **MCP 룰 `parameter-vs-self-state-access` 후보**.

5. **테스트 = 역할 중심** — **MCP 룰 `test-by-role-not-by-flow` 후보**.

6. **tailrec 자가 적용** — 누적 3+ PR.

7. **Profit 팩토리 메서드 자가 계산** — PR #142 결 합류.

8. **operator overloading 자가 활용** — PR #36·#142 결.

9. **else 분기 = 논리 오류** — PR #35 결.

10. **결과 통계 + 수익 계산 책임 분리 (BlackjackResult)** — **MCP 룰 `result-aggregation-locator` 강화** 누적 3+ PR.

## MCP 룰 후보 신규 + 강화 (PR #140)

| 룰 | 신규 / 강화 | 누적 PR |
|---|---|---|
| `higher-order-function-as-domain-view-decoupling` | 신규 | 1 (#140) |
| `sealed-class-library-extension-constraint` | 신규 | 1 (#140) |
| `parameter-vs-self-state-access` | 신규 | 1 (#140) |
| `test-by-role-not-by-flow` | 신규 | 1 (#140) |
| `tailrec-optimization-applicability` | 강화 | 3+ (#138·#142·#140) |
| `result-aggregation-locator` | 강화 | 3+ (#126·#142·#140) |
| `super-keyword-for-shared-logic` | 강화 | 3+ (#138·#142·#140) |
| `sealed-vs-abstract-vs-open-locator` | 강화 | 7+ |
| `kotlin-exception-handling-strategy` | 강화 | 5+ |

## 비교 (PR #113 → #140)

- **1단계 #113 22 thread → 2단계 #140 16 thread**.
- **1단계 #113 Hit/Stay Boolean → 2단계 #140 sealed class 학생 자가 변경**.
- **1단계 #113 Players 일급 컬렉션 사용자 입력 한계 자가 의문 → 2단계 #140 고차함수 자가 학습**.
- **1단계 #113 BlackjackGame 도메인 객체 자가 적용 → 2단계 #140 BlackjackResult + Profit 책임 분리**.
- **2단계 #140 학생 자가 학습 정점 유지** (commit SHA + 자가 의문 5+ 회).
