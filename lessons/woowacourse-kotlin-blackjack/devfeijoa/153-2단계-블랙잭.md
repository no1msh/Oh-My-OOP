# PR #153 — [블랙잭 2단계] 이든 미션

- **Reviewee:** [@devfeijoa](https://github.com/devfeijoa)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/153
- **단계:** 2단계
- **리뷰어:** [@Gyuil-Hwnag](https://github.com/Gyuil-Hwnag) (두루) — 1단계 #121 동일 reviewer (2024→2025 다년 활동)
- **라인 코멘트:** 16 스레드 — *PR 본문 *DM 으로 얘기했던 것처럼 새로 코드 작성 + 마감시간이 곧* 학생 자가 한계 인정 + callback 어휘 학생 자가 *푹 빠짐* + Players 8명 제한 블랙잭 도메인 룰 자가 학습 + State interface (Hit/Stay/Finished) + dead code (사용 안 하는 객체) + 테스트 실패 + 패키지 분리 권유*
- **양 단계 완성 reviewee** (1단계 #121 batch 22 분석 완료 + 2단계 #153 = batch 23 추가).

## 잘한 점 (설계 관점)

### 1. PR 본문 자가 한계 인정 (마감시간 + DM 토론)

PR 본문:

> *"안녕하세요 두루 ! 앞서 DM으로 얘기했던것 처럼 새로 코드를 작성하였습니다. 빠진 기능이 꽤나 존재하지만 마감시간이 곧이라 일단 제출하여 피드백을 받고자 합니다..!"*

→ **학생 자가 마감시간 한계 + 새로 코드 작성 + DM 토론** = PR #130·#139·#143·#121 (학생 자가 한계 인정) 결 합류. **DM 활용** = PR #129·#138·#142·#121 결 합류.

### 2. Players 8명 제한 블랙잭 도메인 룰 자가 학습 (Thread 6)

학생: *"원래 블랙잭 도메인이 가지고 있는 조건인 참가자를 8명까지 제한한다라는 조건을 적용했습니다 !"*

→ **블랙잭 도메인 룰 자가 학습** = PR #138 (rosemin928 *8 덱 룰 자가 학습*) 결의 *8 명 룰* 합류. **MCP 룰 `multi-deck-domain-rule` 강화** (8 명 + 8 덱 룰).

### 3. State interface (Hit/Stay/Finished) — State 패턴 자가 적용

학생 자가 적용 — *Hit/Stay/Finished State 자식 + 추상 메서드*. PR #93·#136·#152·#98·#36·#87·#142·#143·#138 결의 *State 패턴* 어휘 합류.

### 4. callback 어휘 학생 자가 *푹 빠짐* + reviewer 권유

reviewer (Thread 7): *"갑자기 callback에 푹빠지셨군요! callback의 생성과정에 대한 설명과 어떤 특징이 있는지에 대해서 이든의 의견을 남겨주세요💪"*

→ **reviewer 학생 자가 의견 요청** = *PR #109·#129·#126·#143 (학생 자가 의견 요청) 결*. *학생이 callback 자가 적용 + reviewer 가 *학생에게 *이유* 묻기**.

## 못한 점 (설계 관점)

### 1. callback 형태 장단점 + 함수 파라미터 (Thread 2·3·7)

reviewer 3 자리에 걸쳐 *callback 사용 이유 + 장단점* 묻기.

→ **고차함수 / callback 활용 학생 자가 학습** = PR #109·#140·#143 (고차함수 = View 의존 X) 결 합류.

### 2. State 가 profit 알 필요 (Thread 8) — 학생/reviewer 양방향

reviewer: *"규칙에서 `profit`에 대해서 반드시 알아야 할 이유가 있을까요?"*

학생:

> *"규칙에 대한 인터페이스가 아니라 말 그대로 상태를 알려준다고 생각해서, 상태에 관련된 함수들을 구현했습니다. 그래서 State에서도 수익을 계산할 수 있다고 생각했습니다. 그래서 수익을 계산할때 상대의 상태를 받아서 계산하는 것으로 수정했습니다 !"*

→ **State 책임 vs 외부 객체 (Result) 양방향** = PR #142·#143 결 합류.

### 3. 사용 안 하는 객체 (Stay, playerTotalScore) — Dead code (Thread 9·10·15)

reviewer 3 자리에 걸쳐 *사용 안 하는 객체/변수 지적*.

→ **Dead code** = PR #139 (이전 단계 코드 잔재 정리) 결 + PR #130 (Translator 과한 구현) 결 합류. **MCP 룰 `dead-code-removal` 후보**.

### 4. 하드코딩 상수화 (Thread 11·14·16)

reviewer 3 자리에 걸쳐 *하드코딩 상수화 권유*.

→ **상수화** = PR #117·#118·#129·#121 결 합류.

### 5. 테스트 실패 + 테스트 빠진 객체 (Thread 12)

reviewer: *"해당 테스트는 실패하는 것처럼 나오는군요😅 테스트가 빠져있는 객체들이 많은 것 같군요🥲"*

→ **테스트 누락 + 실패** = *학생 자가 마감 시간 한계* 결과. **MCP 룰 `failing-test-as-incomplete-submission` 후보** — *실패하는 테스트 + 누락 = 미완성 제출*.

### 6. 패키지 분리 (Thread 13)

reviewer: *"모든 객체들이 도메인안에 한곳에 모여있다보니 헷갈릴 수 있겠다는 생각이 들었어요~ 세부 패키지에 대해서 분리를 해보는 방법도 좋을 것 같다는 생각이 듭니다💪"*

→ **package-by-domain** = PR #142·#118 결 합류.

### 7. Deck 외부 노출 함수 (Thread 4)

학생 자가 답변: *"해당 함수를 외부로 드러나지 않게, 덱을 외부에서 수정할 수 없게 하였습니다 !"*

→ **encapsulation 자가 적용** = PR #21·#117·#138 결 합류.

## 대표 인용

> [devfeijoa PR 본문]
> *"빠진 기능이 꽤나 존재하지만 마감시간이 곧이라 일단 제출하여 피드백을 받고자 합니다..!"*

→ **학생 자가 마감 한계 인정 + 피드백 우선 전략** = PR #139·#143·#130 결.

> [Gyuil-Hwnag Thread 7]
> *"갑자기 callback에 푹빠지셨군요! callback의 생성과정에 대한 설명과 어떤 특징이 있는지에 대해서 이든의 의견을 남겨주세요💪"*

→ **reviewer 학생 자가 의견 요청 어휘**.

> [devfeijoa Thread 6]
> *"원래 블랙잭 도메인이 가지고 있는 조건인 참가자를 8명까지 제한한다라는 조건을 적용했습니다 !"*

→ **블랙잭 8명 도메인 룰 자가 학습** = PR #138 (rosemin928 8 덱 룰) 결 합류.

## 얻은 교훈 (보편)

1. **블랙잭 8명 제한 + 8 덱 도메인 룰** — **MCP 룰 `multi-deck-domain-rule` 강화**.
2. **Dead code** — **MCP 룰 `dead-code-removal` 후보**.
3. **실패 테스트 = 미완성 제출** — **MCP 룰 `failing-test-as-incomplete-submission` 후보**.
4. **callback 학생 자가 *푹 빠짐* + reviewer 의견 요청**.
5. **State 가 profit 알 필요 = 학생/reviewer 양방향** — PR #142·#143 결 합류.

## MCP 룰 후보 신규 + 강화 (PR #153)

| 룰 | 신규 / 강화 | 누적 PR |
|---|---|---|
| `multi-deck-domain-rule` | 강화 | 3+ (#58·#138·#153) — 8 명 + 8 덱 |
| `dead-code-removal` | 신규 | 1 (#153) |
| `failing-test-as-incomplete-submission` | 신규 | 1 (#153) |
| `package-by-domain` | 강화 | 3+ |
