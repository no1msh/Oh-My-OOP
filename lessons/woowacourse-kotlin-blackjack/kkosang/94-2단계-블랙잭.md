# PR #94 — [블랙잭 2단계] 꼬상 미션

- **Reviewee:** [@kkosang](https://github.com/kkosang)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/94
- **단계:** 2단계
- **리뷰어:** [@KwonDae](https://github.com/KwonDae) (제이든) — 1단계 #68 동일 reviewer
- **라인 코멘트:** 15 스레드 — *PR 본문 자가 의문 (BlackJack 클래스 삭제 가능?) + reviewer *시야 환기 위해 요구사항 없는 룰 가정 의도* 자가 답변 + data class vs class 학생 자가 답변 + State 패턴 abstract fun 미적용 + Controller-View 없이 도메인 + calculateResult + displayResult = 함수 2 역할 (CQS) + 일급 컬렉션 위임 권유 + given-when-then 학생 자가 학습*
- **양 단계 완성 reviewee** (1단계 #68 batch 23 완료 + 2단계 #94 = batch 24).

## 잘한 점 (설계 관점)

### 1. PR 본문 자가 의문 + 인사 (몸 상태)

PR 본문:

> *"몸 상태가 좋지 않아서 오래걸렸습니다 😥 제이든도 환절기에 감기 조심하세요 ㅠㅠ! 1단계 리뷰 반영 하는 부분에서 아직 BlackJack 클래스에 대해서는 해결하지 못하였는데 BlackJack클래스를 삭제하고 HandCard에서 상태를 관리하면서 Participant에서 HandCard를 사용하면 되는걸까요?"*

→ **학생 자가 한계 인정 + reviewer 안부 인사 + 자가 의문** = PR #130·#139·#143·#121·#153 결 합류. 인간적 어휘.

### 2. reviewer 의도 명료화 (Thread 9 - PR 본문 자가 의문 답변)

reviewer:

> *"조금은 시야를 환기시키기 위해 요구사항에는 없었던 룰에 대해서 생각을 해보고 적용을 하게 된다면 현재 구조에서 어떤 문제가 생길 수 있을지에 대해 고민을 유도하는 코멘트였습니다 ㅎㅎ*
>
> *지금 BlackJack이라는 클래스를 통해 블랙잭 로직들을 응집함으로써 잘 분리가 되었습니다. BlackJack 클래스가 없다면 Participant 가 지금 보다는 비대해질 가능성이 있겠죠. 다만,..."*

→ **reviewer 의도 자가 명료화 + 1단계 #68 소프트17 룰 어휘 명시** = *PR #142 (reviewer-self-clarification-explicit) 결 합류 누적 5+ PR*.

### 3. data class vs class 학생 자가 답변

학생: *"Card를 data class로 만든 이유는 값에 대한 정보만 들어있고 get메서드를 통해 읽기전용으로만 사용하고 있어서 사용했습니다. 또한CardDeck에서 Card객체의 값 비교를 테스트할 때, equals()와 hashCode() 같은 메서드를 자동으로 생성해주기 때문에 data class로 수정하였습니다 !"*

→ **Kotlin 어휘 자가 학습** = PR #117·#138·#143 결 합류.

### 4. given-when-then 학생 자가 학습 (Thread 10)

reviewer: *"그리고 given-when-then 패턴을 활용해서 보다 가독성이 높은 테스트 코드를 작성해볼까요?"*

학생: *"테스트 코드에서도 given-when-then이라는 패턴이 존재했군요 ! 수정하였습니다"*

→ **BDD given-when-then 학생 자가 학습** = PR #109·#19 결 합류.

### 5. 추가 칭찬

- Thread 1: *"runCatching 활용을 잘 해주셨네요 👍"*.
- Thread 4: *"랜덤 요소가 있는 부분을 추상화를 잘 해주셨네요 👍"*.
- Thread 11: *"fold 활용 👍 👍"*.
- Thread 12: *"추상화를 통해 각 상태에 따라 로직을 수행하게 잘 구현해주셨네요 💯"*.

## 못한 점 (설계 관점)

### 1. UserDecision 패키지 이동 vs 의존성 (Thread 3)

reviewer: *"단순 패키지 이동으로 해결될 수 있는 부분일까요? 현재 도메인 모델인 Player 에서 UserDecision을 의존하고 있습니다. 단순 코멘트를 반영하는 목적이 아닌 깊게 고민해보고 이해가 가지 않는 부분은 편하게 DM으로 말씀해주셔도 됩니다."*

→ **DM 활용** = PR #129·#138·#142·#121·#153 결 합류.

### 2. ShuffleCardDeck interface 테스트 활용 X (Thread 4)

reviewer: *"테스트시에는 이 인터페이스가 활용이 안되고 있습니다."*

→ **interface-for-test-substitution** = PR #138·#143·#113·#118·#149 결 합류.

### 3. 테스트 보장 X (Thread 5) — *예상 하지 못하게* 모호

reviewer: *"이 테스트는 과연 결과를 보장할 수 있는 테스트일까요? 🧐 `예상 하지 못하게` 라는 테스트 제목이 이해가 잘 가지 않았어요."*

학생: *"만약 생성된 카드에서 모두 expectedCard와 모두 같은 경우가 있을 수 있을 수 있기 때문에, 테스트 결과를 보장할 수 없습니다 ! 해당 테스트를 삭제하도록 하겠습니다 !"*

→ **테스트 결과 보장 = 플레이키 테스트 회피** = **MCP 룰 `flaky-test-avoidance` 후보**.

### 4. State 패턴 abstract fun 미적용 (Thread 7)

reviewer: *"State 패턴의 핵심 중에 abstract fun 추상화 메서드를 활용하여 상태가 변할 때 마다 어떤 상태인지에 따라 다른 로직을 수행하는 것이 기능중에 하나인데요."*

→ **State 자식 책임 부여** = PR #93·#136·#152·#98·#36·#87·#142·#143·#138 결 합류 누적 12+ PR.

### 5. 수익 = Participant 위임 (Thread 8) — Tell-Don't-Ask

reviewer: *"수익을 계산하는 로직도 Participant 에게 위임 해보는건 어떨까요?"*

→ **Tell-Don't-Ask** = 누적 다수 PR.

### 6. Controller-View 없이 게임 가능 (Thread 13) — 테스터빌리티

reviewer:

> *"Controller에서 player에 직접 접근해서 무언가를 직접 꺼내고 넣는 작업들을 해주어야 할까요? (...) 이 코멘트의 궁극적 의도는 \"Controller와 View 없이도 블랙잭 게임을 할 수 있는가?\" 에 대해서 도메인 로직이 충분히 Controller로 부터 분리되었나? 를 고민해보시면 좋겠습니다."*

→ **Controller/View 없이 도메인 = 테스터빌리티** = PR #99 (chaehyuns) 결 + PR #87 (jaeyeongjo 테스트 = 객체 설계 명세서) 결 합류. **MCP 룰 `controller-view-domain-coupling` 강화** 누적 3+ PR.

### 7. `calculateResult` + displayResult = 함수 2 역할 (Thread 13) — CQS

reviewer:

> *"함수의 이름은 calculateResult() 이기에 함수를 진입하는 의도는 결과를 도출하기 위해서입니다.*
> *1. 플레이어들의 게임 결과를 가져온다*
> *2. 가져온 결과를 통해 수익률을 계산하여 출력한다"*

→ **CQS 위반 + 함수 분리** = PR #117·#139·#130·#141·#118 결 합류 누적 5+ PR.

### 8. 일급 컬렉션 위임 + suggestion 블록 (Thread 15)

reviewer:

> *"```suggestion*
> *    fun getParticipantProfitResult(): List<Double> {*
> *        val result = players.getProfitResult()*
> *        return result*
> *    }*
> *```*
>
> *와 같이 일급컬렉션을 사용하여 객체에게 도메인 로직을 집중시킬 수 있습니다. 이후 오목 미션 진행하시면서도 객체가 할 수 있는 일을 최대한 위임하는 연습을 하시면 좋을 것 같습니다 👍"*

→ **일급 컬렉션 위임 + 다음 미션 (오목) 권유** = **MCP 룰 `cross-mission-learning-bridge` 후보** — *현재 미션 → 다음 미션 학습 권유*.

### 9. blackJack.state 의존성 = 테스트 불가 (Thread 14)

reviewer:

> *"현재 blackJack.state 라는 내부적으로 의존성이 있어 테스트가 불가한 구조네요. 테스트가 불가할때는 지난 로또미션, 블랙잭 미션을 진행하며 경험했던 방법들을 이용할 수 있습니다.*
> *1. 테스트 불가 요소를 분리한다*
> *2. 의존성을 갖지 않도록 주입 받는다."*

→ **테스트 불가 = 의존성 분리** = PR #87·#129·#142·#109·#140 결 합류.

### 10. CardDeckTest fixture (Thread 10) — Test Fixture

reviewer: *"이부분은 Test Fixture를 이용해 개선해볼 수 있곘네요!"*

→ **Test Fixture** = 누적 다수.

## 대표 인용

> [kkosang PR 본문]
> *"제이든도 환절기에 감기 조심하세요 ㅠㅠ!"*

→ **학생 인간적 어휘** + 자가 한계 인정.

> [KwonDae Thread 9]
> *"조금은 시야를 환기시키기 위해 요구사항에는 없었던 룰에 대해서 생각을 해보고 적용을 하게 된다면 현재 구조에서 어떤 문제가 생길 수 있을지에 대해 고민을 유도하는 코멘트였습니다 ㅎㅎ"*

→ **reviewer 의도 자가 명료화** + *시야 환기 어휘*.

> [KwonDae Thread 15]
> *"이후 오목 미션 진행하시면서도 객체가 할 수 있는 일을 최대한 위임하는 연습을 하시면 좋을 것 같습니다 👍"*

→ **cross-mission 학습 권유 어휘**.

## 얻은 교훈 (보편)

1. **reviewer 의도 자가 명료화** = PR #142·#126·#130·#109 결 강화 누적 5+ PR.
2. **플레이키 테스트 회피** — **MCP 룰 `flaky-test-avoidance` 후보**.
3. **Controller/View 없이 도메인 = 테스터빌리티** — **MCP 룰 `controller-view-domain-coupling` 강화** 누적 3+ PR.
4. **CQS 위반 (calculateResult + displayResult)** = 누적 5+ PR.
5. **cross-mission 학습 권유 어휘** — **MCP 룰 `cross-mission-learning-bridge` 후보**.
6. **State 자식 책임 부여** = 누적 12+ PR.
