# PR #13 — [블랙잭 1단계] 뽀또 미션

- **Reviewee:** [@hyunji1203](https://github.com/hyunji1203)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/13
- **단계:** 1단계
- **리뷰어:** @vagabond95
- **라인 코멘트:** 19 스레드 — *View 게임 단계별 분리 자가 시도 + Kotlin 어휘 (extension function vs object function vs top-level) 매우 긴 자가 학습*
- **신규 reviewee** (2023년 3월 코호트). PR #11·#31·#66 와 동일 reviewer.

## 잘한 점 (설계 관점)

- **PR 본문 자가 시도 명시** — *"이번에 새롭게 시도해 본 부분이, View 부분을 Input과 Output으로 나눠 작성하지 않고 게임의 단계별로 필요한 view로 묶어 작성해보았습니다."* — **`view-by-stage-split` 자가 시도** (PR #88·#131 결).
- **`인터페이스 활용`** — vagabond95: *"인터페이스 활용 👍"* (CardFactory).
- **`backing field`** — vagabond95: *"backing field 활용 👍"* (Cards).
- **`CardDistributeCondition` 별도 분리** — vagabond95: *"별도로 잘 분리해주셨네요 👍"*.
- **`함수 재활용`** + **`구현 방식 통일`** + **`코드 흐름 명확화`** — vagabond95 칭찬 3회: *"전반적으로 코드 흐름이 아주 명확해졌습니다! 👍"*. **피드백 반영 후 칭찬**.
- **`object function vs top-level extension function` 학생 자가 매우 긴 학습 + reviewer 답변** — vagabond95: *"비즈니스 로직을 이렇게 별도 클래스로 함수로 빼주시는 것과, 필요한 곳에서 내부 함수로 선언하시는 각각의 기준이 있으실까요?"* + 학생 자가 깊은 답변 + 자가 의문: *"실례되지 않는다면 리뷰어님께서는 '별로 클래스 분리 or 필요한 곳 내부 함수 선언' 을 어떤 기준으로 나누시는지 여쭤보고 싶습니다!"* + reviewer 매우 긴 답변:

> *"우선, 클래스의 상태를 이용하여 '스스로' 처리할 수 있고, 아키텍처 레이어 상으로도 적절한 처리 (= domain 에선 domain 과 연관된 로직) 함수의 경우에는 자연스럽게 클래스 내부에 함수를 선언해요. 남은건, 일종의 Utils 성 클래스 or 외부 아키텍처 레이어에 적합한 로직 (= domain 의 결과를 view 에서 활용하기 위해서 가공) 등이 있을 것 같아요. 이에 대해 java -> kotlin 으로 넘어온 후에는 다음과 같은 기준을 가지고 처리하고 있어요. 1. 비교적 단순한 가공 / 변환의 로직 : top level extension function 2. 비즈니스 로직이 들어가는 로직 or 1번의 사례에서 원시값에 대한 케이스 : object function (= util class) ..."*

**reviewer 의 *Kotlin 어휘 사용 기준* 매우 긴 메타 답변 + 학생 자가 학습 모범** — PR #11·#31·#66·#46·#91 결.

## 못한 점 (설계 관점)

### 1. `misc` 패키지 — 일반 명명 + etc/common/misc 함정

vagabond95: *"해당 클래스만 misc 패키지로 두신 이유가 있을까요?"*

학생: *"게임의 다양한 곳에서 자주 쓰일 상수를 넣을 클래스를 찾다가, 게임 규칙이라는 클래스에 넣으면 어떨까 생각해 기타 패키지 안의 GameRule을 만들어 저장해보았습니다! 혹시 이런 방식 말고 그냥 존재하는 다른 클래스에 해당 상수를 놓는 방식이 더 좋을까요?"*

vagabond95: *"우선, etc / common / misc 같은 패키지는 무엇이든 들어갈 수 있기 때문에 저같은 경우에는 최대한 만드는 것을 뒤로 미루려고 합니다. 그리고 기존에 만들어두신 다른 패키지로 들어가기에도 적절해보여서요! 만약 공용으로 사용될 파일을 둘 수 있는 패키지를 두고 싶을지라도, view / model / entity 등의 하위 패키지로 두어 한번더 구분을 지어주면 좋을 것 같아요."*

→ **`etc / common / misc` 패키지 = 일반 명명 안티패턴**:
- *무엇이든 들어갈 수 있음 = 비대화 + 응집 X*.
- *생성 시점 = 마지막까지 미루기 + 적합 패키지 우선 탐색*.

**MCP 룰 후보 `etc-package-anti-pattern`** = *etc/common/misc 패키지 = 마지막 수단*. PR #11 (vagabond95 *Const.kt 일반 명명*) / PR #16 (BeokBeok *BlackJackRule*) 결.

### 2. `Init` 클래스명 — 일반 명명

vagabond95: *"Init 이라는 단어는 common 한 네이밍이라 어떤 함수가 들어가도 크게 어색하지 않기 때문에 해당 클래스로 view 로직이 몰릴 수 있을 것 같아요. 좀 더 구체적인 이름을 지어보는 것은 어떨까요?"*

학생: *"게임의 초기세팅을 관리하는 뷰라는 뜻으로 GameSetView라는 이름으로 수정하였습니다!"*

→ **`vagueName` + 비대화 시그널**. PR #16·#11·#107·#9 결.

### 3. `it2` 변수명 — vagueName 강화

vagabond95: *"`it2` 같은 네이밍은 무엇을 의미하는지 알기 어려워요. 좀 더 구체적인 이름을 사용해주세요."*

학생: *"가독성이 좋아지도록 it은 type으로 it2는 number로 수정하였습니다!"*

→ **중첩 람다 = `it` 다중 = 모호**. 명시적 파라미터명 정공. PR #107 (junseo511 *it → player*) / PR #95·#39 결.

### 4. `RandomCardFactory` 인덱스 예외 처리

vagabond95: *"해당 함수는 index 가 size 를 넘어갈 경우 예외가 발생할 것 같아요. 별도 처리가 필요하지 않을까요?"*

학생: *"넵! 처리했습니다!"*

→ **경계 조건 방어**. PR #46·#26·#9 결.

### 5. `CardNumber` ACE 비즈니스 로직 enum 누수

vagabond95: *"ace 에 대한 비즈니스 로직을 다른 곳으로 이동시켜보는 것도 좋을 것 같아요. enum value 가 선언되는 과정에서 비즈니스 로직이 많아질수록 코드를 이해하기 어렵기도 하고, 함수형태가 가독성이 그리 뛰어나지 않을 것 같아요!"*

학생: *"해당 비즈니스 로직을 관리할 클래스를 새로 만들어 로직을 분리시켰습니다!"*

→ **enum 부가 로직 분리** — PR #137 (HamBeomJoon *Rank.label*) / PR #16 (SeongHoonC *Score 분리*) / PR #43 (chws0508) / PR #74 (송핑크 *Rank 보너스 점수*) 결.

### 6. `CardDistributeCondition` 입력 판단 = View 영역

vagabond95: *"`입력` 에 대한 판단 및 관련된 값은 view 에서 다뤄야할 내용 아닐까요?"*

학생: *"맞습니다! 생각해본 결과 해당 로직을 아예 삭제하는 방향으로 바꿨습니다"*

→ **불필요 객체 자가 삭제**. PR #145·#146·#31 결.

### 7. `Cards.value` 매번 `toList()` 호출 비효율

vagabond95: *"현재는 매번 value 를 가져올 때마다 toList() 가 호출 될 것 같은데, 좀 더 효율적인 방식으로 개선해볼 수 있을까요?"*

→ **불변 객체 + 매번 계산** = *PR #39 (rhthrhrl0 *immutable-recompute-on-call*)* 결.

### 8. `ResultView` 문자열 상수화 + ViewUtils 응집

vagabond95 다중: *"해당 문자열들도 상수로 관리해주면 좋을 것 같아요~!"* + *"이 부분도 ViewUtils 로 관리해보는 것은 어떨까요?"* + *"혹은 View Utils 내용을 외부로 꺼내도 좋겠구요 :) 핵심은, 비슷한 일을 하는 코드는 비슷한 방식으로 접근할 수 있으면 사용자 입장(=다른 개발자)에서 좋을 것 같아요."*

→ **비슷한 일 = 비슷한 방식 (일관성)**. PR #11 (vagabond95 *inconsistent-accessor-pattern*) 결.

### 9. `NumberStrategy` object 도입 + 클래스 분리 기준 자가 학습

vagabond95: *"해당 클래스는 별도로 상태를 가지고 있지 않으므로 object 로 관리해도 좋을 것 같아요!"*

이어서 *클래스 분리 vs 내부 함수 기준 자가 질문* → 위 §잘한점 학생/reviewer 매우 긴 토론.

학생 자가 답변: *"사실 이번 Ace 판단 로직을 수 결정 도메인으로 생각한 후 수 결정 도메인이 겹치는게 없다 판단해서, NumberStrategy() 로직 클래스를 만들어 그 안에 선언했었습니다! 하지만 리뷰어님의 말씀대로 해당 클래스는 별도의 상태를 갖고 있지 않은 점, 카드의 수 합을 계산하는 함수가 있는 class에 선언해도 카드의 합을 계산한다는 역할의 통일성이 있다는 점을 고려해 Cards의 클래스안에 내부 함수 선언으로 수정하였습니다!"*

→ **3 가지 학습**:

#### 9.1. *상태 없는 클래스 = object 정공*

PR #46 (krrong *Calculator object*) / PR #11 (ki960213 *Deck object 자가 학습*) 결.

#### 9.2. *역할 통일성 vs 별도 분리*

- *카드 합 계산 = Cards 의 역할* → *Cards 내부 함수로 응집*.
- *완전 별도 로직 = 별도 클래스 (object/class)* 분리.

#### 9.3. *reviewer 의 Kotlin 어휘 사용 기준 메타 답변 (위 §잘한점)*

**Kotlin top-level extension function vs object function = 사용 기준 명시 메타 학습 모범**.

### 10. `함수형 파라미터 중첩 + it 모호`

vagabond95: *"중첩하여 함수형 파라미터를 전달할 경우 it 보다 명시적인 이름을 사용해보는 것은 어떨까요?"* + *"전반적으로 함수를 전달하여 카드를 분배하는 방식으로 구현되고 있는데, 중첩하여 구성될수록 흐름을 따라가기 어려운 것 같아요. ㅜㅜ"*

학생: *"it 대신 player 이름으로 변경 후 흐름을 따라가기 쉽도록 함수 중첩 전달 방식에서 컨트롤 기반 제어 방식으로 수정했습니다!"*

→ **람다 중첩 = 흐름 추적 어려움**. PR #145·#107·#137 결.

### 11. `BlackjackStage` 구현 방식 통일 (직접 vs 외부 위임)

vagabond95: *"어떤 함수는 카드 분배에 직접 관여하고, 어떤 함수는 카드 분배를 외부로 위임하고 있어요. 구현방식을 통일해보면 좋을 것 같아요."*

→ **일관성 = 비슷한 일 비슷한 방식**.

### 12. `domain 이 print 가정` X

vagabond95: *"domain 에서 `print 가 될 것이다` 라고 가정하는 것이 적절할까요? domain 은 비즈니스 로직만 수행하고, 외부에서 print 에 관여 하도록 수정해볼 수 있을까요?"*

학생: *"컨트롤러에서 print를 관여하도록 수정하였습니다!"*

→ **MVC 경계 + Tell-Don't-Ask**. PR #66·#26·#23·#16·#9 결.

### 13. `"y" 상수화`

vagabond95: *"'y' 값은 상수로 선언하면 재활용 및 수정에 용이할 것 같아요."*

→ **상수 응집**. PR #145·#31 결.

### 14. `extension function` 활용

vagabond95: *"extension function 방식을 이용하면, 좀 더 자연스럽게 사용이 가능할 것 같아요. `cardNumber.toXXX()`"*

→ **Kotlin 확장 함수 어휘**. PR #9·#46·#31·#107 결.

### 15. `when 활용`

vagabond95: *"when 을 활용해보는 것은 어떨까요?"*

→ **Kotlin when 어휘**. PR #91·#9·#26·#43·#16 결.

### 16. `distributePlayer 재활용`

vagabond95: *"distributePlayer(..) 함수를 재활용해보는 것은 어떨까요?"*

→ **DRY**. PR #9·#31·#46 결.

## 리뷰어 의견 요지 (vagabond95)

19 코멘트 특징 (PR #11·#31·#66 와 동일 reviewer):

- **Kotlin 어휘 사용 기준 매우 긴 메타 답변** — *top-level extension function vs object function vs 내부 함수* 사용 기준 명시. **PR #11·#31·#66 와 다른 특징** = *학생 자가 질문 시 매우 긴 답변*.
- **칭찬 분포** — *코드 흐름 명확화, 인터페이스 활용, backing field, 별도 분리* 등 6회 칭찬 + 권고 13회 = 균형.
- **메타 질문 어휘** — *"어떤 기준이 있으실까요?"*, *"어떤 문제점이 생길 수 있을까요?"*.
- **`view-by-stage-split` 자가 시도 인정** — *학생이 PR 본문에 시도 명시 + reviewer 가 *이름 구체화* 권고 (Init → GameSetView)*.

## 대표 인용

> *"우선, 클래스의 상태를 이용하여 '스스로' 처리할 수 있고, 아키텍처 레이어 상으로도 적절한 처리 (= domain 에선 domain 과 연관된 로직) 함수의 경우에는 자연스럽게 클래스 내부에 함수를 선언해요. ... 1. 비교적 단순한 가공 / 변환의 로직 : top level extension function 2. 비즈니스 로직이 들어가는 로직 or 1번의 사례에서 원시값에 대한 케이스 : object function (= util class)"* — vagabond95 (Kotlin 어휘 사용 기준 메타)

> *"etc / common / misc 같은 패키지는 무엇이든 들어갈 수 있기 때문에 저같은 경우에는 최대한 만드는 것을 뒤로 미루려고 합니다."* — vagabond95 (etc 패키지 안티패턴)

> *"사실 이번 Ace 판단 로직을 수 결정 도메인으로 생각한 후 수 결정 도메인이 겹치는게 없다 판단해서, NumberStrategy() 로직 클래스를 만들어 그 안에 선언했었습니다! 하지만 리뷰어님의 말씀대로 (...) Cards의 클래스안에 내부 함수 선언으로 수정하였습니다!"* — hyunji1203 (역할 통일성 자가 학습)

> *"실례되지 않는다면 리뷰어님께서는 '별로 클래스 분리 or 필요한 곳 내부 함수 선언' 을 어떤 기준으로 나누시는지 여쭤보고 싶습니다!"* — hyunji1203 (학생 자가 기준 의문)

## 얻은 교훈

1. **`etc / common / misc` 패키지 안티패턴** — *일반 명명 = 비대화 + 응집 X = 마지막 수단*. **MCP 룰 후보 `etc-package-anti-pattern`**. PR #11·#16 결.

2. **`Kotlin 어휘 사용 기준` 메타 학습**:
   - *클래스 상태 + 도메인 로직* → 내부 함수.
   - *단순 가공/변환* → top level extension function.
   - *비즈니스 로직 + 원시값* → object function (util class).
   
   **MCP 룰 후보 `kotlin-function-locus-criteria`**. PR #11·#46·#91·#107 결.

3. **`상태 없는 클래스 = object 정공`** — PR #46·#11·#16 결.

4. **`역할 통일성 vs 별도 분리`** — *카드 합 계산 = Cards 역할 → 내부 함수 응집*. PR #16·#43·#74 결.

5. **`중첩 람다 + it 다중 = 흐름 추적 어려움`** — *명시적 파라미터명 정공*. PR #107·#145·#137 결.

6. **`Init` 같은 일반 명명 = 비대화 시그널**. PR #16·#11·#107·#9 결.

7. **`it2` 같은 변수명 = vagueName 강화**.

8. **`불변 객체 + 매번 `toList()` 호출`** = *PR #39 immutable-recompute-on-call* 결.

9. **`구현 방식 통일`** — *비슷한 일 = 비슷한 방식 (일관성)*. **MCP 룰 후보 `inconsistent-approach-pattern`**.

10. **`view-by-stage-split` 자가 시도** — *PR 본문 명시 + 학습 가시화*. PR #88·#131 결.

11. **`reviewer 가 Kotlin 어휘 사용 기준 매우 긴 답변` = 학생 자가 질문 트리거** — *학생 자가 *기준 의문* → reviewer 매우 깊은 답변*. PR #11·#46·#91·#107 결의 *학생 자가 의문 풍부 시리즈*.
