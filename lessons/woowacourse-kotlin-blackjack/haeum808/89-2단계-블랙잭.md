# PR #89 — [블랙잭] 해음 2단계 미션

- **Reviewee:** [@haeum808](https://github.com/haeum808) (해음) — 안드로이드 6기 코호트
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/89
- **단계:** 2단계
- **리뷰어:** [@ghojeong](https://github.com/ghojeong) (파이로)
- **라인 코멘트:** 9 스레드 — *PR 본문 flux 상태 아키텍쳐 자가 학습 적용 + reviewer 자가 반성 *미션 본 목표 복귀 + 2단계 갈아엎기 요청* 메타 정점 + State 패턴 2 기준 (다형성 + Context 책임) + 매직 넘버 제거 + map 함수형 어휘 진화 + 콜백 String → Boolean 리턴*
- **haeum808 양 단계 완성** (1단계 #62).
- **메타 케이스 정점**: PR 본문 학생 자가 *reviewer 추천 flux 상태 아키텍쳐 자가 학습* → reviewer 자가 반성 *"순수 코틀린 객체 설계 본 목표를 벗어남"* → 2단계 *싹 갈아엎기* 요청 → 학생 자가 도전 → reviewer 감사. PR #62 reviewer 자가 수정 + PR #89 reviewer 미션 목표 복귀 = ghojeong 양 PR reviewer 자가 반성 어휘 연속.

## 잘한 점 (설계 관점)

### 1. PR 본문 — flux 상태 아키텍쳐 자가 학습

PR 본문:

> *"파이로가 말씀해주신대로 flux 상태 아키텍쳐을 공부하고 적용했습니다! step2를 진행하기 전 step1에 flux 상태 아키텍쳐를 적용하고 시작했습니다. 확실히 단방향으로 상태가 흐르니 새로운 상태를 추가할 때도 편하게 추가할 수 있었습니다. 새로운 상태를 추가해도 다른 곳에서 에러가 나오지 않아서 손쉽게 추가할 수 있었습니다."*

→ **MCP 룰 `pr-body-student-self-study-on-reviewer-recommendation`** ⭐⭐⭐ **신규** — *reviewer 추천 어휘를 학생 자가 학습 + 적용 + PR 본문 명시*. PR #154 (tobae-time 조영호 도서 자가 학습) 결 합류 누적 2 PR → ***학생 자가 학습 도서/어휘 인용 메타*** ⭐⭐⭐ 정점.

### 2. State 패턴 + Participant interface 칭찬 (Thread 1·2)

reviewer:

> *"State 디자인 패턴을 활용해주셨군요 👍. 저는 State 디자인 패턴은 다음 두가지를 확인합니다.*
> *1. **다형성(Polymorphism) 을 어떻게 활용했는가?***
> *2. **Context 의 책임을 어느 객체에게 부여했는가?***
>
> *draw 와 profit 이 State 가 바뀔 때마다 다른 로직이 실행될 수 있도록, State 를 interface 로 먼저 추상화 해주신 점 무척 멋집니다. 👍 💯"*

reviewer (Thread 2):

> *"현재 State 의 Context 는 Dealer 와 Player 라고 이해했습니다. Dealer 와 Player 를 다시 Participant 라는 interface 로 추상화 해주신 점 멋집니다. 👍 interface 를 정말 잘 활용해 주신 것 같습니다."*

→ **MCP 룰 `state-pattern-2-criteria`** ⭐⭐⭐ **신규 정점** — *State 패턴 점검 기준 2: (1) 다형성 활용 + (2) Context 책임 부여*. PR #154 (`state-pattern-no-if-type-check` 다형성)·PR #42 (불변 + 상태 패턴) 결 합류 누적 3 PR → ***State 패턴 어휘 정점*** ⭐⭐⭐.

### 3. TestFixtures 칭찬 (Thread 5)

reviewer: *"테스트를 위한 Fixture 를 선언해 주신 점 무척 좋습니다. 👍 💯"*

### 4. map + 방어적 복사 함수형 어휘 진화 (Thread 8) — 정점

reviewer:

> *"크으! map 의 활용이 매우 능숙해 지셨다는 느낌을 받았습니다. 👍 게다가 방어적 복사까지 신경써 주셔서 더욱 멋집니다. 👍*
>
> *다만, **map 안의 while 문은 indentation depth 를 깊게하는 요인인 것 같은데, 이 로직을 State, Player 등등의 객체에 배분하거나, 함수 분리를 해주시면 좋을 것 같습니다.**"*

→ **학생 함수형 어휘 진화** = 1단계 PR #62 *학생 자가 Slack DM 학습* → 2단계 *map 능숙 + 방어적 복사*. **메타 학습 시간성**.

→ **MCP 룰 `map-with-while-as-indentation-depth-signal`** ⭐⭐ 신규 — *map 안 while = depth 깊어짐 → 책임 배분*.

## 못한 점 (설계 관점)

### 1. reviewer 자가 반성 + 미션 본 목표 복귀 + 2단계 갈아엎기 요청 (review #1) — 정점

reviewer:

> *"해음! 제가 반응형 프로그래밍 덕후이다 보니, 순수 코틀린 언어에서의 객체 설계라는 주제를 벗어나는 방향으로, 해음과 대화를 많이하게 된 것 같습니다.*
>
> ***저도 반성을 하면서, 블랙잭 미션의 본래 목표인 객체 설계로 되돌아가고자 합니다.***
>
> ***죄송하지만, 2단계 미션의 구조를 버리고 다시 작성해주시면 감사하겠습니다.***"*

→ **MCP 룰 `reviewer-self-reflection-mission-goal-return`** ⭐⭐⭐ **신규 정점** — *reviewer 본인 취향 (반응형/flux) → 미션 본래 목표 자가 반성 + 갈아엎기 요청*. PR #62 (`reviewer-self-correction-on-strong-feedback`)·PR #115 (laco-dev *쎄게 양해*)·PR #73 (*휘둘리지 마세요*)·PR #106 (말리빈 *권력 자제*) 결 합류 누적 **5 PR** → ***reviewer 자가 강요 회피 어휘 정점*** ⭐⭐⭐ MCP `oh-my-oop` 핵심 가치 어휘 정점.

→ **ghojeong reviewer 양 PR 자가 반성 어휘 연속** (#62 *강한 피드백 자가 수정* → #89 *미션 본 목표 복귀*) = **MCP `oh-my-oop` 핵심 가치 어휘 양 PR 일관 정점**.

### 2. 학생 갈아엎기 도전 + reviewer 감사 (review #2·#3)

reviewer review #2:

> *"해음! **어찌보면 싹 갈아엎는 요청을 했음에도, 훌륭하게 새로 구현해주셔서 무척 감사합니다.** 🙇 매직 넘버만 조금 더 보완하면 좋을 듯 하여 피드백 남겨보았습니다!"*

reviewer review #3:

> *"해음! **처음부터 구조를 싹 갈아엎기 쉽지 않았을 텐데, 도전해주셔서 감사합니다!** 블랙잭 미션은 이렇게 마무리 하고, 다음 오목 미션도 화이팅입니다. 💪"*

→ **MCP 룰 `student-restart-on-reviewer-request`** ⭐⭐⭐ **신규** — *reviewer *갈아엎기* 요청 → 학생 자가 도전 → reviewer 감사*. PR #154 (`pr-body-restart-from-scratch` rebase 손실 → 학습 기회 자가 재해석) 결의 *갈아엎기* 다른 축 — *학생 자발 vs reviewer 요청 갈아엎기*.

### 3. 매직 넘버 일관 제거 (Thread 3·4·6) — 반복 시그널

reviewer Thread 3: *"다만 곳곳에 매직 넘버가 있는 것은 아쉽습니다. Blackjack, Bust, Hit 을 포함하여 모든 매직 넘버를 유의미한 상수값으로 네이밍해서 선언해주세요."*

reviewer Thread 4: *"state 뿐만 아니라 다른 model 에서 매직 넘버와 문자열은 유의미한 상수로 선언해주시기 바랍니다."*

reviewer Thread 6: *"마찬가지로 매직 넘버와 문자열을 제거할 필요성을 느낍니다."*

→ **매직 넘버 제거 일관 시그널** = 누적 다수.

### 4. `onInputDecision` String → Boolean 리턴 (Thread 6)

```kotlin
val onInputDecision: (String) -> String
```

reviewer:

> *"`val onInputDecision: (String) -> String` 의 경우에는 **String 을 리턴하지 않고 Boolean 을 리턴하도록 하는 것에 대해서는 어떻게 생각하시나요?**"*

→ **MCP 룰 `callback-string-vs-boolean-return-type`** ⭐⭐ 신규 — *콜백 리턴 타입 = 도메인 의미 명시 (Boolean 선택)*. PR #14 (`cli-vs-gui-assumption-test` Boolean) 결 합류.

### 5. `drawUntilSatisfaction` interface 메서드 + 디미터 법칙 (Thread 7)

```kotlin
// 현재 - 디미터 깨짐
state == Hit(state.hand) && state.hand.calculateTotal() <= 16
```

reviewer:

> *"`state == Hit(state.hand) && state.hand.calculateTotal() <= 16` 에 관련된 부분을 `fun drawUntilSatisfaction(): Boolean` 의 형태로 interface 에 선언하는 것은 어떨까 제안해봅니다. State interface 에 선언할지, Participant interface 에 선언할 지는 고민해주세요.*
>
> *고민해주시면서 `state.hand.calculateTotal()` 의 경우 처럼 **디미터 법칙이 깨지는 경우가 없도록** 해주시면 좋겠습니다."*

→ **`dot-2-or-fewer-rule` 디미터** = PR #38 결 누적 2 PR.

→ **MCP 룰 `behavior-locus-state-vs-participant-question`** ⭐⭐ — *행동 책임 위치 = State interface vs Participant interface 학생 자가 고민 유도*.

### 6. README 체크 미완료 (Thread 0)

reviewer: *"README 에 체크를 하지 않고 남겨 두신 이유가 있으시나요?"*

→ **PR 본문 체크리스트 완성도** = PR #115·#154 (tobae-time 셀프 체크리스트) 결.

## 대표 인용

> [ghojeong review #1]
> *"제가 반응형 프로그래밍 덕후이다 보니, 순수 코틀린 언어에서의 객체 설계라는 주제를 벗어나는 방향으로, 해음과 대화를 많이하게 된 것 같습니다. 저도 반성을 하면서, 블랙잭 미션의 본래 목표인 객체 설계로 되돌아가고자 합니다."*

→ **reviewer 자가 반성 + 미션 본 목표 복귀 어휘 정점**.

> [ghojeong review #1]
> *"죄송하지만, 2단계 미션의 구조를 버리고 다시 작성해주시면 감사하겠습니다."*

→ **reviewer 갈아엎기 사과 + 요청 어휘**.

> [ghojeong review #2]
> *"싹 갈아엎는 요청을 했음에도, 훌륭하게 새로 구현해주셔서 무척 감사합니다."*

→ **학생 갈아엎기 도전 감사 어휘**.

> [ghojeong Thread 1]
> *"저는 State 디자인 패턴은 다음 두가지를 확인합니다. 1. 다형성(Polymorphism) 을 어떻게 활용했는가? 2. Context 의 책임을 어느 객체에게 부여했는가?"*

→ **State 패턴 2 기준 어휘 정점**.

> [haeum808 PR 본문]
> *"파이로가 말씀해주신대로 flux 상태 아키텍쳐을 공부하고 적용했습니다! step2를 진행하기 전 step1에 flux 상태 아키텍쳐를 적용하고 시작했습니다."*

→ **학생 자가 reviewer 추천 어휘 자가 학습 어휘**.

## 얻은 교훈 (보편)

1. **reviewer 자가 반성 + 미션 본 목표 복귀** — **MCP 룰 `reviewer-self-reflection-mission-goal-return` 후보** ⭐⭐⭐ 정점. PR #62·#115·#73·#106 결 누적 5 PR — *reviewer 자가 강요 회피 어휘 정점*.
2. **State 패턴 2 기준 (다형성 + Context 책임)** — **MCP 룰 `state-pattern-2-criteria` 후보** ⭐⭐⭐. PR #154·#42 결 누적 3 PR.
3. **학생 자가 reviewer 추천 어휘 자가 학습** — **MCP 룰 `pr-body-student-self-study-on-reviewer-recommendation` 후보** ⭐⭐⭐. PR #154 결 누적 2 PR.
4. **reviewer 요청 갈아엎기 → 학생 자가 도전** — **MCP 룰 `student-restart-on-reviewer-request` 후보** ⭐⭐⭐.
5. **콜백 리턴 타입 도메인 의미 (String → Boolean)** — **MCP 룰 `callback-string-vs-boolean-return-type` 후보** ⭐⭐.
6. **map 안 while = depth 시그널** — **MCP 룰 `map-with-while-as-indentation-depth-signal` 후보** ⭐⭐.
7. **행동 책임 위치 학생 자가 고민 유도** — **MCP 룰 `behavior-locus-state-vs-participant-question` 후보** ⭐⭐.

## 메타 노트

- **haeum808 양 단계 완성 (1단계 #62 → 2단계 #89)** — 1단계 학생 자가 Slack DM 학습 → 2단계 PR 본문 *flux 상태 아키텍쳐 자가 학습 적용* → reviewer 본 목표 복귀 + 갈아엎기 요청 → 학생 자가 도전 + 함수형 어휘 진화 (map 능숙).
- **ghojeong reviewer 어휘 정점 (양 PR 자가 반성 연속)** — #62 *강한 피드백 자가 수정* (forEach 강요 X 정정) → #89 *미션 본 목표 복귀 + 갈아엎기 사과* — **MCP `oh-my-oop` 핵심 가치 reviewer 양 PR 일관 정점**.
- **메타 케이스 9번째** — *reviewer 본인 취향 → 미션 본 목표 자가 반성 + 갈아엎기 요청 → 학생 자가 도전* 정점.
- **누적 메타 케이스 (batch 26 신규)**:
  - 9. **haeum808 (PR #62 → #89)**: ghojeong reviewer 자가 반성 어휘 양 PR 연속 + 학생 자가 reviewer 추천 어휘 자가 학습 + 갈아엎기 도전.
