# PR #96 — [블랙잭 2단계] 악어 미션

- **Reviewee:** [@Hogu59](https://github.com/Hogu59)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/96
- **단계:** 2단계
- **리뷰어:** [@malibinYun](https://github.com/malibinYun) (말리빈) — 2024 코호트 reviewer (PR #95·#23 등) + 2025 PR #118·#141 reviewer (다년 활동)
- **라인 코멘트:** 28 스레드 — *PR 본문 학생 자가 *손이 덱과 카드를 동시에 아는가* 의존성 흐름 자가 의문 + 학생 자가 의견 명시 + 모든 28 스레드 학생 자가 답변 + 자가 후속 의문 6+ 회 + Dealer-Player 비교 vs Human-Human 비교 (유연성) + 카드 재활용 (private constructor + 팩토리 메서드) + MarkType String 입력 다양성 ("A" vs "ACE") 자가 학습 + 매직 넘버 46 → 학생 자가 *팀의 코드* 어휘 학습 + BlackjackGame 객체 권유 + 명명 6+ 변경 (ResultType/ValueType/MarkType/isPossible/Hand.draw/Mark)*
- **양 단계 완성 reviewee** (1단계 #71 = 10 thread 적음 = **2단계 단독 가치**, 2024 코호트). batch 22 = Hogu59 2단계만 분석 (1단계 분석 보류).

## 잘한 점 (설계 관점)

### 1. PR 본문 자가 의존성 흐름 의문 + *손이 덱과 카드를 동시에 아는가*

PR 본문:

> *"`손`이 `덱`과 `카드`를 동시에 아는 것이 맞는가에 대한 코멘트를 보면서, 제가 생각했던 의존성 흐름과 실제로 제가 짠 코드가 다르다는 것을 발견하게 되었습니다. 그 부분에서부터 어떻게 하면 좋을까 고민이 좀 많아지다가 피드백 반영이 많이 늦어졌습니다.*
>
> *아래의 의존성 그래프에 맞춰 최대한 구현해보고자 노력해보았는데, 이런 흐름에 맞추다보니 결국 컨트롤러에서는 `Deck`에서 꺼낸 카드를 직접 `Player`의 `Hand`에 넣어주게 되는 부분이 필연적으로 생기게 되었고, 과연 이렇게 하는 것이 적절한가에 대한 의문이 들었습니다."*

→ **학생 자가 의존성 흐름 의식 + 자가 한계 인정 + 1단계 → 2단계 사이 학습 시간 지연 자가 사과** = *MCP `oh-my-oop` *학습 = 시간 두고 반복* 어휘 정점*. PR #138·#142·#143·#130·#118·#141 결 합류.

### 2. Dealer-Player vs Human-Human 비교 (유연성)

학생 자가 의견 (Thread 2):

> *"Human(Player, Dealer의 상위 인터페이스)으로 굳이 옮기지 않은 이유는, 과연 Player에게 비교하는 계산이 필요한가에 대한 생각이 들었기 때문입니다. 실제로 블랙잭 게임을 하게 된다면, 플레이어는 판단을 못하더라도 딜러는 가장 적절한 판단을 알아서 해서 플레이어에게 결과를 주기 때문에, 딜러만 판단에 대한 로직을 가지는게 적절하다고 생각했습니다!"*

reviewer 자가 후속:

> *"음~ 저는 Human이 Human 끼리 비교하는 것도 하나의 방법이라 생각이 드네요. 플레이어 중 한 명이 딜러가 되어서 게임을 이어가게 만드는 유연함을 제공해줄 수도 있으니까요.*
>
> *여러 방법에 대해 가능성을 열어두고, 장단점들을 잘 저울질 해보셔도 좋겠어요 :)"*

→ **유연성 어휘 + N 안 트레이드오프** = *플레이어 → 딜러 역할 전환 가정* 유연성. **MCP `oop_propose_alternatives` 핵심 가치 합류**.

**MCP 룰 `flexibility-vs-current-rule-tradeoff` 후보** — *현재 룰 vs 미래 유연성 트레이드오프*.

### 3. MarkType String 입력 다양성 학생 자가 학습 ("A" vs "ACE")

reviewer (Thread 7):

> *"다른 개발자들은 'A' 대신 'ACE'를 넣는다면, 또, 'S'대신 Spade, spade ,♠️를 넣는다면 어떻게 될까요? 이 부분을 모두 대응해주어야할까요? 또, 도메인 로직에 포함시키는 것이 적절할까요?*
>
> *생성에 대한 가짓수를 제공해주는 건 매우 좋지만, 그것은 도메인 로직을 해치지 않으며 누구나 이해 가능한 형태로 제작했을 때 빛을 바란다 생각해요 :)"*

학생 자가 답변 (메타 학습):

> *"1) 제가 생각하는 범주보다 더 다양한 경우의 수가 존재한다는 것에 대해서 간과하고 있었던 것 같습니다. (제가 모든 경우를 파악하거나 통제할 수 있다고 착각했습니다.)*
> *2) 도메인 로직을 지키면서 누구나 이해가능한(혹은 범용적인) 형태로 제작하는 것에 대해 앞으로도 더 고민해보도록 하겠습니다!"*

→ **학생 자가 *모든 경우 통제 못함* 메타 학습** = **MCP `oh-my-oop` 핵심 가치 (자가 한계 인지) 어휘 정점**. **MCP 룰 `input-diversity-uncontrollable` 후보** — *입력 다양성 = 모든 경우 통제 X*.

### 4. 매직 넘버 46 = *팀의 코드* 어휘 학생 자가 학습 (Thread 10)

reviewer:

> *"저는 저 이외에 아무도 이해할 수 없는 코드는 절대 작성하지 않아요. 또, 기능들은 필요에 의해 만드는 편입니다 :) 내가 작성한 코드더라도, 프로젝트에 들어가는 순간 그건 개인의 것이 아닌, 팀의 코드이니까요 :)"*

학생 자가 답변 (메타 학습):

> *"나의 코드가 아니라 팀의 코드로서 가치가 있는 코드인가.. 알려주셔서 감사합니다!*
>
> *모두가 같이 운용하는 어떠한 사업장이라면 나만 사용하는 어떤 물건을 공용공간에 함부로 두지 않는 것처럼 프로젝트 공간 내에서도 공유하여 사용되지 않을 부분은 과감히 삭제하는 것이 좀 더 옳다는 생각을 하게 되었습니다!"*

→ **팀의 코드 어휘 = maintainer perspective locator 정점** + **YAGNI 적용 학생 자가 학습** = *PR #146 (YAGNI 인용) 결 + PR #130 (YAGNI 위반 자가 인지) 결 강화*.

**MCP 룰 `team-code-vs-personal-code` 후보** — *팀의 코드 어휘*. PR #87·#76·#107·#82·#130 (maintainer perspective locator) 결 강화 누적 12+ PR.

### 5. 학생 자가 *엘레강트 오브젝트* 책 인용 (Thread 7)

학생:

> *"이부분은 엘레강트 오브젝트 책을 보면서 클래스는 충분히 많은 (부)생성자를 가져야한다는 생각에 공감해서 만든 팩토리 메서드였습니다."*

→ **외부 책 인용 (엘레강트 오브젝트)** = PR #136·#118·#108 결 합류. **MCP 룰 `external-book-reference` 후보** — *외부 책 자가 학습 인용*.

### 6. 카드 재활용 (private constructor + 팩토리 메서드) — PR #118 결 합류

reviewer (Thread 6): *"카드를 사용하는 경우는 얼마나 될까요? 한 번 만들어진 카드를 재활용해볼 수 있을까요?"*

학생 자가 적용: *팩토리 메서드 + private constructor*. PR #118 (Flyweight) 결 합류 누적 2+ PR.

### 7. 명명 6+ 변경 자가 적용

- `ResultType` → `GameResult` (Thread 13).
- `ValueType` → `Denomination` (Thread 9).
- `MarkType` → `Suit` (Thread 9).
- `isPossible` → `isHittable` (Thread 17).
- `Hand.draw()` → `Hand.add()` (Thread 12).
- `MarkType.of(String)` → 삭제 (Thread 7).

→ **이름 의미 정확성 자가 학습 정점** = PR #36·#117·#129·#130·#139·#109·#113·#118 결 합류 누적 9+ PR.

### 8. 학생 자가 의존성 그래프 그림 + 자가 의문 (PR 본문)

학생이 *의존성 그래프* 그림으로 의존성 흐름 시각화. **자가 시각화 학습 정점**. **MCP 룰 `student-self-dependency-graph-visualization` 후보**.

### 9. 모든 28 스레드 학생 자가 답변 + 자가 후속 의문 6+ 회 + 메타 학습 어휘

## 못한 점 (설계 관점)

### 1. Dealer.play(deck) 의존성 학생 자가 의문

학생 자가 의문 (Thread 1):

> *"그러면 `Dealer`는 `deck`에 의존해야만 구현이 가능하다고 생각이 됩니다. 물론, 현재 집중해야 할 문제가 아닐 수 있겠지만 혹시 `Dealer`가 `deck`에 의존하지 않고서 구현하는 방법은 없을까 고민이 있어서 질문했었습니다!"*

reviewer:

> *"dealer가 deck을 받지 않는다면, 어떤 것을 받아야할까요? 반드시 필요한 게 deck이라면, 다른 방법의 옵션은 없겠지요. 정말 deck말고 다른 것을 받을 수 있게 구성하는 방법은 없었나요?"*

→ **의존성 의식 + reviewer 의 *다른 인자 옵션* 어휘** = PR #138 (객체 소유 vs 매개변수 전달 자가 학습) 결 합류.

**MCP 룰 `parameter-options-exploration` 후보** — *함수 인자 옵션 다양성 의식*.

### 2. `cards as MutableList<Card>` 캐스팅 위험 — `cards.clear()` 시나리오

reviewer (Thread 3):

> *"아래 로직을 실행시키면 어떻게 될까요?*
> ```kotlin
> val hand = Hand()
> val cards = hand.cards as MutableList<Card>
> cards.clear()
> ```"

학생 자가 적용: `toList()` 방어적 복사.

→ **reviewer 문제 시나리오 코드 예시** = PR #118 (`reviewer-scenario-code-example`) 결 강화 누적 2+ PR.

### 3. `isBlackJack` 4장 21점 — 블랙잭 룰 정확성

reviewer (Thread 4): *"카드 4장을 가지고 있고, 총합이 21이라면, 이것은 블랙잭인가요?"*

학생: *"저 외에 다른 사람이 볼 때에는 모두 오해할 수 있는 네이밍이네요..!"* → 함수 삭제.

→ **블랙잭 룰 정확성** = PR #130·#138·#142 (초기 2장 + 21점 = 블랙잭) 결 합류 누적 11+ PR ⭐⭐⭐.

### 4. `applyProfitRate` 명명 — Money 메서드 시그니처 (Thread 5)

reviewer: *"옆의 동료에게 이 메서드 시그니처만 보고 어떤 일을 수행할 것 같은지 물어본다면, 바로 자신이 기대한 답변을 받을 수 있을까요?"*

학생: *"`applyProfitRate` 가 제가 생각할 수 있는 가장 이해가 쉬운 이름이라서 해당 이름으로 변경해두었습니다"*

→ **동료 시점 어휘** = PR #87·#76·#80·#82 결.

### 5. TestDeck 프로덕션 → test 패키지 (Thread 14)

reviewer: *"이 객체는 프로덕션 코드에 포함하는 것이 적절한 객체일까요?"*

학생 자가 적용: test 패키지 이동.

→ **production vs test 분리** = PR #130·#118 결 합류.

### 6. 16/17 매직 넘버 일관성 + 상수화 (Thread 15)

reviewer: *"어느곳은 16, 다른 곳은 17, 이 숫자들의 의미를 다르게 둘 필요가 있을까요? 또, 매직 넘버나 문자열은 상수화 시켜보는 건 어떨까요?"*

→ **블랙잭 룰 정확성 (`<= 16` vs `< 17`)** + 매직 넘버 상수화. PR #129·#138·#142·#143 결의 *블랙잭 hit 한계 명명* 결 합류 누적 13+ PR.

### 7. 객체 메시지 vs 값 꺼내기 (Thread 16) — Tell-Don't-Ask

reviewer: *"객체의 값을 꺼내 계산하기보다, 객체에게 메시지를 던져 물어보는 건 어떨까요?"*

→ **Tell-Don't-Ask 정공** = 누적 다수 PR.

### 8. HumanInfo 내부 상태 변경 vs 결과 객체 반환 (Thread 18)

reviewer: *"결과를 반영하기 위해 돈의 내부 상태를 변경해야할까요? 게임 결과를 위한 객체를 따로 만들어 결과만을 담은 객체를 반환하도록 구성해보는 건 어떨까요?"*

학생 자가 적용:

> *"내부 상태를 변경하는 로직이 조금 자연스럽지 못하다는 것을 다시 한번 생각하게 된것 같습니다! (...) HumanInfo 리스트를 감싸는 일급컬렉션을 만들어 `PlayersResult`로 구현해보았습니다!"*

→ **immutable 결과 객체 + 일급 컬렉션** = PR #141·#142·#143 결 합류.

### 9. CsvSource 활용 (Thread 19)

reviewer: *"CsvSource를 활용해보는 건 어떨까요?"*

→ **JUnit 파라미터화 테스트 어휘** = *Kotlin 어휘 자가 학습*. **MCP 룰 `parameterized-test-via-csvsource` 후보**.

### 10. BlackjackGame 객체 권유 (Thread 11) — PR #109·#113 결 합류

reviewer:

> *"컨트롤러가 여전히 게임 진행에 대한 도메인 로직을 알고 있어보여요. 게임을 관제하는 게임 객체를 만들어보는 건 어떨까요? 아래 형태처럼 코드를 구성해보셔도 좋겠어요 :)*
> ```kotlin
> val blackjackGame = BlackjackGame()
> val gameResult = blackjackGame.start()
> print(gameResult)
> ```"

학생 자가 적용 + 자가 한계 인정:

> *"기존의 로직 전부를 옮기기에는 도메인에서 InputView를 알아야하거나 OutputView를 알아야하는 상황이 발생할 것 같아 일부만 옮겨진 것 같습니다... 최대한 옮겨보려 했는데, 해당 내용에 대해서 조금 더 코멘트 주실 수 있으실까요..?"*

reviewer 답변 (Thread 20):

> *"이 부분은 여전히 도메인 로직이 보이네요! 람다나 인터페이스를 활용해서 View의 요소를 간접적으로 전달해주는 방법을 고민해보세요 :) 그리고, 코드 자동 정렬 단축키는 늘 생활화하세요!"*

→ **도메인 ↔ View 분리 + 고차함수 활용 + IDE 단축키 생활화** = PR #109·#113·#140 결 합류 + **개발 생산성 어휘**. **MCP 룰 `ide-shortcut-as-productivity` 후보**.

### 11. Dealer ↔ 여러 Player 비교 메서드 (Thread 21)

reviewer: *"딜러에게 한 플레이어와 비교하는 메서드도 있으니, 여러 플레이어와 비교하는 메서드가 있어도 자연스럽겠네요!"*

→ **단일 비교 vs 다중 비교 메서드 자연성** = PR #143 (Dealer.allPlayersMatchProfit) 결 합류.

### 12. Hand 초기값 생성자 (Thread 22) — 테스트 용이

reviewer: *"생성자로부터 초기 값을 받을 수 있게 구성해보는 건 어떨까요? 테스트 코드를 더욱 쉽게 작성할 수 있겠어요 :)"*

→ **테스트 = 도메인 설계 개선 어휘** = PR #87·#129·#142·#109·#140 결 합류 누적 5+ PR.

### 13. Card 인스턴스 재활용 (Thread 24)

reviewer: *"카드 인스턴스를 재활용해볼 수 있을까요?"*

→ **Flyweight 패턴** = PR #118·#138·#142 결 합류 누적 4+ PR.

### 14. `humanInfo open` 의미 (Thread 25)

reviewer: *"humanInfo가 open인 특별한 이유가 있었나요? 상속관계에서 자식이 부모의 어디까지 접근 가능한지 문법적으로 많이 공부해보셔도 좋겠네요 :)"*

→ **`open` 키워드 의미 의식** = PR #129·#126·#138·#142·#143·#113 (sealed vs open vs abstract) 결 합류.

### 15. Human.DEFAULT_CARD_COUNT + MAX_POINT 묻기 → 블랙잭 (Thread 26)

reviewer: *"`카드의 수가 DEFAULT_CARD_COUNT 개고, 카드 점수의 합이 MAX_POINT이면 무엇일까요?` 라고 다른 개발자에게 묻는다면, 블랙잭이겠네요! 라고 답할 수 있는 개발자는 몇이나 될까요?"*

→ **추상 상수 vs 도메인 어휘** — *추상화가 의미를 가리는 안티패턴*. **MCP 룰 `abstract-constant-hides-domain-meaning` 후보**.

### 16. 베팅 값 변경 vs 처음 생성 (Thread 27)

reviewer 자가 후속 (오해 후 정정):

> *"아하 이 부분은 초기 베팅 값을 초기화하는 로직이었군요? 이후에도 또 다시 변경이 일어날 거라는 오해가 먼저 들어서 이렇게 피드백을 남겼네요. 처음 입력받은 베팅 값을 이후에 변경하도록 구성하지 않고, 처음 생성할 때 넣어주도록 구성해보는 건 어떨까요?"*

→ **reviewer 자가 오해 후 정정** = PR #126·#130·#142·#109 결 합류 누적 5+ PR. **`var betAmount` vs `val betAmount`** = PR #139·#142·#141 결 합류.

### 17. assertAll 차이 (Thread 28)

reviewer: *"assertAll로 감싸는 것과 어떤 차이점이 있을까요?"*

→ **테스트 fail-fast vs fail-all** = PR #109 (assert-all-vs-multiple-assertions) 결 합류.

## 대표 인용

> [Hogu59 PR 본문]
> *"`손`이 `덱`과 `카드`를 동시에 아는 것이 맞는가에 대한 코멘트를 보면서, 제가 생각했던 의존성 흐름과 실제로 제가 짠 코드가 다르다는 것을 발견하게 되었습니다."*

→ **학생 자가 의존성 흐름 의식** — *학습 가시화 정점*.

> [malibinYun Thread 7]
> *"다른 개발자들은 'A' 대신 'ACE'를 넣는다면, 또, 'S'대신 Spade, spade ,♠️를 넣는다면 어떻게 될까요?"*

→ **입력 다양성 통제 X 어휘** — *동료 개발자 시점*.

> [Hogu59 Thread 7]
> *"제가 모든 경우를 파악하거나 통제할 수 있다고 착각했습니다."*

→ **학생 자가 메타 학습 어휘**.

> [malibinYun Thread 10]
> *"내가 작성한 코드더라도, 프로젝트에 들어가는 순간 그건 개인의 것이 아닌, 팀의 코드이니까요 :)"*

→ **팀의 코드 어휘** — *maintainer perspective locator 정점*.

> [Hogu59 Thread 10]
> *"나의 코드가 아니라 팀의 코드로서 가치가 있는 코드인가.. 알려주셔서 감사합니다!"*

→ **학생 자가 *팀의 코드* 메타 학습**.

> [malibinYun Thread 2]
> *"음~ 저는 Human이 Human 끼리 비교하는 것도 하나의 방법이라 생각이 드네요. 플레이어 중 한 명이 딜러가 되어서 게임을 이어가게 만드는 유연함을 제공해줄 수도 있으니까요."*

→ **유연성 어휘 + 미래 가능성 의식**.

## 얻은 교훈 (보편)

1. **학생 자가 의존성 흐름 의식 + 자가 시각화 (의존성 그래프 그림)** — **MCP 룰 `student-self-dependency-graph-visualization` 후보**.

2. **유연성 vs 현재 룰 트레이드오프 (Human-Human 비교)** — **MCP 룰 `flexibility-vs-current-rule-tradeoff` 후보**.

3. **입력 다양성 = 모든 경우 통제 X 학생 자가 메타 학습** — **MCP 룰 `input-diversity-uncontrollable` 후보**.

4. **팀의 코드 어휘** — **MCP 룰 `team-code-vs-personal-code` 후보**. maintainer perspective locator 정점 누적 12+ PR.

5. **외부 책 인용 (엘레강트 오브젝트)** — **MCP 룰 `external-book-reference` 후보**.

6. **추상 상수 vs 도메인 어휘** — *DEFAULT_CARD_COUNT + MAX_POINT 가 *블랙잭* 의미 안 드러남*. **MCP 룰 `abstract-constant-hides-domain-meaning` 후보**.

7. **함수 인자 옵션 다양성 의식** — **MCP 룰 `parameter-options-exploration` 후보**.

8. **IDE 단축키 = 개발 생산성** — **MCP 룰 `ide-shortcut-as-productivity` 후보**.

9. **JUnit 파라미터화 테스트 (CsvSource)** — **MCP 룰 `parameterized-test-via-csvsource` 후보**.

10. **reviewer 자가 오해 후 정정** — 누적 5+ PR.

11. **명명 6+ 변경 자가 적용 (ResultType/ValueType/MarkType/isPossible/draw/Mark)** — 누적 9+ PR.

12. **모든 28 스레드 학생 자가 답변 + 자가 후속 의문 6+ 회** — *2024 코호트 정점*.

## MCP 룰 후보 신규 + 강화 (PR #96)

| 룰 | 신규 / 강화 | 누적 PR |
|---|---|---|
| `student-self-dependency-graph-visualization` | 신규 | 1 (#96) |
| `flexibility-vs-current-rule-tradeoff` | 신규 | 1 (#96) |
| `input-diversity-uncontrollable` | 신규 | 1 (#96) |
| `team-code-vs-personal-code` | 신규 | 1 (#96) |
| `external-book-reference` | 신규 | 4+ (#96·#136·#118·#108) |
| `abstract-constant-hides-domain-meaning` | 신규 | 1 (#96) |
| `parameter-options-exploration` | 신규 | 1 (#96) |
| `ide-shortcut-as-productivity` | 신규 | 1 (#96) |
| `parameterized-test-via-csvsource` | 신규 | 1 (#96) |
| `reviewer-scenario-code-example` | 강화 | 2+ (#118·#96) |
| `function-name-vs-return-semantic-mismatch` | 강화 | 9+ |
| `blackjack-rule-fidelity-comparison` | 강화 | 13+ ⭐⭐⭐ |
| `flyweight-pattern-card-caching` | 강화 | 4+ |
| `lateinit-var-call-order-invariant` | 강화 | 12+ |
