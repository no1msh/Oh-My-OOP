# PR #22 — [블랙잭 1단계] 링링 미션

- **Reviewee:** [@RightHennessy](https://github.com/RightHennessy)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/22
- **단계:** 1단계
- **리뷰어:** [@galcyurio](https://github.com/galcyurio) (**신규 reviewer — 10번째**)
- **라인 코멘트:** 16 스레드 — *학생 자가 PR 본문 질문 명시 + OCP 칭찬 + 테스트 가독성 (Google Testing Blog 인용) + LinkedList pop 자가 학습*
- **신규 reviewee** (2023년 3월 코호트).

## 잘한 점 (설계 관점)

- **PR 본문 자가 질문 명시** — *"아, 추가로 제 질문들은 아래 코드에 달아놓았습니다. 😊"* — **PR 본문에 *질문이 인라인 코멘트로* 분산 명시**. PR #76·#82·#107·#46·#91·#11·#13 결.
- **`GameResultCondition` enum + OCP 칭찬** — galcyurio: *"말씀해주신 것처럼 장단점이 있습니다. 지금 방법은 추가적인 분기가 생길 경우 기존 코드를 건드리지 않고도 enum constant를 추가하여 기능을 추가할 수 있다는 장점이 있습니다. 그래서 저는 OOP 관점에서 보았을 때, 개방 폐쇄 원칙을 잘 지켰다고 생각해요 :+1:"*. **개방폐쇄 원칙 (OCP) 명시 + 칭찬**. PR #16·#137·#100 의 *원칙 명시 어휘* 결.
- **`CardPack` 분리 칭찬** — galcyurio: *"네, 잘 구현해주셨습니다 :+1:"* (CardPack 분리).
- **테스트 가독성 자가 학습 (Google Testing Blog 인용)** — galcyurio + 학생 인용:

> *Google Testing Blog (Testing on the Toilet): "One property of a good test is `clarity`. Clarity means that a test should serve as readable documentation for humans, describing the code being tested in terms of its public APIs. Two more important properties are `completeness` and `conciseness`. A test is complete when its body contains all of the information you need to understand it, and concise when it doesn't contain..."*

**외부 자료 + 학생 자가 인용 + 자가 적용**. PR #91·#95·#23·#26·#16·#46·#77 의 *외부 자료* 결의 *학생측 외부 자료 인용*.

## 못한 점 (설계 관점)

### 1. `Cards size == INITIAL_CARDS_SIZE` 검증 — 학생 자가 의문

학생 자가 의문: *"카드를 한장도 안받은 경우에 가진 카드를 보여주기위한 코드인데요, 이 하나를 위해 Controller에서 Cards를 import 하는 것이 적합할까요? 아니면 controller 안에서 상수처리를 해야할지, view에서 경우를 나누어 처리를 해줘야할지 궁금합니다!"*

galcyurio 1: *"Cards는 항상 size가 INITIAL_CARDS_SIZE일 수밖에 없으니 체크를 할 필요가 없습니다."*

```kotlin
init {
    require(cards.size == INITIAL_CARDS_SIZE)
}
```

galcyurio 2: *"위와 별개로 domain에서 ui를 의존하는 반대의 경우는 문제가 되더라도 ui layer에서 domain layer의 클래스를 사용하는 것은 문제 없습니다."*

galcyurio 3: PR #22 다른 위치 URL 인용.

학생 자가 답변: *"아 이 로직은 한장도 더 받지 않고 'n'을 응답하고 끝난 경우 현재 들고 있는 카드를 보여주기 위해 있는 로직이어서 삭제하지 않았습니다..! (한장이라도 더 받은 사람은 마지막에 'n'을 입력해도 가진 카드를 출력하지 않습니다...!)"*

→ **3 가지 학습**:

#### 1.1. *`init { require }` Kotlin 어휘 = 객체 invariant*

- *생성 시점 검증* + *Cards size 보장*.
- PR #16·#9·#107 결.

#### 1.2. *`UI → Domain` 의존 vs `Domain → UI` 의존 양방향성 명시*

galcyurio: *"domain에서 ui를 의존하는 반대의 경우는 문제가 되더라도 ui layer에서 domain layer의 클래스를 사용하는 것은 문제 없습니다."* — **MCP 룰 후보 `dependency-direction-clarification`** = *UI → Domain 의존은 OK / Domain → UI 의존은 문제*. PR #145 (junseo511 *View 가 Domain 객체 생성*) 결.

#### 1.3. *학생 자가 *기능 의도* 명시 후 자가 결정*

*"한장도 더 받지 않고 'n'을 응답하고 끝난 경우 현재 들고 있는 카드를 보여주기 위해"* — **기능 의도 명시 후 자가 유지 결정**. PR #91·#11·#43 결.

### 2. `enum 내 companion object` 불가 + 상수 공유

학생: *"21을 상수처리하고 싶었는데, enum 클래스 내부에서는 companion object를 사용불가하다는 것을 알았습니다. 상수처리를 하지 않는 것이 맞을까요? 아니면 다른 클래스에서 이미 존재하는 상수값을 가져오는 것이 맞을까요?"*

galcyurio: *"다른 곳에서 해당 상수를 쓰지 않는다면 top-level에 private으로 선언해두면 됩니다. 하지만 Player를 보니 이미 선언된 곳이 있으니 해당 상수를 public으로 풀어주신 후 참조하는게 어떨까요?"*

학생: *"조언해주신대로 Player의 상수를 참조하였습니다! 🤗"*

→ **3 가지 학습**:

#### 2.1. *enum 내 companion object Kotlin 어휘 (실제로는 가능)*

학생의 *"enum 내부에서는 companion object를 사용불가"* = *Kotlin 어휘 학생 자가 잘못된 가정* — *실제로는 가능* (Kotlin 1.x+).

#### 2.2. *상수 위치 = 사용처 응집*

- *한 곳만 사용* = *top-level private*.
- *여러 곳 사용* = *기존 상수 위치 public*.

**MCP 룰 후보 `constant-shared-vs-local`**. PR #11·#16 결.

#### 2.3. *Player 의 상수를 GameResultCondition enum 이 참조 = 의존 방향*

*도메인 객체 (Player) 의 상수* → *enum 이 참조* = *의존 OK*. 반대 (Player → enum 상수) 도 OK.

### 3. `Card` View 로직 누수

galcyurio: *"UI 로직입니다. UI layer의 View에서 데이터를 받아서 UI를 그리도록 개선해보세요."*

학생: *"UI 로직에서 이뤄져야 하는 일에 대해 모두 UI에서 행할 수 있도록 코드를 변경하였습니다."*

→ **MCP 기존 룰 `enum-with-ui-value`**. PR #137·#77·#23·#26·#16·#9·#11·#13·#82 결.

### 4. `ALL_CARDS` mutable static — CardPack 객체 분리 권유

galcyurio: *"변경 가능한 컬렉션을 static하게 선언해두는 것은 좋지 않습니다. `ALL_CARDS`가 모두 remove되는 경우에 이 프로그램은 더 이상 정상적으로 동작하지 않을거에요. 모든 카드를 불변으로 캐시해두는 것과 그 카드들을 하나씩 draw 하는 기능을 별도로 분리해보세요."*

학생 자가 의문: *"제가 이해를 잘 못한 것 같아 추가적으로 질문을 드리자면.. > 모든 카드를 불변으로 캐시해두는 것과 그 카드들을 하나씩 draw 하는 기능을 별도로 분리해보세요. 이 말씀이 단순히 remove를 하지 말라는 뜻일까요? 아니면 캐싱을 따로 해두고 다른 카드 덱을 만들어서 그 안에서 draw 하라는 말씀이실까요? 😂"*

galcyurio: *"remove 뿐만 아니라 add 또한 되어선 안됩니다. 현재 구현은 계속해서 같은 종류의 카드가 나올 수도 있기 때문에 잘못된 구현입니다. > 캐싱을 따로 해두고 다른 카드 덱을 만들어서 그 안에서 draw 하라는 말씀이실까요? 😂 맞습니다. 변경 가능한 컬렉션을 가진 CardPack과 같은 구현체를 만들어보세요."*

→ **3 가지 학습**:

#### 4.1. *Static mutable = 전역 상태 + 의도 변화 위험*

- *`val ALL_CARDS: MutableList`* = *프로그램 전역 + remove/add 가능*.
- *모두 remove = 동작 X / add = 중복 카드*.

**MCP 룰 후보 `static-mutable-collection-unsafe`**. PR #11·#46·#16 결.

#### 4.2. *불변 캐시 + 가변 Deck 분리*

- *`val ALL_CARDS: List<Card>`* (불변 + static) + *`class CardPack(remainingCards: MutableList<Card>)`* (가변 인스턴스) — *원본 보호 + draw 시 인스턴스 카드 소진*.

PR #74·#108·#133·#46·#77·#26 결.

#### 4.3. *학생 자가 의문 + reviewer 명확화*

*"이 말씀이 단순히 remove를 하지 말라는 뜻일까요?"* — **학생 자가 학습 갭 명시 모범**. PR #95·#107·#16·#76 결.

### 5. `GameResult` 승/패/무 String = UI 로직

galcyurio: *"승, 패, 무 String은 UI 로직입니다. UI layer에서 GameResult에 따라 승, 패, 무를 출력하도록 만들어보세요."*

→ **enum-with-ui-value 강화**. PR #137·#77·#23·#26·#16·#9·#11·#13·#82·#100·#82·#80 결.

### 6. `when` 활용

galcyurio: *"when expression을 활용해보세요."*

학생: *"when으로 분기처리를 하도록 코드를 변경하였습니다!"*

→ **Kotlin when 어휘**. PR #91·#9·#26·#43·#16·#76 결.

### 7. `Shape` UI 로직

galcyurio: *"UI 로직입니다. UI layer에서 데이터를 받아서 그리도록 만들어보세요."*

→ **enum-with-ui-value 결**.

### 8. `BlackJackRefereeTest` 함수명 = 룰 설명 가능?

galcyurio: *"테스트 케이스 이름에는 어떻게 동작하는지 또는 요구사항이 명시되어야 합니다. 누군가에게 블랙잭의 룰에 대해 설명할 때, `딜러를 상대로 링링은 패배하고 우기는 승리하고 써니는 무승부`하는 게임이라고 이야기할 수 있을까요? 링링은 왜 패배하는지, 우기는 왜 승리하는지, 써니는 왜 무승부하는지가 드러나야 합니다."*

학생: *"카드합으로 승무패를 구별한다는 것을 반영하여 함수 이름을 변경하였습니다!"*

→ **테스트명 = 룰 설명 가능 + 이유 명시**. PR #26·#46·#99·#107·#145·#9 의 *테스트 명세서* 결.

### 9. `테스트 본문 = clarity + completeness + conciseness` Google Blog

galcyurio: *"테스트 본문에는 테스트하고자 하는 것을 이해할 수 있는 정보를 모두 가지고 있어야 하고, 반대로 불필요한 내용을 전부 감추어야 합니다."* + [Google Testing Blog 글](https://testing.googleblog.com/2014/03/testing-on-toilet-what-makes-good-test.html)

학생 자가 인용 + 매우 긴 답변 (위 §잘한점) + 자가 정리:

> *"제가 생각했을때 이 테스트에서 불필요한 정보는 점수에 영향을 주지 않는 카드의 모양, 플레이어의 이름이고, 꼭 필요한 정보는 뽑은 카드 목록과 그 카드의 합이라고 생각했습니다."*

galcyurio 추가 권유 + 코드 예시:

```kotlin
// given
val player1 = createPlayer(CardNumber.SEVEN, CardNumber.K) // 9
val player2 = createPlayer(CardNumber.EIGHT, CardNumber.ONE) // 17
```

학생: *"테스트 코드 밖에서 불필요한 요소들을 처리하여, 테스트 코드 안에서는 필요한 요소들만 입력 받아 객체를 반환하도록 하는 함수를 생성하였습니다."*

→ **5 가지 학습**:

#### 9.1. *학생 자가 외부 자료 인용*

학생이 *Google Blog 영어 원문 자가 발췌* + *3 properties (clarity/completeness/conciseness) 명시* — **학생측 외부 자료 인용 모범**. PR #91·#77 의 *외부 책 인용* 결.

#### 9.2. *불필요 정보 분리 = 테스트 픽스처 함수 추출*

- *`createPlayer(cardNumber, cardNumber)` 헬퍼* = *카드 모양/플레이어 이름 등 불필요 정보 함수 내부에 숨김*.

PR #99 (KwonDae *Test Fixture 품질*) / PR #91 (murjune *createBustPlayers vararg*) 결.

#### 9.3. *학생 자가 의문 + reviewer 코드 예시*

학생 자가 *불필요 정보 식별 (모양/이름)* + reviewer 가 *코드 예시 (createPlayer)* 첨부.

#### 9.4. *side effect 없이 반환 = 유연한 구조*

galcyurio: *"side effect 없이 반환을 해주는 현재 구조가 더 유연한 구조입..."* — **함수형 + 결과 반환**.

#### 9.5. *불필요 정보 검토 vs 학생 인식 갭*

galcyurio 추가 권유 *"여전히 테스트 코드에 불필요한 정보가 남아있습니다"* — *학생이 첫 시도 후에도 추가 불필요 정보 존재* — **테스트 정제 = 반복 학습**.

### 10. `BlackJackRefereeTest 구조` 자가 의문

학생: *"BlackJackRefree는 딜러와 플레이어들을 넘겨 각각의 사람들이 가지는 카드를 비교하는 구조라, 무엇을 테스트하고 있는지 한눈에 깔끔하게 보일 수 없는 구조라 생각했습다. (물론 제가 놓치고 있는 것일 확률이 높지만요..) 그렇다면 이 구조가 잘못된 것일까요? 지금처럼 `BlackJackRefree` 를 통해 결과를 새로운 `List<PlayerGameResult>`로 만들어서 반환하는 것이 아니라, 결과를 바로바로 출력해주는 것이 더 나을까요?"*

galcyurio: *"side effect 없이 반환을 해주는 현재 구조가 더 유연한 구조입..."*

→ **side effect X 결과 반환 = 함수형 정공**. PR #145·#46·#76 결.

### 11. `DealerTest` 동일 불필요 정보

galcyurio: *"BlackJackRefereeTest와 마찬가지로 나머지 테스트 케이스도 불필요한 정보를 분리해보세요!"*

→ **반복 적용**.

### 12. `LinkedList pop` 활용

galcyurio: *"직접 0번째 카드를 가져오는 대신 `LinkedList`와 같이 이미 잘 만들어진 자료구조를 활용해보는건 어떨까요?"*

학생: *"코틀린에는 왜 pop이 없지.? 라고 생성해서 자꾸 shuffle했던 것 같습니다. Linked List를 사용하면 해결 가능한 거였군요..! ㅎㅎ 수정하였습니다."*

→ **Kotlin 자료구조 어휘 + Java LinkedList 활용**. **MCP 룰 후보 `data-structure-tool-knowledge`**. PR #46·#23 결.

### 13. `CardPack` 최초 1회 셔플

galcyurio: *"현실처럼 CardPack을 최초에 한 번만 셔플해주셔도 충분합니다 :)"*

학생: *"앗.. 그렇군요 ㅎㅎ.. 계속해서 랜덤해서 뽑는다고 생각해서 자꾸 shuffle했던 것 같습니다. 최초 한번만 shuffle 하도록 변경하겠습니다!"*

→ **PR #77·#66 (Deck shuffle 위치) 결**.

### 14. `테스트 함수` 위치 (테스트 함수 아래)

galcyurio: *"테스트 클래스에서는 테스트 함수가 더 중요하므로 테스트 함수 아래에 위치하도록 해주세요."*

→ **테스트 파일 구조 컨벤션**. PR #145 (코드 포맷 가독성) 결.

### 15. `BlackJackRefereeTest` 함수 외부 처리 자가 적용

학생: *"불필요한 일을 테스트 함수 외부에서 처리하도록 코드를 수정하였습니다!"*

→ **자가 적용 명시**.

## 리뷰어 의견 요지 (galcyurio) — **신규 reviewer**

16 코멘트 특징:

- **`:+1:` 칭찬 + `OOP/OCP 원칙` 명시** — *"개방 폐쇄 원칙을 잘 지켰다고 생각해요 :+1:"* — **객체지향 원칙 약어 명시**. PR #16·#137·#100 (조영호 어휘 + KwonDae) 결의 *galcyurio 합류*.
- **외부 자료 첨부** — *Google Testing Blog* — PR #91·#67·#23·#26·#16·#46·#77·#107·#82 결.
- **`UI → Domain 의존 OK / Domain → UI 의존 문제` 명시 어휘** — **MVC 의존 방향 메타**.
- **코드 예시 첨부** — `createPlayer(CardNumber, CardNumber)` 코드.
- **반복 권유 (지속적 정제)** — *"여전히 불필요한 정보가 남아있습니다"* — *학생이 첫 시도 후 추가 정제 필요*.

## 대표 인용

> *"그래서 저는 OOP 관점에서 보았을 때, 개방 폐쇄 원칙을 잘 지켰다고 생각해요 :+1:"* — galcyurio (OCP 명시 칭찬)

> *"domain에서 ui를 의존하는 반대의 경우는 문제가 되더라도 ui layer에서 domain layer의 클래스를 사용하는 것은 문제 없습니다."* — galcyurio (MVC 의존 방향 메타)

> *"One property of a good test is `clarity`. Clarity means that a test should serve as readable documentation for humans, describing the code being tested in terms of its public APIs."* — RightHennessy (Google Testing Blog 자가 인용)

> *"코틀린에는 왜 pop이 없지.? 라고 생각했었는데 Linked List를 사용하면 해결 가능한 거였군요..!"* — RightHennessy (Kotlin 자료구조 어휘 자가 학습)

> *"이 말씀이 단순히 remove를 하지 말라는 뜻일까요? 아니면 캐싱을 따로 해두고 다른 카드 덱을 만들어서 그 안에서 draw 하라는 말씀이실까요? 😂"* — RightHennessy (학생 자가 학습 갭 명시)

## 얻은 교훈

1. **`MVC 의존 방향 메타`** — *UI → Domain OK / Domain → UI 문제*. **MCP 룰 후보 `dependency-direction-clarification`**.

2. **`개방 폐쇄 원칙 (OCP)` 명시** — *enum 추가 = 기존 코드 변경 X = OCP 정공*. PR #16·#137·#100 결.

3. **`Static mutable collection unsafe`** — *전역 + remove/add 가능 = 동작 X*. **MCP 룰 후보 `static-mutable-collection-unsafe`**.

4. **`불변 캐시 + 가변 Deck 인스턴스 분리`** — PR #74·#108·#133·#46·#77·#26 결.

5. **`테스트 본문 3 properties (clarity/completeness/conciseness)`** — *Google Testing Blog 외부 자료 + 학생 자가 인용*. **MCP 룰 후보 `test-3-properties-clarity-completeness-conciseness`**.

6. **`LinkedList pop` 활용** — *Kotlin 자료구조 어휘 + Java 표준 라이브러리 활용*. **MCP 룰 후보 `data-structure-tool-knowledge`**.

7. **`enum 내 companion object` 어휘 가능성** — *학생 자가 잘못된 가정 (불가) vs 실제 가능*. **MCP 룰 후보 `kotlin-vocabulary-misconception`**.

8. **`상수 공유 vs local`** — *한 곳 = top-level private / 여러 곳 = public*. **MCP 룰 후보 `constant-shared-vs-local`**.

9. **`테스트명 = 룰 설명 가능 + 이유 명시`** — *"왜 패배/승리/무승부?"* — PR #26·#46·#99·#107·#145·#9 결.

10. **`테스트 정제 = 반복 학습`** — *첫 시도 후에도 추가 불필요 정보 검토*. PR #99 (Test Fixture 진화) 결.

11. **`init { require }` Kotlin 어휘 = 객체 invariant** — PR #16·#9·#107 결.

12. **`side effect X 결과 반환 = 함수형 정공`** — PR #145·#46·#76 결.

13. **`외부 자료 학생측 인용` 모범** — PR #91·#77 결의 *학생측 강화*.

14. **`galcyurio reviewer 패턴`** — *간결 + 원칙 명시 + 외부 자료 + 코드 예시 + 반복 권유* — **10번째 reviewer 합류**.
