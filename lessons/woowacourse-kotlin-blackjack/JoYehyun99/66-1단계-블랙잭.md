# PR #66 — [블랙잭 1단계] 예니 미션

- **Reviewee:** [@JoYehyun99](https://github.com/JoYehyun99)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/66
- **단계:** 1단계
- **리뷰어:** @vagabond95
- **라인 코멘트:** 18 스레드 — *학생 자가 답변 부재 + reviewer 일방향 진단 + 디미터 법칙 명시*
- **신규 reviewee** (2024 코호트). PR #31 (no1msh) 와 동일 reviewer. **PR 본문 짧음 (인사) + 학생 코멘트 응답 없음** = *피드백 반영 명시 X 시나리오*.

## 잘한 점 (설계 관점)

- **deck 재사용 자가 시도** — vagabond95: *"deck 재사용 시도 👍 그런데, shuffled 된 deck 을 저장하고 이를 그대로 리턴할 경우 결과적으로는 매번 똑같이 shuffled 된 deck 이 리턴될 것 같아요. shuffled 해주는 순서를 변경해보면 어떨까요?"*. **자가 시도는 인정 + 추가 권고 결합** (재사용 + 셔플 순서) — PR #46·#74·#108·#133·#136·#23·#26·#77 의 *카드 캐싱* 결.
- **`Hit` 상태 스스로 다음 상태 결정** — vagabond95: *"Hit 에서 처리해주신 것처럼, 상태가 스스로 다음 상태를 결정하도록 리펙토링해볼 수 있을까요~?"* — *State 패턴 부분 자가 적용 인정 + 확장 권유*. PR #98·#39·#43·#152·#93·#136 의 *State.compareTo 다형성* 결.

## 못한 점 (설계 관점)

### 1. `setUp` / `init` 함수명 모호 — vagueName 4 회

vagabond95 1차: *"함수 네이밍이 너무 모호한 것 같아요. 무엇을 초기에 세팅한다는 것인지 구체적으로 명시해주면 좋겠습니다!"* (Controller)

vagabond95 2차: *"마찬가지로 무엇을 초기화하는 것인지 의미가 모호해요. 좀 더 구체적인 이름을 지어주면 좋겠어요."* (State)

vagabond95 3차: *"무엇을 더한것인지 구체적으로 드러나면 좋을 것 같아요."* (Hand.sum)

vagabond95 4차: *"또한 calculate 와 sum 은 비슷한 의미를 가지고 있기 때문에 하나로 줄여도 좋겠습니다!"* (calculateSum)

→ **명명 모호 다중 발생**:
- *`setUp()`* = *무엇을 setup?* 의도 모호.
- *`init()`* = *State 초기화? 게임 초기화?*.
- *`sum()`* = *카드 점수 합? 배팅 금액 합?*.
- *`calculateSum()`* = *calculate + sum 중복 의미*.

**MCP 기존 룰 `vagueName`** — *명사형 vs 동사+명사 구체화*. PR #99·#26·#23·#39·#46·#16·#9·#43·#77·#31 결의 *공통 안티패턴*.

### 2. `BlackjackGame` Model 이 display 알 수 없음

vagabond95: *"BlackjackGame 은 model 이므로, display 가 될거라는 사실은 알 수 없지 않을까요~?"*

→ **MVC 경계** — *Model 이 View 의 표현 책임 알기 X*. PR #26·#23·#16·#77·#39·#46 의 *enum-with-ui-value* 결의 *역방향* (model 이 view 인식).

### 3. `askForPlayerAction` Boolean 반환 의미 모호

vagabond95: *"askForPlayerAction 라는 네이밍을 봤을 때, 리턴되는 boolean 값이 무엇을 의미하는지 알기 어려울 것 같아요."*

→ **부울 명명 = 기준점 명시 누락**:
- *`askForPlayerAction(): Boolean`* — *true 면 Hit? Stay? 동작 계속?*.
- 대안: *`shouldHit(): Boolean`* / *`isContinue(): Boolean`* — *true 의 의미 명시*.

PR #98 (chaehyuns *isWin/isLose 기준점*) / PR #151 (jerry8282 *canHit/isBust 의미 통일*) 결.

### 4. `calculateResult` public 호출 순서 의존

vagabond95: *"calculateResult 함수는 public 이므로 언제든 호출이 가능한데요, play 하지 않고 먼저 호출할 경우 의도하지 않은 결과가 리턴될 것 같아요. 외부에서는 함수를 어떤 순서로 호출할지 알 수 없으므로 특정 함수들의 호출 순서가 보장되어야한다면, 리펙토링이 필요할 것 같습니다!"*

→ **호출 순서 의존 = invariant 결함**:
- *`play()` 전에 `calculateResult()` 호출 가능* = *내부 상태 미완성 + 무의미 결과*.
- 해결:
  - (A) **State 분기 + 예외** = *Play 안 된 상태에서 호출 시 Exception* (PR #91 murjune *논리적 오류*).
  - (B) **결과 객체 반환** = *`play().result()` 체이닝 강제*.
  - (C) **builder 패턴** = *play() 후 result() 단계*.
  - (D) **State 패턴** = *Finished 상태에서만 result() 노출*.

PR #91 (murjune *finishState 미할당 시 예외*) / PR #9 (BeokBeok *Builder 호출 순서*) / PR #16 (SeongHoonC *init 블록 invariant*) 결.

### 5. `딜러 승패 조건식 버그` — suggestion 첨부

vagabond95: *"현재의 조건식으로 딜러의 승리/패배를 판단하기에는 버그를 유발할 가능성이 있어보여요. 예를들어 만약 아래와 같은 상태라면, 어떻게 카운트가 되어야할까요?"*

```kotlin
val dealerWinCount = outcomes.values.count { it.wins == 1 && it.losses == 2 }
```

→ **도메인 룰 정확성 = 결정적 테스트로 검증**:
- *학생 조건식*: *현재 코드의 *비일관 케이스* 미고려*.
- 결정적 테스트 = *Bust 양측 / 무승부 / 다중 플레이어* 시나리오.

PR #26·#46·#16·#23 의 *블랙잭 룰 정확성* 결.

### 6. `WinningState` participants 책임

vagabond95: *"WinningState 를 구하는 로직들은 dealer 와 개별 player 를 모두 알고 있는 participants 에서 스스로 처리할 수 있을 것 같습니다!"*

→ **Tell-Don't-Ask + 일급 컬렉션**:
- *외부 (BlackjackGame) 가 dealer + player 데이터 꺼내 비교* → *Participants 일급 컬렉션이 자기 책임*.

PR #99·#74·#108·#133·#137·#46 의 *일급 컬렉션 자가 학습* 결.

### 7. `Dealer 스스로 처리`

vagabond95: *"해당 구현은 딜러가 스스로 처리할 수 있을 것 같습니다!"*

→ **Tell-Don't-Ask + 책임 위치**. PR #98·#39·#43 의 *Dealer 책임 자가 적용* 결.

### 8. `디미터 법칙 (Law of Demeter)` 명시

vagabond95: *"score 를 구하기 위해 알아야하는 domain depth 가 깊어보여요. `디미터 법칙`을 활용하여, API 를 개선해볼 수 있을까요?"*

→ **디미터 법칙 = 친구의 친구의 친구 X**:
- *`participant.hand.cards.calculateSum()`* = *3 단계 의존 = 디미터 위반*.
- *`participant.score()`* = *1 단계 메시지*.

**MCP 기존 룰 `god-object` 변형 + 디미터 법칙 명시**. PR #93·#133·#146·#137 의 *디미터 법칙 어휘* 결.

### 9. `CardDeck.initial~` public 함수 호출 시기

vagabond95 1차: *"외부에서 함수를 꼭 한번만 호출한다고 보장이 불가능하기 떄문에, initial~ 과 같이 최초에만 호출되기를 기대하는 함수를 public 함수로 제공하는 것은 고민해봐야 할 문제인 것 같아요."*

vagabond95 2차 (메타 어휘): *"접근제한자나 함수의 호출시기 검토가 필요하거나, 해당 함수가 이 클래스에 어울리지 않는다는 신호일 수도 있습니다 :)"*

→ **3 가지 학습**:

#### 9.1. *Public 함수 = 호출 횟수 무관*

- *`initial()` public + 외부 1 회만 호출 기대* = *외부가 N 회 호출 가능 = invariant 결함*.

#### 9.2. *접근제한자 = 호출 제한*

- *private / internal / protected* 활용.

#### 9.3. *"클래스에 어울리지 않는다는 신호"* — 메타 어휘

- *함수 잘못 위치 시그널 = 책임 재배치 권유*. **MCP `oh-my-oop` 의 *책임 잘못 배치 시그널* 어휘**.

PR #9 (BeokBeok *Builder 호출 순서*) / PR #16 (SeongHoonC *init 블록*) / PR #91 (murjune *finishState invariant*) 결.

### 10. `OutputView` 책임 외 정보 + 계산 + 승패 판단

vagabond95 1차: *"이러한 기준은 view 에서 몰라도 되는 정보일 것 같아요."*

vagabond95 2차: *"2 라는 숫자가 무엇인지 좀 더 의미가 드러나면 좋겠습니다."*

vagabond95 3차: *"View 에서 계산할 필요가 없는 부분 같아요!"*

vagabond95 4차: *"View 는 승리/패배 상태를 어떻게 출력할지만 고민할 뿐, 승리 패배 그 자체를 판단해서는 안될 것 같아요."*

→ **View 책임 = 표현 + 입출력 + 도메인 로직 X**:
- *View 가 점수 비교, 승패 판단, 계산 = 도메인 로직 누수*.
- *상수 (`2`) 매직 넘버 = vagueName + 의미 모호*.

PR #28·#79·#16·#26·#9 의 *View 로직 누수* 결.

### 11. `Card 묶음 재활용` 권유

vagabond95: *"Card 묶음 자체는 변경이 없을것이므로, 객체를 재활용해봐도 좋겠네요!"*

→ **불변 객체 + 재활용**. PR #74·#108·#133·#136·#23·#26·#46 의 *Card 캐싱* 결.

### 12. `Controller` 코드 단계별 분리

vagabond95: *"코드를 단계별로 나눠보면 좋을 것 같아요. 현재 코드는 가독성이 조금 떨어지는 것 같습니다 😢"*

→ **함수 추출 + 단계별 의미 명시**. PR #46 (말리빈 *라면 비유*) / PR #99 (KwonDae *Controller god-object*) 결.

### 13. `shuffled() 순서` — 매번 동일 셔플 결과

vagabond95: *"shuffled 된 deck 을 저장하고 이를 그대로 리턴할 경우 결과적으로는 매번 똑같이 shuffled 된 deck 이 리턴될 것 같아요. shuffled 해주는 순서를 변경해보면 어떨까요?"*

→ **셔플 위치 = 재사용 시 매번 vs 1 회 결정**:
- *덱 캐싱 + shuffled() 1 회 = 매번 동일 결과*.
- *덱 캐싱 + shuffled() 매 호출 = 매번 다른 결과 + 캐시 의미 약함*.
- *덱 캐싱 + draw() 시 random 인덱스 = 캐시 + 다양성*.

PR #77 (murjune *Deck shuffle 생성 시 1회 vs 매번*) / PR #74·#26·#23 결.

## 리뷰어 의견 요지 (vagabond95)

18 코멘트 특징 (PR #31 와 동일 reviewer):

- **간결 + 명명 의문 위주** — 명명 모호 진단 4 회 = *vagueName 다중 발생 = 학생 자가 학습 갭 시그널*.
- **`👍` 칭찬 1 회** (deck 재사용 시도) + 권고 17 회 = *피드백 위주 분포*.
- **디미터 법칙 명시** — *원칙 약어 직접 명시*. PR #16 (BeokBeok *SRP/LSP*) / PR #137·#148·#151·#146 의 *조영호 어휘* 결의 *디미터 법칙 어휘*.
- **suggestion 블록 1 회** (딜러 승패 조건식). PR #26·#39·#43·#77 결.
- **메타 어휘** — *"클래스에 어울리지 않는다는 신호"* — *시그널 어휘 = 책임 재배치 진단*.
- **학생 코멘트 응답 부재** — *학생 자가 답변 + 자가 학습 가시화 부족*. PR #43·#46·#91 (학생 자가 토론 풍부) 와 *대조적*.

## 대표 인용

> *"score 를 구하기 위해 알아야하는 domain depth 가 깊어보여요. `디미터 법칙`을 활용하여, API 를 개선해볼 수 있을까요?"* — vagabond95 (디미터 법칙 명시)

> *"접근제한자나 함수의 호출시기 검토가 필요하거나, 해당 함수가 이 클래스에 어울리지 않는다는 신호일 수도 있습니다 :)"* — vagabond95 (책임 재배치 시그널 어휘)

> *"calculateResult 함수는 public 이므로 언제든 호출이 가능한데요, play 하지 않고 먼저 호출할 경우 의도하지 않은 결과가 리턴될 것 같아요."* — vagabond95 (호출 순서 invariant)

> *"View 는 승리/패배 상태를 어떻게 출력할지만 고민할 뿐, 승리 패배 그 자체를 판단해서는 안될 것 같아요."* — vagabond95 (View 책임 명확)

## 얻은 교훈

1. **`명명 모호 다중 (vagueName 4 회)` = 학생 자가 학습 갭 시그널** — *setUp / init / sum / calculateSum* — *동일 PR 에서 4 회 명명 모호 진단 = 학생의 *명명 어휘 미숙* 시그널*. **MCP `vagueName` 강화 가치**.

2. **`함수 호출 순서 의존 = invariant 결함`** — *Public 함수 + 호출 순서 의존 = 외부가 N 회/잘못 순서 호출 가능*. 해결 = *State 분기 + Exception / 결과 객체 반환 / Builder / State 패턴*. PR #91·#9·#16·#46·#39 결.

3. **`디미터 법칙 (Law of Demeter)` 명시** — *친구의 친구의 친구 X = `a.b.c.d` 금지*. PR #93·#133·#146·#137 의 *어휘* 결의 *원칙 명시 강화*.

4. **`Public + 'initial~' 명명 = 호출 시기 검토 시그널`** — *최초 1 회 호출 기대 함수 = private 또는 init 블록* + *"클래스에 어울리지 않는다는 신호"* 메타 어휘. **MCP 룰 후보 `initial-method-public-leak`**.

5. **`부울 명명 기준점 명시 누락`** — *`askForPlayerAction(): Boolean`* — *true 의 의미 명시 X*. PR #98·#151 결.

6. **`Model 이 View 알기 X`** — *MVC 경계 + Model 자기 책임만*. PR #26·#23·#16·#77·#39·#46 결.

7. **`View 책임 = 표현 + 입출력 + 도메인 X`** (4 회 진단) — *학생이 *View 에 도메인 로직* 누수*. PR #28·#79·#16·#26·#9 결.

8. **`shuffled() 위치 = 캐싱 + 다양성 양립 어려움`** — *덱 캐싱 + shuffled 1회 = 매번 동일 / 매 호출 셔플 = 캐시 의미 약함*. PR #77 (murjune *Deck shuffle 위치*) 결.

9. **`Tell-Don't-Ask + participants 일급 컬렉션 자기 책임`** — *외부가 dealer + player 데이터 꺼내 비교 → Participants 가 자기 책임*. PR #99·#74·#108·#133·#137·#46 결.

10. **`학생 자가 답변 부재 시 학습 가시화 ↓`** — *PR #66 = 학생 코멘트 응답 0 회* — *PR #43·#46·#91·#77 의 *학생 자가 토론 풍부* 와 대조*. **PR 본문 자가 의문 / 학생 코멘트 응답 = 학습 가시화 핵심 매개**.

11. **`vagabond95 reviewer 패턴 일관성`** — *PR #31·#66 동일 reviewer = 간결 + 메타 질문 + 디미터 법칙 명시 + 시그널 어휘*. *2023년 3월 + 2024 코호트 모두 활동*.

12. **`도메인 룰 정확성` (딜러 승패 조건식 버그)** — *학생 코드의 비일관 케이스 미고려*. PR #26·#46·#16·#23 의 *블랙잭 룰 정확성* 결.
