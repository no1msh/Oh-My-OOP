# PR #77 — [블랙잭 1단계] 오둥이 미션

- **Reviewee:** [@murjune](https://github.com/murjune)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/77
- **단계:** 1단계
- **리뷰어:** @KwonDae (제이든)
- **라인 코멘트:** 24 스레드 — *학생 자가 PR 본문 의문 + 외부 블로그 인용 + Higher Order Function 자가 학습 + 일급컬렉션 정의 자가 주장*
- **신규 reviewee** (2024년 코호트). PR #99·#67 (KwonDae) 와 동일 reviewer.

## 잘한 점 (설계 관점)

- **PR 본문 자가 의문 + 이미지 첨부** — *"Card 들의 합을 계산하는 방식을 동적으로 바꾸고 싶어 위와 같이 생성자에 calculator 객체를 만들었습니다. 그러나, calculator를 domain model(일급 컬렉션)에 넣는 것이 맞는것일까.. 라는 고민을 하고 있습니다. 이에 대해 제이든의 피드백을 받고 싶습니다🙋‍♂️"* + 코드 이미지 첨부. **학생 자가 학습 모범 + 시각 자료**. PR #128·#135·#108·#133·#136·#137·#148·#151·#98·#95·#99 결.
- **`value class` Kotlin 어휘 자가 활용** — KwonDae 칭찬: *"value class 활용 👍"*. **Kotlin 1.5+ value class = inline 성능 + 타입 안전성** — PR #137·#148·#90·#146 의 *Kotlin 어휘 학습* 결.
- **`Read-Only 컬렉션 + plus operator` 자가 활용** — KwonDae 칭찬: *"Read-Only 컬렉션을 사용하기 때문에 plus 연산자로 처리해주는 부분 인상깊었네요 👍"*. **불변 객체 + 새 컬렉션 반환** — PR #46 (krrong *operator times*) / PR #39 (rhthrhrl0 *컬렉션 + operator*) 결.
- **`TestFixture` 활용 풍부** — KwonDae 칭찬 2회 (`CardFixtures.kt`, `DeckFixtures.kt`): *"TestFixture를 잘 활용해주셨네요 💯"* / *"TestFixture 사용으로 가독성이 좋아졌네요 👍"*. **PR #99 (s6m1n *Test Fixture 품질*) 권고 자가 적용 모범** — *KwonDae 가 양 PR 모두 리뷰* 했기에 *PR #99 학생 자가 학습이 PR #77 학생에게 권고로 이어진 패턴* 가능성.
- **`Deck.removeLast()` 빈 컬렉션 사전 방어** — KwonDae 칭찬: *"룰 마다 다르겠지만? cards가 비어있을 때 removeLast()가 예외가 발생할 수 있는데 사전에 막아주는 코드 좋습니다 👍"*. **방어 코드 + Kotlin 어휘**.
- **`UiModel` View 캡슐화 자가 학습** — KwonDae 칭찬: *"UiModel로 View에서 나타낼 데이터들을 캡슐화 해주셨군요..! 👍"*. **MVVM / Presentation Model 어휘** — PR #88·#131 의 *view-by-stage-split* 결의 진화.
- **추상화 활용** — KwonDae 칭찬: *"추상화를 잘 이용해주셨네요 👍 공통 로직을 중복으로 구현하기 보다 super 키워드를 활용해 한곳에서 관리할 수 있도록 개선해볼 수 있겠네요!"* + 외부 글 ([Kotlin 공식 문서 - inheritance](https://kotlinlang.org/docs/inheritance.html#calling-the-superclass-implementation)). **외부 자료 첨부**.

## 못한 점 (설계 관점)

### 1. `Hand 책임 위치` — 학생 자가 깊은 토론 (Hand vs Participant)

KwonDae 1차: *"현재 참가자(플레이어, 딜러)의 상태(들고있는 카드 정보, bust 여부 등)을 알고있다면 계산이 간소화 될거라 생각합니다. 참가자가 Hit를 수행할 때 마다 현재 Ace를 들고있다면 현재 스코어에 따라 1과 11중 어떤 값을 취해야 할 지 판단할 수 있기 때문에 참가자(Participant)가 스스로 판단하여 점수를 계산하게끔 할 수 있을거에요"*

학생 자가 반박: *"나의 상태(score) 도 결국 Hand의 상태(cards)를 가져와 계산하는 것이라 생각합니다. Hand 가 스스로 점수가 몇 점인지, 버스트인지, 블랙잭인지 판단하는 것이 좋다고 생각합니다!"*

KwonDae 2차: *"카드를 가져오거나 내 상태에 대한 판단은 Hand가 하는걸까요? 이러한 현재 내 카드에 대한 판단은 참가자가 할 수 있을겁니다. 또한, 이 로직은 딜러와 플레이어의 공통 로직이 되겠네요!"*

학생 후속: *"참여자(Participant)가 Hand 의 상태를 가져와 판단하기 보다는 Hand 가 자신의 상태(cards)를 기반으로 스스로 점수가 몇 점인지, 버스트인지, 블랙잭인지 판단하는 것이 좋다고 생각합니다!"*

→ **3 가지 학습**:

#### 1.1. *책임 위치 = Hand vs Participant (양방향 토론)*

- **Hand 책임** (학생 주장):
  - *카드 모음의 상태 (점수, Bust, BlackJack) 는 *카드 모음 자체* 가 알 수 있음*.
  - *Participant 가 Hand 위임 = 응집*.
- **Participant 책임** (KwonDae 주장):
  - *Ace 1↔11 결정은 *현재 스코어* 에 따라 변동 → 참가자 상태와 결합*.
  - *Hit 명령 → Participant 가 자기 상태 판단 + Hand 업데이트*.

**둘 다 유효 + 다른 트레이드오프** — *Hand = 데이터 응집 / Participant = 상태 변경 응집*. PR #99 (s6m1n *Hand 클래스 도입*) / PR #98 (chaehyuns *State + HandCards*) / PR #46 (krrong *CardBunch*) 결.

#### 1.2. *학생 자가 주장 유지 + reviewer 인정*

학생이 *자가 주장 유지* + *KwonDae 가 *학생 주장 수용* (`이 로직은 딜러와 플레이어의 공통 로직` 권고 후 그 이상 밀지 않음)*. **학생/reviewer 양방향 토론 + 학생 결정 존중**. PR #95 (말리빈 *"옳고 그름 없고 장단점만"*) / PR #46 (말리빈 *Dealer→Player vs Participant 토론*) 결.

#### 1.3. *학생 의문에 대한 reviewer 자가 부연 + 사과*

KwonDae: *"앗, 제가 오히려 헷갈리게 질문을 드린거 같아 죄송하네요 🥲 정리하면, 처음에 일급 컬렉션이라고 생각하지 못하고 캡슐화한 클래스라고 생각했었습니다."* — **reviewer 자가 사과 + 자가 부연**. PR #95·#137·#147·#151 의 *reviewer 자가 한계 인정* 결.

### 2. `Hand 일급 컬렉션 정의` — 학생 자가 매우 긴 외부 글 인용 답변

KwonDae 의문: *"이 경우에 Hand는 일급컬렉션 일까요?"*

학생 자가 매우 긴 답변:

```kotlin
class Hand(
    val cards: List<Card>,
    private val pointCalculator: PointCalculator = DefaultPointCalculator(BLACKJACK_NUMBER),
) : List<Card> by cards {
```

> *"저는 일급컬렉션이라고 생각합니다. 이유: 저는 일급컬렉션이 기본 collection 에 `비지니스 로직을 입힌 domain model` 이라고 스스로 정의했습니다. 해당 `Hand` 는 `List<Card> + 비지니스 로직` 형태 입니다. 저는 일급컬렉션이 기본 collection 에 `비지니스 로직을 입힌 domain model`라고 생각하기 때문에 by 키워드를 써도 일급컬렉션이라 생각합니다."*

> *(이후 학생의 외부 블로그 인용)* *"일급컬렉션의 규칙 중 하나는 [컬렉션을 제외한 다른 멤버변수는 존재하지 않아야](https://developerfarm.wordpress.com/2012/02/01/object) ..."*

→ **3 가지 메타 학습**:

#### 2.1. *학생 자가 일급 컬렉션 정의 명시*

학생의 *"스스로 정의했습니다"* — **자가 학습 + 자가 정의** 모범. PR #74·#108·#133·#99·#46 의 *일급 컬렉션 자가 학습* 결.

#### 2.2. *외부 블로그 인용 + 정의 검증*

학생이 *외부 블로그의 일급 컬렉션 규칙 (1 변수만) 인용* + *자가 코드가 규칙 위반인지 자가 검증*. **외부 자료 + 자가 검증 모범** — PR #95·#67·#23·#26·#16·#46 의 *외부 자료 첨부* 결의 *학생측 외부 자료 인용*.

#### 2.3. *`by` 키워드 (Kotlin 위임)*

- *`class Hand(val cards: List<Card>) : List<Card> by cards`* = *Hand 가 `List<Card>` 인터페이스를 *cards 에 위임*.
- **Kotlin Delegation 어휘** — *상속 X + 인터페이스 위임*. PR #74 (송핑크 *Delegate vs 일급 컬렉션 자가 학습 + 트레이드오프*) 결의 *Hand 적용*.

**MCP 룰 후보 — `delegate-vs-firstclass-collection`** = *PR #74 (말리빈 *Delegate 활용 일급 컬렉션 권고 X*) 와 본 PR (학생 *Delegate 사용*) 의 *동일 토론 패턴*. 양가성 인정*.

### 3. `Participant 추상화 + judge` — 참가자 vs 참가자 일반화

KwonDae: *"승부를 판단하는 로직을 참가자 객체가 스스로하게끔 할 수 있을까요? 즉, 플레이어와 딜러 구분 없이 Participant 자체가 다른 Participant와 비교하여 판단할 수 있지 않을까요?"*

```kotlin
abstract class Participant {
   fun judge(otherParticipant: Participant): Result { ... }
}
```

학생 자가 답변 + 코드 + 도메인 룰:

> *"실제 블랙잭 게임에서 딜러와 플레이어가 둘다 Bust 상태 일 때, 플레이어가 지는 것으로 알고 있습니다! 따라서, 딜러와 플레이어의 계산 방식이 약간 다릅니다!! 사실 이 부분이 마음에 들지 않아 개선하기 위해 머리를 싸매고 있네요..😅"*

```kotlin
// Dealer
override fun judge(other: Participant): GameResult =
    when {
        other.isBust() -> GameResult.WIN
        isBust() -> GameResult.LOSE
        ... 나머지는 동일

// Player
override fun judge(other: Participant): GameResult =
    when {
        isBust() -> GameResult.LOSE  // 자기 Bust 우선 (둘 다 Bust 시 플레이어 패배)
        ...
```

KwonDae 후속: *"오둥이의 블랙잭 게임장은 둘 다 bust 일 때 플레이어가 패배하는 룰을 적용하고 있었군요..! 오둥이도 이부분이 마음에 들지 않는다면 같이 고민해봐도 좋을 것 같습니다 :)"*

→ **5 가지 학습**:

#### 3.1. *참가자 vs 참가자 일반화 (`judge(other: Participant)`) 시도*

- *`Dealer.compareResult(player)`* (한 방향) < *`Participant.judge(other)`* (양방향 일반화).
- *형제 구현체 간 강한 의존 차단 + DIP 적용*.

PR #46 (krrong 2단계 *Dealer → Participant 양방향 토론*) / PR #95·#23·#148·#151·#49·#132 결.

#### 3.2. *블랙잭 룰 정확성 (둘 다 Bust 시 플레이어 패배)*

학생 자가 도메인 룰 인용 — *"실제 블랙잭 게임에서 딜러와 플레이어가 둘다 Bust 상태 일 때, 플레이어가 지는 것으로 알고 있습니다!"*. **MCP 룰 후보 `blackjack-rule-fidelity`** + PR #26 (잭슨 *Bust 양측 명시*) / PR #46 (말리빈 *BlackJack ≠ 21*) 결.

#### 3.3. *judge 다형성 vs 비대칭 도메인 룰*

- *판정 룰이 *대칭* (점수 비교) 인 경우 = `Participant.judge` 일반화 정공.
- *판정 룰이 *비대칭* (Player Bust 우선) 인 경우 = *각자 다른 judge 구현* 필요 → *Participant 일반화의 한계*.

#### 3.4. *학생 자가 *마음에 들지 않음* 자가 명시*

*"사실 이 부분이 마음에 들지 않아 개선하기 위해 머리를 싸매고 있네요..😅"* — **학생 자가 *현 코드 만족도 명시*** = *MCP `oh-my-oop` 의 *학생 결정 + reviewer 협력* 어휘와 일치*. PR #128·#135·#108·#133·#98·#95·#99·#46 결.

#### 3.5. *reviewer 가 *학생 결정 존중* + *공동 고민 권유**

KwonDae: *"오둥이도 이부분이 마음에 들지 않는다면 같이 고민해봐도 좋을 것 같습니다"* — **공동 학습 어휘**. PR #46 (말리빈 *"슬랙 DM도 좋고"*) / PR #99 (KwonDae *DM 권유*) 결.

### 4. `play 함수 + onHit 람다 → 생성자 주입` 권유 — Higher Order Function 자가 학습

KwonDae: *"지금 공통 로직을 잘 분리해주셨는데요, play 함수도 `onHit: (String) -> Boolean,` 외에는 동작이 같아서 사용자가 HIt를 할지 말지에 대한 람다는 Player의 생성자에서 받아보는건 어떨까요? Dealer와 같은 [HIgher Order Function](https://kotlinlang.org/docs/lambdas.html#higher-order-functions)이 될 수 있어보여 이 함수도 추상화가 가능할 것 같습니다."*

→ **2 가지 학습**:

#### 4.1. *람다 인자 주입 vs 생성자 주입*

- *매 호출마다 람다 인자 주입* = *호출자가 매번 람다 전달*.
- *생성자에서 람다 보유* = *객체 생성 시 1회 + 매 호출에서 재사용*.
- *호출 횟수 N 회 시* = *생성자 주입 정공*.

#### 4.2. *Higher Order Function 외부 글*

KwonDae 가 *Kotlin 공식 문서 인용* — **외부 자료 첨부**. PR #95·#67·#23·#26·#16·#46 의 *외부 자료 첨부* 결.

### 5. `PointCalculator` 도메인 모델 vs 분리 — PR 본문 자가 의문

PR 본문 학생 자가 의문: *"calculator를 domain model(일급 컬렉션)에 넣는 것이 맞는것일까.."*

KwonDae 답변: *"딜러와 플레이어의 카드 계산 방식이 다른걸까요?.? 이 고민은 Participant 클래스를 abstract class로 추상화 시킴으로써 해결이 가능할거라 생각합니다. (...) 지금 고민중이신 카드계산은 공통로직에 해당하여 추상클래스에서 공통로직으로 소화할 수 있습니다."*

학생 후속: *"게임의 기획이 바뀌어 카드 score 계산 알고리즘이 바뀔 수도 있을 것 같아, 계산 알고리즘을 따로 빼두려 했습니다!"*

→ **2 가지 학습**:

#### 5.1. *전략 패턴 의도 (`PointCalculator` 분리)*

- *카드 점수 계산 알고리즘 변동 가능성* = *전략 객체 (PointCalculator) 분리* 의도.
- *YAGNI 위반 가능성*: *현재 요구사항에는 단일 알고리즘 (블랙잭 점수)*.

PR #146 (yrsel *YAGNI 자가 인정*) / PR #67 (s6m1n 1단계 *PointCalculator 등장 가능*) 결.

#### 5.2. *Participant abstract class 공통 로직 vs 별도 객체*

- *Participant abstract class* = *상속 + 메서드 공통 위치*.
- *별도 PointCalculator* = *컴포지션 + 책임 분리*.

**상속 vs 컴포지션 트레이드오프**. PR #95·#74·#46 결.

### 6. `Rank.label`, `Suit.스페이드` View 누수 자가 학습

KwonDae 1차: *"label 은 출력용으로 존재하는 걸까요? 출력되는 문구를 모양으로 바꾼다던가 수정 사항이 생긴다면 도메인 로직을 수정해야 하겠네요 🧐"*

학생: *"완전 아차싶네요.. 수정하겠습니다!!💪"*

KwonDae 2차: *"`스페이드` 는 도메인로직일까요? 위에 Rank 의 label 처럼 문구가 아닌 모양으로 수정되어야 한다면 도메인 로직이 수정되어야 합니다."*

학생: *"오...! 놏치고 있었네요 반영하겠습니다~!"*

→ **MCP 기존 룰 `enum-with-ui-value`** (lotto/blackjack 다수 PR 결). PR #28·#78·#112·#125·#147·#23·#26 결.

### 7. `Deck.shuffle()` 매번 vs 생성 시 1회

KwonDae: *"카드를 뽑는 로직인 pull() 메서드 내부의 shuffle 이라는 랜덤요소로 테스트가 불가능한 상황입니다. Deck이 생성되는 때 받는 카드들이 이미 랜덤요소가 들어간 상태로 받는다면 매번 pull() 할 때 마다 shuffle 행동이 필요할까요? 실제로 블랙잭 게임에서도 hit 할 때 마다 섞지 않습니다 🧐"*

→ **현실 메타포 + 테스터빌리티**:
- *현실 블랙잭* = *덱 셔플 1회 + 매 카드 뽑기*.
- *학생 코드* = *매 뽑기마다 셔플 (무한 셔플)* = *현실 룰 X + 테스트 불가*.
- *해결* = *Deck 생성 시 셔플 1회 + 매 뽑기는 결정적*.

PR #74·#23·#95·#99·#26·#39·#46 의 *랜덤 테스트* 결.

### 8. `Controller god-object → 참가자 스스로 행동`

KwonDae: *"ParticipantHands를 통해서 플레이어를 생성해야 할까요? `player.firstDraw(deck)` `dealer.firstDraw(deck)` 객체 스스로 행동하게끔 매세지만 던져볼까요?"*

KwonDae 별도: *"이 클래스 대신 참가자(딜러, 플레이어)가 스스로 행동하게끔 할 수 있을까요? Controller에서 이부분에 대한 코멘트 드린 부분과 연결되는 내용입니다."*

→ **Tell-Don't-Ask + Controller god-object**. PR #99·#95·#28·#78·#112·#125·#147·#43 결.

### 9. `GameResult` 승/패/무 도메인 vs UI

KwonDae: *"승,패,무는 도메인 로직일까요~? UI 로직이 수정된다면 여기서 수정이 일어나야 됩니다 🧐"*

→ **enum-with-ui-value 결**.

### 10. `Deck 테스트` 추가 권고

KwonDae: *"이 도메인에 대한 테스트는 진행되지 않았네요. card를 생성자로 주입받음으로써 테스트가 가능하게끔은 잘해주셨습니다 👍 예로, 아래 테스트도 진행해볼까요? > A하트 카드 한장이 남았을 때, Hit시 A하트 카드가 나와야 한다."*

→ **결정적 테스트 케이스 권고**. PR #16·#39·#46·#23·#26 결.

### 11. `ktlint` 확인 권고

KwonDae: *"ktlint 확인해주세요! 👍"* (이미지 첨부).

→ **린터 = 코드 컨벤션 자동화**. PR #41·#137·#148 의 *Kotlin 어휘* 결의 *컨벤션 도구*.

## 리뷰어 의견 요지 (KwonDae / 제이든)

24 코멘트 특징 (PR #99 와 동일 reviewer):

- **이미지 첨부** — ktlint 결과 화면. **시각 자료** — PR #46 (krrong *Parameterized Test 스크린샷*) 결.
- **외부 글 첨부** — *Kotlin 공식 문서 (lambdas + inheritance)*. PR #95·#67·#23·#26·#16·#46 결.
- **메타 질문 + 학생 자가 답변 유도** — *"Hand는 일급컬렉션 일까요?"*, *"label 은 출력용으로 존재하는 걸까요?"*, *"승,패,무는 도메인 로직일까요~?"*.
- **`?.?`, `🧐` 등 가벼운 이모티콘** — *학생 친화적 어조*. PR #99 (KwonDae *`?.?`*) / PR #95 (말리빈 *`?_?`*) 와 동일 어휘.
- **reviewer 자가 사과** — *"앗, 제가 오히려 헷갈리게 질문을 드린거 같아 죄송하네요 🥲"* — PR #95·#46·#137 결.
- **공동 학습 권유** — *"같이 고민해봐도 좋을 것 같습니다"* — PR #46 (말리빈 *DM 권유*) / PR #99 (KwonDae *DM 권유*) 결.
- **학생 결정 존중** — Hand vs Participant 토론에서 *학생 주장 수용 후 추가 권고 X*. **MCP `oh-my-oop` 의 *학생 선택지 인정* 어휘**.

## 대표 인용

> *"calculator를 domain model(일급 컬렉션)에 넣는 것이 맞는것일까.. 라는 고민을 하고 있습니다. 이에 대해 제이든의 피드백을 받고 싶습니다🙋‍♂️"* — murjune (PR 본문 학생 자가 의문 + 이미지)

> *"저는 일급컬렉션이 기본 collection 에 `비지니스 로직을 입힌 domain model` 이라고 스스로 정의했습니다."* — murjune (학생 자가 일급 컬렉션 정의)

> *"실제 블랙잭 게임에서 딜러와 플레이어가 둘다 Bust 상태 일 때, 플레이어가 지는 것으로 알고 있습니다! 따라서, 딜러와 플레이어의 계산 방식이 약간 다릅니다!! 사실 이 부분이 마음에 들지 않아 개선하기 위해 머리를 싸매고 있네요..😅"* — murjune (학생 자가 도메인 룰 인용 + 자가 만족도 명시)

> *"오둥이의 블랙잭 게임장은 둘 다 bust 일 때 플레이어가 패배하는 룰을 적용하고 있었군요..! 오둥이도 이부분이 마음에 들지 않는다면 같이 고민해봐도 좋을 것 같습니다 :)"* — KwonDae (학생 결정 존중 + 공동 학습 권유)

> *"앗, 제가 오히려 헷갈리게 질문을 드린거 같아 죄송하네요 🥲"* — KwonDae (reviewer 자가 사과)

> *"이 경우에 Hand는 일급컬렉션 일까요?"* — KwonDae (정의 자가 의문 + 학생 자가 답변 유도)

## 얻은 교훈

1. **`by` 키워드 (Kotlin Delegation) + 일급 컬렉션 양가성** — *`class Hand(...) : List<Card> by cards`* = *위임 + 일급 컬렉션 양가성*. **MCP 룰 후보 `delegate-vs-firstclass-collection`**. PR #74 (송핑크 *Delegate vs 일급 컬렉션 토론*) 결.

2. **`value class` Kotlin 어휘** — *Kotlin 1.5+ inline 성능 + 타입 안전성*. PR #137·#148·#90·#146·#39 의 *Kotlin 어휘 학습* 결.

3. **`Higher Order Function` + 생성자 주입** — *람다 인자 주입 (매 호출) vs 생성자 주입 (1 회 + 재사용)*. **MCP 룰 후보 `lambda-injection-locus`**. PR #46 (krrong *Kotlin 어휘*) / PR #137·#132·#99·#23 의 *onXX 콜백* 결.

4. **`Hand vs Participant` 책임 위치 양방향 토론** — *Hand = 데이터 응집 / Participant = 상태 변경 응집*. **둘 다 유효 + 다른 트레이드오프 + 학생 결정 존중**. PR #99·#98·#46·#74·#43 의 *Hand 클래스 도입* 결.

5. **`참가자 vs 참가자 일반화 (`judge(other: Participant)`)`** + **블랙잭 룰 비대칭 (둘 다 Bust 시 플레이어 패배)** — *판정 룰이 *대칭* 이면 일반화 정공 / *비대칭* 이면 각자 다른 구현 필요*. **MCP 룰 후보 `judge-symmetry-violation`**. PR #46·#95·#23·#148·#151·#49·#132 결.

6. **`외부 블로그 인용 + 자가 정의 검증`** — *학생이 *외부 일급 컬렉션 정의 (1 변수만)* 인용 + *자가 코드가 규칙 위반인지 자가 검증*. **외부 자료 + 자가 검증 학생 모범**. PR #95·#67·#23·#26·#16·#46 의 *외부 자료 첨부* 결의 *학생측*.

7. **`Deck.shuffle()` 매번 vs 생성 시 1회** — *현실 메타포 + 테스터빌리티*. *현실 룰 = 셔플 1회 + 매 카드 결정적*. PR #74·#23·#95·#99·#26·#39·#46 결.

8. **`reviewer 자가 사과 + 자가 부연`** — *"제가 오히려 헷갈리게 질문을 드린거 같아 죄송하네요 🥲"* — **양방향 토론 + reviewer 자가 한계 인정**. PR #95·#46·#137·#147·#151 결.

9. **`공동 학습 권유`** — *"같이 고민해봐도 좋을 것 같습니다"* — **학생/reviewer 협력 어휘**. PR #46 (말리빈 *DM 권유*) / PR #99 (KwonDae *DM 권유*) 결.

10. **`enum-with-ui-value` 결 (Rank.label, Suit.스페이드)** — **MCP 기존 룰** + lotto/blackjack 다수 PR. PR #28·#78·#112·#125·#147·#23·#26·#16 결.

11. **`Test Fixture 활용`** — *PR #99 (KwonDae *Test Fixture 품질*) 권고가 PR #77 학생에게 *적용된 후 칭찬* 받는 패턴* — **reviewer 의 학습 시드가 다른 학생에게 이어지는 *학습 전파 케이스***.

12. **`PR 본문 자가 의문 + 이미지`** — *코드 이미지 첨부* + *외부 자료 인용* + *자가 학습* — **학생 자가 학습 가시화 모범**. PR #98·#99·#136·#137·#148·#46 결.

13. **`PointCalculator` 분리 = YAGNI 위반 vs 전략 패턴 의도** — *현재 단일 알고리즘 + 미래 변동 가능성*. *PR #146 (YAGNI 자가 인정)* 결. **MCP 룰 후보 `premature-strategy-extraction`**.

14. **`ktlint` 자동 컨벤션 도구** — *Kotlin 어휘 + 컨벤션 자동화*. *리뷰어가 *코드 스타일 = 자동화 도구* 권유*.
