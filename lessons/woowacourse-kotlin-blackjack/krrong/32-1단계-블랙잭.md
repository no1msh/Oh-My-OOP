# PR #32 — [블랙잭 1단계] 크롱 미션

- **Reviewee:** [@krrong](https://github.com/krrong) — **이 학생이 PR #148·#151 의 reviewer** (크롱). 학생/리뷰어 메타 흥미 케이스.
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/32
- **단계:** 1단계
- **리뷰어:** @malibinYun (말리빈)
- **라인 코멘트:** 29 스레드 — *Card 인스턴스 캐싱 + Referee → Dealer 통합 + 사이드 이펙트* 학습 풍부

## 잘한 점 (설계 관점)

- **`Card` 52개 인스턴스 미리 캐싱 + Card.all() 재활용 자가 학습** — 학생 자가 답변: *"이번 리뷰를 받고 `Card`클래스에 미리 52개의 카드 인스턴스를 만들어두고 재사용 할 수 있도록 수정했습니다. 이렇게 되면 기존에 있던 모양과 숫자를 랜덤으로 생성하는 클래스들은 필요 없어진다고 생각이 들어 삭제하였고, `CardDeck` 클래스에서 카드를 뽑을 때 랜덤으로 동작하도록 수정했습니다."* — *카드 = 52개 고정 + 뽑는 순서가 랜덤* 메타포 자가 인식. **PR #148 의 *크롱 리뷰어 본인이 학생 시절 같은 자가 학습 진행* — 메타 흥미 케이스!**
- **`Referee` → `Dealer` 통합 자가 학습** — 학생 자가 답변: *"딜러와 플레이어의 승패를 계산하고 저장하는 `Referee`클래스를 만들어 해당 로직들을 이동했습니다!"* + 추가 후속: *"사이드 이펙트를 발생시키지 않기 위해 Referee 클래스를 삭제하고 승자를 판단하는 로직을 다시 딜러에게 주었습니다! 딜러는 플레이어들의 리스트를 받아서 점수를 비교하고 최종 결과를 `Map<String, Consequence>`형태로 돌려주도록 하였습니다!"*. **Referee 도입 후 부수효과 진단 → 딜러 통합 후퇴 자가 학습 모범**. PR #74 (알송, 말리빈) 의 *Referee 자가 토론* / PR #58 (수달, 페로로) 의 *학생 Referee 추구* 와 같은 결.
- **사이드 이펙트 = Player 가변 vs 결과 반환 자가 학습** — 학생 자가 인용: *"이 메서드에서 발생시키는 사이드 이펙트는 Player 가 갖고 있는 승패에 대한 정보를 바꾸는 것을 의미하는 것으로 이해했는데 맞을까요?"* + 말리빈 인정 + 위키 링크. **사이드 이펙트 정의 자가 학습**. PR #85·#137·#148 와 같은 결.
- **`CardNumber.ACE.value = null` → ACE 1 기본 자가 학습** — 학생 자가 답변: *"이 부분에 대해서 페어와 많은 의견을 나누었습니다. 다른 카드와는 다르게 ACE카드는 1과 11로 사용이 가능하기 때문에 특정 값으로 고정 시키기보다 null로 만든 뒤에 점수를 계산하는 부분에서 상황에 맞게 처리하자는 결론이 나와 위와 같이 작성했습니다! ACE카드는 일반적으로 1로 사용하기 때문에 그에 맞게 수정했습니다!"* + 말리빈: *"`점수를 계산하는 부분에서 상황에 맞게 처리하자`는 의견은 동의해요 :) 하지만, CardNumber 내부 값이 null인지 아닌지 비교하기보다는, CardNumber 자체가 ACE인지 비교를 하면 된다고 생각해요"*. **`null` 우회 vs `is ACE` 명시 비교 자가 학습**.
- **`isSuccessAddCardToPlayer` 함수명 자가 학습** — 말리빈 진단 *"addCard를 했는데 반환은 Boolean이네요 :) C 스타일의 코드처럼 느껴졌습니다."* + 학생 자가 반영. *Boolean 반환 시 명시적 이름*. PR #124·#125·#114·#41 와 같은 결.
- **`Set<Card>` → `List<Card>` 자가 학습** — 말리빈 진단 *"이후에 카드 여러 벌을 가지고 게임을 할 수 있는 요구사항이 추가된다면, Set으로 구현했을 때 어떤 일이 발생할 수 있을까요?"* + 학생: *"블랙잭 도메인에 대한 지식이 부족해서 단순히 52장의 카드로만 진행하는 게임이라고 생각했습니다."* + List 변경. PR #58·#108 와 같은 결.
- **`CardBunch` 2장 검증 자가 후퇴** — 학생 자가 인용: *"굳이 필요한 생성 조건인가?라는 생각을 했습니다. `CardBunch`는 단순히 유저가 들고 있는 카드를 의미하는데 생성 당시 2장임을 검사할 필요가 있는가, 컨트롤러에서 2장에 맞게 나누어주면 되는 것이 아닌가 하는 생각에 생성 조건을 삭제하였습니다!"*. *YAGNI + 책임 분배 자가 인식*.
- **블랙잭 룰 정확성 자가 학습** — 말리빈 진단 *"딜러가 블랙잭일 때, 플레이어가 블랙잭이 아니면 모든 플레이어는 패배합니다. 블랙잭의 도메인에 대해 더 공부해서 반영해보세요"* + 학생 자가 반영 + *"총점이 21점을 넘으면 어떤 상태인지, 등등의 게임 용어들이 있었지요"* 후속 반영. PR #148·#108·#125·#114·#131·#136·#137·#151 와 같은 결.
- **Card private constructor + 캐싱 + all() 활용 자가 반영** — 학생 자가 답변: *"코멘트 해주신 부분을 생각해보니 외부에서 올바른 값만 넣어주면 인스턴스를 생성할 수 있었고, 생성자를 private으로 바꿔서 Card 객체 이외에서는 인스턴스를 생성하지 못하게 막았고 `all()`을 통해 캐싱된 카드들을 가져다 쓸 수 있도록 변경하였습니다!"*. PR #108·#133·#136·#137·#146·#74 와 같은 결.
- **`Effective Kotlin Item 4/14` 학생 자가 학습** — 말리빈 권고 *"Effective Kotlin 서적의 챕터를 참고해서 스스로 생각해본 것과 비교해보아요. Item 4 : Inferred Type으로 리턴하지 말라. Item 14 : 변수 타입이 명확하지 않은 경우 확실하게 지정하라"* — PR #108 (시아) 와 같은 결.
- **`.sum { }` + `.any { }` Kotlin 확장 함수 자가 학습** — 학생 자가 인용: *"확장함수를 사용하니까 가독성도 좋고 코드도 짧아지네요 :) 더 공부해보도록 하겠습니당🙃"*.
- **Test Fixture 자가 학습 + 한글 명명 결정** — *PR #67 (s6m1n) 의 `BeforeEach` setUp 한글 명명* 과 같은 결.

## 못한 점 (설계 관점)

### 1. `CardBunch.size` 로 중복 카드 비교 — `contains` Kotlin 어휘 누락

말리빈: *"중복 카드를 사이즈로 비교하는 게 매우 어색합니다. contains를 활용해보세요."*

→ **`set.size != list.size` 우회 vs `list.contains(card)` 명시**. *Kotlin 어휘 = contains*. PR #67·#88 와 같은 결.

### 2. `addCard` 가 Boolean 반환 = C 스타일

말리빈: *"addCard를 했는데 반환은 Boolean이네요 :) C 스타일의 코드처럼 느껴졌습니다. 다른 개발자들은 해당 Boolean 값이 어떤 의미를 가지고 있을 지 한 번에 알아볼 수 있을까요?"*

학생: *"Boolean값이 어떤 의미를 가지고 있을지 한 번에 알아볼 수 있도록 함수명을 `isSuccessAddCardToPlayer`으로 변경하였습니다!"*

→ **Boolean 반환 = is/has 접두사 + 의미 명시**. PR #124·#125·#114·#41 와 같은 결.

### 3. `CardNumber 출력 메서드` View 의존

말리빈: *"해당 메서드는 View에서만 사용하네요 :) 도메인과 뷰의 로직을 분리해주세요."*

→ **enum 출력 메서드 = View 책임 누수**. PR #124·#28·#78·#112·#125·#128·#135·#137·#75 와 같은 결.

### 4. `overCondition` 함수명 모호

말리빈: *"overCondition이란 말은 어떤 의미를 가지고있을까요? 다른 개발자들도 한 번에 알아볼 수 있을까요?"*

학생: *"조금 더 직관적으로 알아볼 수 있게 `canGetCard`로 바꾸었는데 괜찮은 것인지 잘 모르겠네요,, 함수의 네이밍이 정말 어려운 것 같습니다..😢"*

→ **`overCondition` = 모호 vs `canGetCard` = 직관**. *`canDraw()` / `canHit()` Kotlin 어휘*. PR #75·#151·#41 와 같은 결.

### 5. `Player.calculateWinning` 사이드 이펙트 = Player 가변 변경

말리빈: *"이 역할은 정말 이 객체의 역할일 지 고민해보세요 :) 또, 사이드이팩트를 발생시키는 메서드의 작성은 최대한 지양해보세요."*

학생: *"딜러와 플레이어의 승패를 계산하고 저장하는 `Referee`클래스를 만들어 해당 로직들을 이동했습니다! 그리고 이 메서드에서 발생시키는 사이드 이펙트는 Player 가 갖고 있는 승패에 대한 정보를 바꾸는 것을 의미하는 것으로 이해했는데 맞을까요?"*

말리빈: *"네 :) 사이드 이팩트에 대한 정의는 [이 링크](https://en.wikipedia.org/wiki/Side_effect_(computer_science))를 참고해보세요!"*

→ **사이드 이펙트 = 함수가 외부 상태 변경 + 반환값 외 효과**. *순수 함수 (입력 → 출력)* 가 *예측 가능성*. *학생 자가 학습 모범*. PR #85·#137·#148 와 같은 결.

### 6. `CardDeck` 랜덤 위치 학생 자가 토론

말리빈 진단: *"shape와 card를 랜덤으로 생성할 필요가 있을까요? 해당 객체를 랜덤으로 생성했기에 필요하지 않은 로직들이 생겨나진 않았을까요? 정말로 랜덤이 필요한 곳은 어디일까요?"*

학생 자가 답변 깊음 (700+자): *"이 부분은 열심히 고민해봤는데 마땅한 방법이 떠오르지 않습니다..😢 ... 나름의 고민의 결과로 CardDeck 자체가 아래 프로퍼티를 갖게 하는 방법을 생각했습니다. ... 하지만 이렇게 구현하게 되면 랜덤을 사용하기 때문에 단위 테스트가 불가능하다고 생각됩니다. 접근 방법이 잘못된 것일까요?"*

말리빈 답변: *"지금은 카드를 뽑을 때 새로운 카드를 생성하는 형태이지요! 여러 벌의 카드를 사용할 때에 대해 생각해보면 좋겠어요 :) 또, 덱의 52장의 카드가 모두 떨어지면 이후 어떻게 해야할 지도 생각해보면 좋겠어요 🙂. 위 모두, 모양과 숫자가 동일한 카드를 재사용할 수 있다는 특징이 있지요. 한 번만 만들어두고 여러 번 재사용할 수 있게 만들어보셨으면 좋겠어요 :). 로또 번호도 45개를 만들어두고 재사용했었지요! **현실 세계에서 카드를 뽑으면, 카드의 요소가 랜덤 처럼 보이지만, 사실은 뽑는 카드의 순서가 랜덤이지요** 이런 생각으로 정말로 랜덤이 필요한 곳은 어디인지에 대해 질문을 드렸어요"*

→ **랜덤 = 카드 자체 X, 뽑는 순서가 랜덤**. *카드 52장 고정 + 뽑는 순서가 랜덤* 메타포. *학생 자가 학습 + 자가 적용 (Card.all() 캐싱 + CardDeck.drawCard 랜덤) 모범*. **현실 메타포 핵심 인용**. PR #146·#108·#133·#136 와 같은 결.

### 7. `CardDeck.drawCard 52장 모두 사용 후` 처리 부재

학생 자가 인용: *"또 말씀해주신 여러 벌의 카드를 사용할 때는 `Card`클래스에 있는 값을 여러번 가져오도록 확장할 수 있고, 52장의 카드가 모두 떨어진 경우 역시 다시 카드를 가져올 수 있도록 확장할 수 있을 것 같다고 생각이 들었습니다. (현재는 `IllegalArgumentException`를 반환하도록 만들어둔 상태입니다)"*

→ **카드 부족 시나리오 = `IllegalArgumentException` vs 무한 게임 메타포 트레이드오프**. PR #108·#133·#136·#41·#53·#74 와 같은 결.

### 8. `Referee` 사이드 이펙트 → 삭제 + 딜러 통합

말리빈: *"사이드 이팩트를 발생시키는 것 보다, 게임 결과를 반환하게 만들어보는 것은 어떨까요?"* + 후속: *"디미터의 법칙을 참고해서, 딜러와 플레이어의 점수를 가져오는 것을 변경해보는 것은 어떨까요?"*

학생 자가 반영: *"딜러와 플레이어의 점수를 가져오기 위해 객체에 메시지를 넘기는 방법으로 수정했습니다. 또, 사이드 이펙트를 발생시키지 않기 위해 Referee 클래스를 삭제하고 승자를 판단하는 로직을 다시 딜러에게 주었습니다!"*

→ **Referee 사이드 이펙트 + Demeter 위반 → Dealer 통합**. *현실 메타포 (Referee 분리) 시도 후 트레이드오프 후퇴 자가 학습*. PR #74·#58·#147 와 같은 결.

### 9. `Card 의 init require` = 의미 없는 검증 (자가 인식)

말리빈: *"이 로직은 정말로 의미 있는 검증일까요?"*

학생: *"캐싱된 카드를 가져오기 때문에 필요 없을 것이라고 생각했었는데 혹시라도 다른 값을 넣어 Card 객체를 만들지 못하도록 작성했었는데 의미 없는 코드인 것 같아 삭제했습니다!"*

→ **불필요한 방어 = 데드 코드**. *외부 호출 흐름 검증* 후 *불필요 시 삭제*. PR #88·#103·#147 와 같은 결.

### 10. `ACE 보너스 체크 1번만` 자가 학습

말리빈: *"ACE가 4장 있더라도, ACE의 점수 체크는 1번만 하면 됩니다. (카드 목록에 ACE의 유무 검사) 애초에 ACE가 2장이면, 22점이 아닌, 12점이기 때문이에요. 물론 2점으로도 사용할 수 있겠지요. 그 점을 고려해서 로직을 작성해볼까요? 😀"*

학생: *"ACE의 값은 1로 고정하여 계산하도록 수정했습니다! 그리고 ACE를 가지고 있다면 10을 더해줘도 21을 넘지 않는지 확인하고 넘지 않으면 10을 더해주도록 수정했습니다!"*

→ **Ace 보너스 +10 단일 적용 (PR #128·#114·#125·#108·#131·#132·#41·#49 와 같은 결의 정공 패턴)**.

### 11. `Dealer.canGetCard <= 15` vs `>= 17` 불일치 (콘솔 출력 vs 코드)

말리빈: *"딜러의 점수가 15이하 일 때만 카드를 받을 수 있네요 :) 콘솔엔 17이상 이라는 출력을 하고 있는데, 코드에서 사용하는 상수가 다르면 이후 디버깅할 때 어려울 것이 예상됩니다"*

학생: *"17로 수정했습니다 :)"*

→ **상수 vs 출력 메시지 일치성**. *디버깅 비용*. PR #131·#137 와 같은 결.

### 12. `Consequence (승/패/무) 문자열` = View 의존

말리빈: *"승 패 무 라는 문자열들은 도메인 영역 일지 고민해볼까요? 다른 비슷한 enum class가 또 있는 지 찾아볼까요?"*

학생 자가 답변: *"CardNumber는 \"총 합계계산\" 이라는 도메인 로직에서 사용하기 때문에 필요하지만 Consequence, Shape 은 단순히 출력만을 위해 값을 가지고 있던 것 같습니다. 생각하지 못한 부분을 짚어주셔서 감사합니다🙃"*

→ **enum UI value 안티패턴 (PR #75·#124·#28·#78·#112·#125·#128·#135·#137 와 같은 결)**.

### 13. `Dealer 의 if-else 다중 분기` → when + 게임 용어

말리빈: *"총점이 21점을 넘으면 어떤 상태인지, 등등의 게임 용어들이 있었지요 :) 해당 용어를 도메인 로직에 녹여내보는 것은 어떨까요? 세 번째 줄의 조건은 정말로 `블랙잭` 의 조건이 맞을까요? 또, if문이 많을 때는 when절을 활용해보세요"*

→ **`Bust` / `Blackjack` 도메인 어휘 활용 + when 패턴 매칭**. PR #137·#148·#136 와 같은 결.

### 14. `askGetCard + isSuccessAddCardToPlayer` 메서드 분리 — 학생 자가 학습

학생 자가 인용: *"이 부분에 대해서 많이 고민해봤는데,, 카드를 추가할 수 있는지 여부는 위의 함수인 `askGetCard`에서 while문 내부에서 확인을 하고, `isSuccessAddCardToPlayer` 함수는 플레이어에게 카드를 받을지 물어보고 받는다면 카드를 주가하는, 그리고 카드를 추가하는데 성공하면 true를, 실패했다면 false를 반환하도록 하는 일을 하는데요, 이 전체 과정이 하나의 일이라고 볼 수는 없는 것인지 궁금합니다..🙃"*

말리빈 답변: *"음! 말씀 대로 추가할 수 있는 지에 대한 여부는 player.canGetCard()로 묻고 있네요. 이 부분은 제가 잘못 말씀드렸어요 😥. 저는 플레이어가 카드를 받았는데, 그것에 대한 성공을 꼭 Boolean으로써 반환 해야 하는 지 의문이 들어요. 사실 InputView.getDecision(player)의 결과를 반환하는 것과 크게 다르지 않은 로직이라 생각합니다. 또, askGetCard 메서드를 봤을 때 수행하는 로직을 예측하기가 어렵다는 느낌을 많이 받았어요. **플레이어가 카드를 받을 수 있을 때 까지 반복문을 돌리고, 플레이어가 카드를 추가하는 데 성공했는데!... 갑자기 연재 종료된 웹툰이 된 느낌이랄까요?** 🤔"*

→ **메서드 의도 모호 = 연재 종료된 웹툰 비유**. *호출자가 다음 동작 예측 불가 = 책임 분배 실패 신호*. 학생 + 말리빈 양방향 토론 모범.

### 15. `RefereeTest` 명세 추상화 부족 — 자가 학습

말리빈: *"합계 수치만 가지고는 게임의 룰을 정확하게 알기는 어려워보여요. `플레이어보다 딜러가 점수가 높으면 딜러가 승리한다` 같은 이름은 어떨까요?"*

→ **테스트 함수명 = 룰 표현**. PR #137·#90·#136·#88 와 같은 결.

## 리뷰어 의견 요지

- **Boolean 반환 함수명 = is/has + 의미 명시** ("C 스타일 코드 느낌").
- **카드 = 52개 고정 + 뽑는 순서가 랜덤** (현실 메타포 핵심).
- **사이드 이펙트 = 외부 상태 변경 = 예측 불가 = 지양**.
- **Set vs List = 도메인 변경 (여러 벌) 대비**.
- **`Card.init require` = 외부 호출 검증 후 불필요 시 삭제**.
- **`overCondition` 모호 → `canGetCard` 직관**.
- **`Consequence` UI value = 도메인 누수**.
- **블랙잭 도메인 어휘 + when 활용**.
- **메서드 의도 명시 = "연재 종료된 웹툰 X" 비유**.
- **테스트 = 룰 표현 + 추상화**.
- **Card private constructor + 캐싱 + all() 활용 정공**.
- **ACE 보너스 = 카드 목록 ACE 유무 검사 + 단일 +10**.
- **상수 vs 출력 메시지 일치성 (디버깅 비용)**.

## 대표 인용

**말리빈 (현실 메타포 = 카드 자체 X, 뽑는 순서가 랜덤, CardDeck.kt):**
> 현실 세계에서 카드를 뽑으면, 카드의 요소가 랜덤 처럼 보이지만, 사실은 뽑는 카드의 순서가 랜덤이지요
> 이런 생각으로 정말로 랜덤이 필요한 곳은 어디인지에 대해 질문을 드렸어요 🙂
> 크롱은 어떻게 생각하시나요?

**말리빈 (메서드 의도 = 연재 종료된 웹툰 비유, Controller.kt:52):**
> 또, askGetCard 메서드를 봤을 때 수행하는 로직을 예측하기가 어렵다는 느낌을 많이 받았어요.
> 플레이어가 카드를 받을 수 있을 때 까지 반복문을 돌리고, 플레이어가 카드를 추가하는 데 성공했는데!... 갑자기 연재 종료된 웹툰이 된 느낌이랄까요? 🤔

**말리빈 (사이드 이펙트 정의 + 위키 링크, Player.kt):**
> 사이드 이팩트에 대한 정의는 [이 링크](https://en.wikipedia.org/wiki/Side_effect_(computer_science))를 참고해보세요!

**말리빈 (CardNumber ACE 비교 = is ACE, CardNumber.kt):**
> `점수를 계산하는 부분에서 상황에 맞게 처리하자`는 의견은 동의해요 :)
> 하지만, CardNumber 내부 값이 null인지 아닌지 비교하기보다는, CardNumber 자체가 ACE인지 비교를 하면 된다고 생각해요 🙂

**학생 자가 학습 (Card 52개 캐싱 + 뽑는 순서 랜덤, CardDeck.kt):**
> 이번 리뷰를 받고 `Card`클래스에 미리 52개의 카드 인스턴스를 만들어두고 재사용 할 수 있도록 수정했습니다. 이렇게 되면 기존에 있던 모양과 숫자를 랜덤으로 생성하는 클래스들은 필요 없어진다고 생각이 들어 삭제하였고, `CardDeck` 클래스에서 카드를 뽑을 때 랜덤으로 동작하도록 수정했습니다.

**학생 자가 학습 (사이드 이펙트 자가 정의, Player.kt):**
> 이 메서드에서 발생시키는 사이드 이펙트는 Player 가 갖고 있는 승패에 대한 정보를 바꾸는 것을 의미하는 것으로 이해했는데 맞을까요?

**학생 자가 학습 (Referee 자가 삭제 + Dealer 통합, Referee.kt):**
> 사이드 이펙트를 발생시키지 않기 위해 Referee 클래스를 삭제하고 승자를 판단하는 로직을 다시 딜러에게 주었습니다! 딜러는 플레이어들의 리스트를 받아서 점수를 비교하고 최종 결과를 `Map<String, Consequence>`형태로 돌려주도록 하였습니다!

**학생 자가 학습 (Consequence/Shape UI value 인식, Consequence.kt):**
> CardNumber는 "총 합계계산" 이라는 도메인 로직에서 사용하기 때문에 필요하지만 Consequence, Shape 은 단순히 출력만을 위해 값을 가지고 있던 것 같습니다. 생각하지 못한 부분을 짚어주셔서 감사합니다🙃

## 얻은 교훈

1. ***카드 = 52개 고정 + 뽑는 순서가 랜덤*** = *현실 메타포 핵심*. *Card.all() 캐싱 + CardDeck 랜덤 추출*. **MCP 룰 후보 — `random-on-card-vs-order`** (현실 메타포). PR #146·#108·#133·#136·#74 와 같은 결.

2. ***사이드 이펙트 = 외부 상태 변경 + 반환값 외 효과 = 예측 불가 = 지양***. *Player 가변 변경 → 결과 반환 패턴*. PR #85·#137·#148 와 같은 결.

3. ***Referee 도입 → 사이드 이펙트 + Demeter 위반 → Dealer 통합 후퇴***. *현실 메타포 vs 도메인 정합 자가 학습 모범*. PR #74·#58·#147 와 같은 결.

4. ***메서드 의도 = "연재 종료된 웹툰 X" 비유*** — 말리빈 명료 비유. *메서드 이름과 다른 책임 흡수 = 호출자 예측 실패*.

5. ***`addCard` Boolean 반환 = C 스타일 = 의미 모호*** → *is/has 접두사 + 명시적 이름*. PR #124·#125·#114·#41 와 같은 결.

6. ***`CardNumber.ACE` 비교 = `is ACE` 명시 > `value == null` 우회***. *enum 멤버 자체 비교*가 *Kotlin 어휘 정공*.

7. ***Set vs List = 도메인 변경 (여러 벌) 대비***. *Set = 중복 차단 + 1벌 가정*. *List = 중복 가능 + N벌 가능성*. PR #67·#88·#90 와 같은 결.

8. ***Card private constructor + all() 활용 = 외부 인스턴스 생성 차단***. PR #108·#133·#136·#137·#146·#74 와 같은 결.

9. ***Ace 보너스 = ACE 유무 검사 + 단일 +10 (Ace 4장도 +10 한 번만)***. PR #128·#114·#125·#108·#131·#132·#41·#49 와 같은 결.

10. ***상수 vs 출력 메시지 일치성 = 디버깅 비용***. *Dealer.16 코드 vs 17이상 메시지* 불일치 자가 반영. PR #131·#137 와 같은 결.

11. ***Consequence (승/패/무) UI value = 도메인 누수***. *학생 자가 인식 모범*. PR #75·#124·#28·#78 와 같은 결.

12. ***학생 krrong (이 PR 작성자) = PR #148·#151 의 reviewer 본인 = 메타 학습 케이스***. *학생 시절의 자가 학습이 reviewer 메타 질문 (역할 vs 상태 자가 학습, 반란군 비유) 으로 후속 누적*. *학습 곡선 메타 분석 가치*.

13. ***`overCondition` 모호 → `canGetCard` 직관***. *Kotlin Boolean 어휘 = `canDraw()` / `canHit()`*. PR #75·#151·#41 와 같은 결.

14. ***Effective Kotlin Item 4/14 자가 학습 (Inferred Type 반환 + 명확한 변수 타입)*** — PR #108·#137 와 같은 결.

15. ***학생 자가 답변 깊이 + 자가 토론 풍부 + reviewer 답변 흡수 = 학습 가시화***. *Referee 자가 토론 + 사이드 이펙트 자가 정의 + CardDeck 랜덤 위치 자가 학습 + ACE null 자가 토론*. PR #146·#148·#151·#137·#41·#74 와 같은 결.
