# PR #141 — [블랙잭 2단계] 조이 미션

- **Reviewee:** [@gahyunkim](https://github.com/gahyunkim)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/141
- **단계:** 2단계
- **리뷰어:** [@malibinYun](https://github.com/malibinYun) (말리빈) — 1단계 #118 동일 reviewer (2024→2025 다년 활동)
- **라인 코멘트:** 21 스레드 — *PR 본문 *MVC보다 객체 어떤 내용* 학생 자가 학습 의식 + 학생 자가 답변 + commit SHA + 자가 의견 명시 풍부 + tailrec 자가 적용 + 테스트 카드 스텁 (`SPADE_ACE`) + ScoreCalculator → Hand 응집 + GameResultDecider → GameResult.companion 이동 (PR #142 결) + 일급 컬렉션 (GameResults, ProfitResult, PlayersResult) + var decisionMaker 람다 분리 학생/reviewer 양방향*
- **양 단계 완성 reviewee** (1단계 #118 batch 21 분석 완료 + 2단계 #141 = batch 22 추가).

## 잘한 점 (설계 관점)

### 1. PR 본문 자가 학습 의식 + *MVC 보다 객체 어떤 내용*

PR 본문:

> *"말리빈! 2단계 미션도 열심히 구현해봤습니다! 아직 많이 부족한 부분도 많지만, 일단은 2단계 리뷰를 통해서 더 많은 리뷰를 받고싶어서 보냅니다! MVC보다는 각 객체에 어떤 내용이 들어가는 게 좋을 지 고민을 해보았습니다."*

→ **PR #118 reviewer 어휘 자가 적용** = *말리빈 1단계 *MVC 패턴 매몰 X* 어휘 → 학생 자가 학습 시점 정공*. **MCP `pattern-vs-object-naming-priority` 강화** 누적 2+ PR (#118·#141).

### 2. tailrec 학생 자가 적용 + while → 재귀 변경

reviewer (Thread 3):

> *"또, 어려운 내용일 수 있지만 재귀함수를 활용해서 게임을 실행시켜보셔도 좋겠어요. tailrec 키워드를 함께 알아보세요!*
>
> *어딘가에 while문으로 동작하고 있는 부분들이 보이는데, 그 조건대로 무언가를 행동하는건 그 객체의 역할일지, 게임 룰의 역할일지도 고민해보세요!"*

학생 자가 적용. → **tailrec 자가 학습** 누적 4+ PR (#138·#142·#140·#141) ⭐⭐.

### 3. 테스트 카드 스텁 (`SPADE_ACE = Card.of(...)`)

reviewer (Thread 8):

> *"테스트에 필요한 카드를 매번 만들어내는 건 매우 귀찮은 일이지요. 미리 카드 스텁을 테스트 패키지에 만들어 두고 재활용해보는건 어떨까요?*
> ```kotlin
> val SPADE_ACE = Card.of(...)
> ```"

학생 자가 적용 — *카드 스텁을 테스트 패키지에 구현 + 적용 commit*.

→ **테스트 fixture 재사용** = PR #21 (test-fixture-essential-info), PR #126·#130 (test-fixture-secondary-constructor) 결 합류. **MCP 룰 `test-fixture-stub-reuse` 후보** — *카드 스텁 = 테스트 패키지 재사용*.

### 4. ScoreCalculator → Hand 응집 + 객체 책임 분리

reviewer (Thread 12):

> *"ScoreCalculator 이 객체가 하는 일을 여기에 결합시키지 않고, 응집시켜보는 건 어떨까요?"*

학생 자가 적용:

> *"Hand 자체가 Player나 Dealer가 뽑은 카드를 가지고 있기 때문에 여기서 계산해주는 것이 오히려 더 좋을 것 같습니다! ScoreCalculator를 없애고 Hand 내에서 Ace가 포함되었을때까지의 값을 모두 계산할 수 있도록 구현해주었습니다."*

→ **god-object 회피 + Hand 책임 응집** = PR #117 (stateless-class-vs-companion-object) + PR #142 결의 *반대 시점 (외부 객체 vs 응집)*. **MCP 룰 `cohesion-vs-separation-tradeoff` 후보**.

### 5. GameResultDecider → GameResult.companion 이동 (PR #142 결)

reviewer (Thread 5):

> *"이 객체가 하는 일은 GameResult 에 옮겨볼 수도 있겠다는 생각이 드네요. 로또 결과를 계산했듯이요!"*

학생 자가 적용: `GameResultDecider` 삭제 + `GameResult.companion object` 함수 이동.

→ **enum 책임 강화 + 외부 객체 제거** = PR #142 (GameResult.cashOut) + PR #143 (ProfitRate.calculateProfitRate) 결 합류.

### 6. 일급 컬렉션 (GameResults, PlayersResult, ProfitResult)

reviewer (Thread 13):

> *"게임의 결과를 나타내는 일급 컬렉션을 만들고, 그 객체에 어울리는 역할을 부여해보는 건 어떨까요? 또, 그 게임의 결과를 담는 객체는 어느 객체에서 반환하면 좋을지 충분히 고민해보세요."*

학생 자가 적용:

> *"전체 플레이어와 딜러의 수익을 담은 ProfitResult 클래스, 플레이어들의 모든 수익을 담은 PlayersResult를 만들어서 해당하는 클래스를 이용할 수 있도록 구현해주었습니다!*
> *- PlayersResult: Map<Player, Money>를 만들어서 플레이어들의 수익을 담아주는 클래스*
> *- ProfitResult: 전체 플레이어들의 수익과 딜러의 수익을 담은 클래스 구현"*

→ **일급 컬렉션 + 책임 명확** = PR #142 (Scoreboard), PR #143 (ProfitInfo) 결 합류.

### 7. ACE 카드 존재 파악 Hand.isAceExist() 자가 적용

reviewer (Thread 2):

> *"Hand 객체의 값을 꺼내 비교하기보다, Hand에게 메시지를 던져 물어보도록 구성해보는 건 어떨까요?"*

학생 자가 적용:

> *"hand 객체 내에서 값을 확인해서 메시지를 던질 수 있도록 isAceExist()를 hand 객체 내에서 관리할 수 있도록 해주었습니다!"*

→ **Tell-Don't-Ask 정공** = PR #87·#129·#138 결.

### 8. 추가 칭찬 + 모든 21 스레드 학생 자가 답변 + commit SHA

## 못한 점 (설계 관점)

### 1. 카드 부족 시 예외 처리 + 랜덤 분리 (PR #118 1단계 결 진화)

reviewer (Thread 1):

> *"남아있는 카드가 1장일 때 2장을 뽑으면 어떤 일이 발생할까요? 또, 랜덤에 대한 테스트가 불가능한 구조가 되었어요. 어떻게하면 랜덤에 대한 요소를 분리해서 테스트 가능하도록 만들 수 있을까요?"*

학생 자가 적용: *TestCardGenerator 분리 + 카드 부족 예외 처리*.

→ **카드 부족 시 graceful 처리** = PR #118 (1단계 카지노 메타포 새 덱) 와 *반대 시점 (예외 처리)*. **MCP 룰 `card-shortage-handling-strategy` 강화** 누적 2+ PR (#118·#141).

### 2. var decisionMaker 람다 분리 — 학생/reviewer 양방향 (Thread 7·9)

reviewer (Thread 7): *"플레이어까지 람다로 전달할 필요는 없다고 생각해요. 여기에는 람다로 전달하지 않고 구성해보는 건 어떨까요?"*

학생 자가 적용 (`var decisionMaker`):

```kotlin
class Player {
    var decisionMaker: ...  // ← var + 외부 할당
}
```

reviewer 자가 후속 (Thread 9):

> *"다른 개발자들은 이 변수를 외부에서 꼭 할당해야한다는 사실을 바로 알 수 있을까요? (...) 플레이어에게 람다를 전달하고 있는 것은 달라지지 않았어요. 플레이어 객체 안에서 람다를 사용하지 않도록 구현해보시면 좋겠어요 :)"*

→ **var + 외부 할당 = 호출 순서 invariant 부재** = PR #139·#142·#118 (lateinit/var) 결 합류. **MCP 룰 `lateinit-var-call-order-invariant` 강화** 누적 11+ PR.

reviewer 후속 = *학생 자가 적용이 *본질적 변경 X (람다만 var 로 옮김)* 명시*. **MCP 룰 `superficial-refactoring-vs-essential-change` 후보** — *표면 리팩토링 vs 본질 변경*.

### 3. ProfitCalculator → 매핑 중복 제거 (Thread 10)

reviewer: *"playerResult.result.profitRate로 접근 하는 것과 어떤 차이가 있었나요?"*

학생: *"playerResult.result.profitRate로 바로 접근할 수 있었는데 불필요하게 중복된 매핑을 하고 있었던 것 같습니다!"*

→ **불필요한 추상화 자가 인지** = PR #117 (function-decomposition-creates-identity) 결 합류.

### 4. Money.toInt() 불필요 - 직접 amount 접근

reviewer (Thread 11): *"money 의 amount를 접근하지 않고 이 함수를 만든 이유는 무엇인가요?"*

학생 자가 적용: *toInt() 제거*.

→ **불필요한 함수 = 직접 접근** 학생 자가 학습.

### 5. Players 객체 메시지 전달 (Thread 3)

reviewer: *"foreach를 써볼 수도 있지만, players 객체에게 모든 플레이어 플레이하라는 메시지를 던져보는 건 어떨까요?"*

→ **Tell-Don't-Ask + 일급 컬렉션 자가 행동** = PR #129·#138·#142·#140 결 합류.

### 6. 게임 룰 위치 자가 의문 (Thread 3 reviewer 메타 어휘)

reviewer:

> *"어딘가에 while문으로 동작하고 있는 부분들이 보이는데, 그 조건대로 무언가를 행동하는건 그 객체의 역할일지, 게임 룰의 역할일지도 고민해보세요!"*

→ **객체 역할 vs 게임 룰 위치 어휘** = PR #126·#129·#143 (business-logic-vs-controller-logic) 결 합류.

## 대표 인용

> [gahyunkim PR 본문]
> *"MVC보다는 각 객체에 어떤 내용이 들어가는 게 좋을 지 고민을 해보았습니다."*

→ **PR #118 reviewer 어휘 학생 자가 적용** — *말리빈 *MVC 매몰 X* → 학생 자가 학습*.

> [malibinYun Thread 9]
> *"플레이어에게 람다를 전달하고 있는 것은 달라지지 않았어요. 플레이어 객체 안에서 람다를 사용하지 않도록 구현해보시면 좋겠어요 :)"*

→ **표면 리팩토링 vs 본질 변경 어휘**.

> [malibinYun Thread 8]
> *"테스트에 필요한 카드를 매번 만들어내는 건 매우 귀찮은 일이지요. 미리 카드 스텁을 테스트 패키지에 만들어 두고 재활용해보는건 어떨까요?"*

→ **테스트 카드 스텁 재사용 어휘**.

## 얻은 교훈 (보편)

1. **MVC 매몰 X 학생 자가 학습 (PR #118 reviewer 어휘 적용)** — **MCP 룰 `pattern-vs-object-naming-priority` 강화** 누적 2+ PR.

2. **tailrec 자가 학습 + while → 재귀** — 누적 4+ PR.

3. **테스트 카드 스텁 재사용** — **MCP 룰 `test-fixture-stub-reuse` 후보**.

4. **god-object 회피 + Hand 응집 (ScoreCalculator 제거)** — **MCP 룰 `cohesion-vs-separation-tradeoff` 후보**.

5. **외부 객체 제거 + enum 책임 강화 (GameResultDecider → GameResult.companion)** = PR #142·#143 결 합류.

6. **일급 컬렉션 (PlayersResult, ProfitResult, GameResults)** = PR #142·#143 결.

7. **`var decisionMaker` 표면 리팩토링 자가 적용 + reviewer 자가 정정 (본질 변경 X)** — **MCP 룰 `superficial-refactoring-vs-essential-change` 후보**.

8. **객체 역할 vs 게임 룰 위치** — PR #126·#129·#143 결.

9. **불필요한 추상화 자가 인지** (PR #117 결 합류).

10. **모든 21 스레드 학생 자가 답변 + commit SHA** — 2025 코호트 정점 유지.

## MCP 룰 후보 신규 + 강화 (PR #141)

| 룰 | 신규 / 강화 | 누적 PR |
|---|---|---|
| `pattern-vs-object-naming-priority` | 강화 | 2+ (#118·#141) |
| `tailrec-optimization-applicability` | 강화 | 4+ |
| `test-fixture-stub-reuse` | 신규 | 1 (#141) |
| `cohesion-vs-separation-tradeoff` | 신규 | 1 (#141) |
| `superficial-refactoring-vs-essential-change` | 신규 | 1 (#141) |
| `card-shortage-handling-strategy` | 강화 | 2+ (#118·#141) |
| `lateinit-var-call-order-invariant` | 강화 | 11+ |
| `result-aggregation-locator` | 강화 | 4+ |

## 비교 (PR #118 → #141)

- **1단계 #118 18 thread → 2단계 #141 21 thread**.
- **1단계 #118 *MVC 매몰 X* 어휘 → 2단계 #141 PR 본문 학생 자가 *MVC 보다 객체 내용 고민***.
- **1단계 #118 Flyweight 패턴 자가 학습 → 2단계 #141 Hand 응집 + ScoreCalculator 제거**.
- **1단계 #118 GameResultDecider → 2단계 #141 GameResult.companion 이동**.
- **2단계 #141 var decisionMaker 표면 리팩토링 → reviewer 자가 정정 (본질 변경 X)**.

→ *진화 = 학생 자가 학습 시간성 + reviewer 어휘 자가 흡수 + 표면 vs 본질 차이 학습*.
