# PR #118 — [블랙잭 1단계] 조이 미션

- **Reviewee:** [@gahyunkim](https://github.com/gahyunkim)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/118
- **단계:** 1단계
- **리뷰어:** [@malibinYun](https://github.com/malibinYun) (말리빈) — 2025 코호트 신규 reviewer 등장 (말리빈 = 2024 PR 들 reviewer = krrong (PR #32) 의 reviewer 였음; 2024 → 2025 reviewer 활동 유지)
- **라인 코멘트:** 18 스레드 — *PR 본문 자가 한계 인정 + 시간 부족 + 페어 학습 + 모든 18 스레드 학생 자가 답변 + commit SHA + Ace 1 vs 11 기본 학생 자가 의견 vs reviewer 수학적 비교 어휘 (4*11-3*10 vs 4*1+10) + Flyweight 패턴 학생 자가 학습 + `cards.allCards.clear()` 코드 예시 시나리오 + 「객체지향의 사실과 오해」 학생 자가 인용 + MVC 패턴 매몰 vs 객체 이름 집중 reviewer 메타 어휘 + data class copy 비활성화 (Kotlin 2.0) 학생 자가 학습 + 카드 부족 시 새 덱 vs 게임 종료 (카지노 메타포)*
- **신규 reviewee** (2025년 코호트 — 모바일 안드로이드 7기, 누적 9 PR + #118 = **2025 코호트 10 PR 코퍼스 정점**).

## 잘한 점 (설계 관점)

### 1. PR 본문 자가 한계 인정 + *많이 혼내주세요* 어휘

PR 본문:

> *"안녕하세요 말리빈! 이번 블랙잭 미션을 리뷰 받게 된 조이입니다. 블랙잭 미션을 진행하다보니 시간이 부족해 좀 더 꼼꼼히 확인하지 못한 부분들이 많은 것 같습니다.. 리팩토링을 하면서 조금 더 좋은 코드를 만들기 위해 노력해보겠습니다! 많이 많이 혼내주세요!"*

→ **학생 자가 시간 부족 인정 + 적극 학습 어휘 (*많이 혼내주세요*)** = PR #130·#139·#143 (학생 자가 한계 인정) 결 합류.

### 2. 말리빈 (2024 reviewer) → 2025 reviewer 활동 유지 메타 케이스

말리빈 (`@malibinYun`) = 2024년 PR 들 (PR #32 krrong 등) 의 reviewer + 2025년 PR #118 의 reviewer.

→ **reviewer 의 다년 활동 = 학생/reviewer 진화 메타 케이스 변형** — 학생 → reviewer 진화 (krrong/hyemdooly) 와 *반대 시점 (reviewer → reviewer 다년 활동)*.

**MCP 룰 `multi-year-reviewer-activity` 후보** — *reviewer 의 코호트 간 활동 유지*.

### 3. 학생 「객체지향의 사실과 오해」 자가 인용 + *역할·책임·협력* 어휘

학생 자가 답변 (Thread 8):

> *"객체지향의 사실과 오해를 읽으면서, 객체를 단순히 현실의 사물을 모방하는 존재가 아니라 명확한 역할과 책임을 가진 독립적인 행위자로 바라보게 되었습니다. 현실에서는 말리빈이 해준 피드백처럼 카지노 딜러가 점수를 계산하는 모습을 볼 수 있지만, 소프트웨어 설계에서는 딜러에게 모든 계산과 승패 판정 책임을 부여하기보다는, 각 객체가 단일 책임을 갖도록 역할을 분리하는 것이 바람직하다고 생각했습니다."*

→ **학생 자가 「객체지향의 사실과 오해」 인용 + *역할·책임·협력* 어휘 자가 사용** = **MCP `oh-my-oop` 핵심 가치 정점**. 

#### 3.1. *현실 메타포 vs 객체 분리 트레이드오프*

학생 자가 입장 — *현실 메타포 (딜러 = 점수 계산) vs 단일 책임 (GameResultDecider 분리)*. PR #117·#129·#130·#143·#109·#113 결의 *현실 메타포 정합* 어휘 와 *학생 자가 *현실 메타포 비판***.

**MCP 룰 `metaphor-fidelity-vs-srp-tradeoff` 후보** — *현실 메타포 정합 vs 단일 책임 트레이드오프*.

### 4. Ace 1 vs 11 기본 + 수학적 비교 어휘 (말리빈)

학생 자가 의견 (Thread 2):

> *"저는 값을 계산하는 로직에서 ACE를 기본적으로 11로 판단하는 것이 더 옳다고 생각했습니다. 만약 여러 장의 카드를 받았을 때, 그 카드들 안에 ACE가 존재한다면 기본적으로 11점으로 계산하고, 총합이 21을 초과하는 경우에만 1점으로 변경하는 것이 블랙잭 규칙에 더 적절하다고 생각했습니다!"*

reviewer (수학적 비교 어휘):

> *"글쎄요, ACE 카드가 10점이 추가되는 경우가 있는 것이지, 11점에서 출발해서 10점씩 깎이는 것이 더 어색하다 생각해요. ACE 카드가 4장인 경우, `4 * 11 - 3 * 10` 로직을 작성하는 게 적절해보이시나요? 아니면, `4 * 1 + 10` 로직을 작성하는 게 적절해보이시나요?*
>
> *일반적으로 ACE는 어떤 게임에서든 1로 계산하기도 하니까요. 다각도로 볼 수 있는 시각을 연습해보세요."*

학생 자가 적용:

> *"ACE를 1로 계산하는 게 더 일반적이겠네요! 말리빈의 피드백대로 기본적으로는 1로 계산하고 특정 상황에 따라 10을 추가해주는 방식으로 재 구현해보았습니다!"*

→ **reviewer 수학적 비교 어휘 (4*11-3*10 vs 4*1+10)** = *추상화의 자연스러움 = 수학식의 단순성*. **MCP 핵심 가치 정점**.

PR #117 (수학적 풀이 외부글) 결 합류 + *수학적 직관성 = 모델 자연스러움* 어휘.

**MCP 룰 `mathematical-naturalness-as-model-criterion` 후보** — *수학식 단순성 = 모델 자연스러움 지표*.

### 5. Flyweight 패턴 학생 자가 학습

학생 자가 답변 (Thread 4):

> *"52장의 카드를 모두 생성한 후, 이미 생성된 동일한 카드 객체가 존재한다면 해당 객체를 재사용하는 방식은 메모리 측면에서 효율적이라고 생각했습니다. (...) 블랙잭의 경우 가능한 카드 조합이 52가지로 제한되어 있으므로, Flyweight 패턴을 적용해 52장의 카드에 대해 캐싱을 구현한다면, 이미 생성된 카드 객체를 재사용하여 메모리 사용량을 크게 줄일 수 있을 것 같았습니다."*

→ **GoF 디자인 패턴 (Flyweight) 학생 자가 학습 + 자가 적용** = PR #138·#142 (companion lazy 캐싱) 결의 *학생 자가 패턴 어휘 사용*. **MCP 룰 `flyweight-pattern-card-caching` 후보**.

### 6. `Card private constructor + 팩토리 메서드` 자가 적용 — 중복 인스턴스 회피

reviewer (Thread 11): *"여전히 동일한 값을 가지는 중복 인스턴스를 생성할 수 있겠어요."*

학생 자가 적용:

```kotlin
class Card private constructor(val cardRank: CardRank, val shape: Shape)
```

→ **private constructor + 팩토리 메서드** = PR #35 결의 *private constructor + companion factory*. 누적 2+ PR.

### 7. data class copy 비활성화 (Kotlin 2.0) — 학생 자가 학습

reviewer (Thread 11): *"data class 도 이제는 copy의 영향을 받지 않도록 구성해볼 수 있지요."* + 외부글 (kotlinlang.org/docs/whatsnew2020.html).

학생 자가 답변: *"@Deprecated(DeprecationLevel.ERROR)를 이용하면 copy()를 비활성화 하는 것이 가능하다고 하네요!"*

→ **Kotlin 2.0 + 신규 어휘 자가 학습** = *Kotlin 어휘 깊은 자가 학습*. **MCP 룰 `kotlin-version-feature-learning` 후보**.

### 8. 추가 칭찬 + 모든 18 스레드 학생 자가 답변 + commit SHA

## 못한 점 (설계 관점)

### 1. `cards.allCards.clear()` 코드 예시 시나리오 — reviewer 가 *문제 시나리오*

reviewer (Thread 5):

> *"아래 코드를 실행시킨다면 어떤 문제가 생길 수 있을까요?*
> ```kotlin
> val cards = Cards(listOf(.....))
> cards.allCards.clear()
> ```"

학생 자가 답변:

> *"위의 코드를 실행하게 되면, allCards는 MutableList로 구현되어있기 때문에 외부에서 조작이 가능하게 됩니다. clear()를 통해서 모든 카드를 삭제하게 될 수 있을 것 같습니다."*

→ **reviewer 가 *문제 시나리오 코드 예시* + 학생 자가 답변 모범** = *Tell-Don't-Ask + mutable list 외부 노출 위험 자가 학습*. PR #21·#117·#138·#143 결 강화.

**MCP 룰 `reviewer-scenario-code-example` 후보** — *reviewer 가 *문제 발생 시나리오 코드 예시* = 학생 자가 답변 유도*.

### 2. *MVC 패턴 매몰되지 않기* reviewer 메타 어휘

reviewer (Thread 10·13):

> *"MVC 패턴이라 Service 라는 이름을 붙이신거라면, 거기에 벗어나서 객체의 이름을 짓는 데 집중해보세요."*

> *"이전에 언급했던 것 처럼, MVC 패턴에 매몰되지 않고 생각해보시면 좋겠어요 :)"*

학생: *"MVC 패턴에 매몰되지 않게 일단은 각 클래스들에 맞는 책임과 역할을 가지고 있는지를 판단하면서 구현해보겠습니다!"*

→ **패턴 매몰 vs 객체 이름 집중** = *MCP `oh-my-oop` 핵심 가치 (패턴 적용 X = 책임 우선)*. PR #126 (KwonDae *MVC 절대 원칙*) 결의 *반대 시점 (MVC 매몰 회피)* 트레이드오프.

**MCP 룰 `pattern-applied-without-need` 강화** 누적 7+ PR (#80·#145·#100·#76·#39·#31·#118). **MCP 룰 `pattern-vs-object-naming-priority` 후보** — *패턴 (Service, Manager) vs 객체 이름 (역할 명시) 우선순위*.

### 3. CardsGenerator 테스트 가능성 + Shuffler interface

reviewer (Thread 6): *"이 카드 생성기는 랜덤의 요소때문에 테스트가 불가능한 구조이네요."*

학생 자가 적용: `Shuffler interface` 분리 + `TestShuffler` (test util).

→ **DIP 정공 + production vs test 분리** = PR #138·#143·#113·#109 결의 *interface-for-test-substitution* 누적 7+ PR.

### 4. `turn`/`isHit` 이름 의미 모호

reviewer (Thread 7): *"turn 이라는 메서드명은 다른 개발자들이 보았을 때 어떤 역할을 할지 바로 유추해낼 수 있을까요?"*

학생 자가 적용: `performTurn` + `decideToHit`.

→ **이름 의미 정확성** = PR #36·#117·#129·#130·#139·#109·#113 결 합류 누적 7+ PR.

### 5. GameResultDecider 외부 객체 vs Dealer 내부 위치

reviewer (Thread 8) 후속:

> *"객체 분리하는 것은 공감하지만, 매번 결과를 알아야할 때마다 이 객체를 생성해야겠네요. 생성이 꼭 필요한 것일지 고민해보시면 좋겠어요. 또, 객체 분리했다 해도, 여전히 딜러 내부에 위치하는 건 어떨지 고민해보세요. 다른 개발자들이 점수 계산 로직을 찾을 때 어디부터 찾아볼지 상상해보면서요!"*

→ **동료 개발자 시점 어휘 (maintainer perspective locator)** = PR #87·#76·#80·#107·#82 결 + **객체 생성 비용 + 객체 위치 (Dealer 내부 vs 외부 객체) 트레이드오프**. PR #129·#138·#142 (외부 장치 X = 참가자 자가) 결의 *반대 시점*.

### 6. Shape/Rank enum 텍스트 정보 = 도메인 vs 뷰

reviewer (Thread 9): *"이 블랙잭 게임 도메인을 똑 떼어다가 안드로이드에 이식한다면, 이 텍스트 값은 유효한가요?"*

학생 자가 답변:

> *"안드로이드에서 텍스트 정보는 주로 리소스 파일(strings.xml)에 저장하여 다국어 지원 및 유지보수를 용이하게 하는 것으로 알고 있습니다."*

학생 자가 적용: `Card.displayName()` 확장 함수.

→ **다국어/이식성 의식 + 확장 함수 정공** = PR #117·#126·#143 결 합류. **MCP 룰 `model-view-dependency-direction` 강화** 누적 12+ PR ⭐⭐⭐.

### 7. `Cards.drawCards()` 역할 추가 — 카드 뭉치 자가 행동

reviewer (Thread 12):

> *"이 객체는 값을 갖고있기만 하고 맡고 있는 역할이 없네요. 여기에 할 수 있는 역할을 어디에서 하고 있는 것은 아닐지, 혹은 다른 곳에서 적절히 하고 있기에 이 객체가 필요 없는 것은 아닐지 고민해보세요."*

학생 자가 적용: `Cards.drawCards()` 메서드 추가.

→ **객체 책임 X = 객체 불필요 vs 책임 추가**. PR #142 (Game/Scoreboard 분리) 결의 *책임 응집*.

### 8. 패키지 구조 model/view/service → model/view/controller

reviewer (Thread 13): *"이 질문 이전에 패키지 구조를 model, view, service 로 구성하는 것은 적절한 것인지부터 고민해보시면 좋겠네요 :)"*

학생 자가 적용 → controller (MVC).

→ **패키지 구조 의식**. PR #142 (package-by-domain) 결 합류.

### 9. `dealer.dealerPlay` 함수 분리 — CQS 위반 자가 정정

reviewer (Thread 14): *"이친구를 호출하면 딜러가 몇번 카드를 뽑았을지 횟수를 가져올 것 같이 생겼는데, 안에서는 뽑는 일까지 해버리고있네요."*

학생 자가 적용 → `dealerPlay()` + `getDrawCount()` 분리.

→ **CQS 정공** = PR #117·#139·#130 (CQS 위반) 결의 *학생 자가 정정* 누적 3+ PR.

### 10. `determineGameResult` 명명 정확성

reviewer (Thread 15): *"이 메서드는 정말 게임의 결과를 줄 거라 생각했는데, 뭔가 결과가 아니라 다른 걸 주고 있네요."*

학생 자가 적용 → `determineGameResult()` 명명.

→ **함수 이름 vs 반환 의미 일치** = PR #36·#117·#130 결 합류.

### 11. 템플릿 패턴 vs 단순 List 섞기

reviewer (Thread 16): *"템플릿을 쓸 필요까지 있었을지 고민해보시면 좋겠네요!"*

→ **`pattern-applied-without-need`** 누적 7+ PR 강화. 단순 List 섞기 = 템플릿 패턴 무의미.

### 12. 카드 부족 시 새 덱 vs 게임 종료 (카지노 메타포)

reviewer (Thread 18):

> *"더이상 뽑을 카드가 없다면 에러가 발생하겠어요. 카지노에서 나눠줄 카드가 없다면, 바로 게임장 문 닫고 사람들 내쫓는 것은 자연스러운 일일까요?"*

학생 자가 답변 + 적용:

> *"더이상 뽑을 카드가 없다면, 게임을 끝내는 것보다 새로운 카드덱을 섞어서 다시 게임을 시작하는게 더좋을 것 같다는 생각이 듭니다."*

→ **카지노 메타포 + 우아한 처리** = PR #129·#138 (블랙잭 8 덱 도메인 룰) 결 합류 + *graceful shutdown vs auto-refill* 트레이드오프.

**MCP 룰 `card-shortage-handling-strategy` 후보** — *카드 부족 시 처리 (에러 vs 새 덱 vs 종료) 트레이드오프*.

### 13. Ace 11 기본 시 4 ace 처리 자가 의문 + 학생 자가 변경

§4 인용. 학생 자가 *11 기본 → 1 기본 + 10 추가* 자가 변경 + while 적용:

```kotlin
fun calculateTotalCardScore(): Int {
    var score = hand.getScore()
    var aceCount = countAce()
    while (score + ACE_PLUS_VALUE <= GameResultDecider.BLACKJACK_SCORE && countAce() > DEFAULT_ZERO) {
        score += ACE_PLUS_VALUE
        aceCount--
    }
    return score
}
```

→ **Ace 1 기본 + while + 10 추가** = PR #20·#64·#110·#28·#125·#147·#82 의 *Ace 11 기본 + while 차감* 안티패턴과 *반대 시점 정공*.

다만 *while 사용* — PR #117 (수학적 풀이) / PR #138·#142 (재귀 + tailrec) 와 *반대 시점*. **MCP 룰 `ace-bonus-while-vs-math-vs-recursion` 후보** — *Ace 처리 3 방식 트레이드오프*.

### 14. GameResult enum 도메인 vs 메시지 함수 자가 적용

reviewer (Thread 17): *"게임 결과를 enum으로 나타내보는 건 어떨까요?"*

학생 자가 적용:

```kotlin
enum class GameResult { WIN, LOSE, DRAW }
```

→ **enum 변환 정공** = PR #117·#126·#130·#113·#143 결 합류.

### 15. Hand 클래스 자가 추가 — 카드 뭉치 vs 손 카드 분리

학생 자가 답변 (Thread 2):

> *"Hand라는 클래스를 생성하면서, 결국 ScoreCalculator도 플레이어나 딜러가 손에 들고 있는 카드들의 수를 바탕으로 점수를 계산하기 때문에 생성자를 hand로 변경해주었습니다!"*

→ **Hand 분리 (god-object 회피)** = PR #117·#126·#129·#138·#143 결 합류.

### 16. CardDistributor 추가 — 카드 분배자 분리

학생 자가 적용 — *CardDistributor 객체 추가* (카드 분배 책임).

→ **분배자 객체 분리** = *PR #21 (CardPicker 가상 객체) 결의 *반대 시점***. CardDistributor 도 *가상 객체* 위험 (PR #21·#80·#145·#100 결).

**MCP 룰 `virtual-object-vs-real-domain` 강화** 누적 8+ PR ⭐⭐⭐.

### 17. 모든 18 스레드 학생 자가 답변 + commit SHA + 학생 자가 의견 명시 풍부

## 리뷰어 의견 요지

- **게임 규칙 = 도메인 vs 컨트롤러**.
- **Ace 1 기본 + 10 추가 (4 ace 수학적 비교)**.
- **CardRank.ACE enum 비교 (문자열 비교 X)**.
- **Card 중복 인스턴스 회피 (Flyweight + private constructor)**.
- **`cards.allCards.clear()` 코드 예시 시나리오**.
- **CardsGenerator 테스트 가능성 + Shuffler interface**.
- **`turn`/`isHit` 이름 의미 모호**.
- **GameResultDecider 외부 객체 vs Dealer 내부 위치 + 동료 개발자 시점**.
- **Shape/Rank enum 텍스트 = 도메인 vs 뷰 + 안드로이드 이식**.
- **MVC 패턴 매몰 vs 객체 이름 집중**.
- **data class copy 비활성화 (Kotlin 2.0) 외부글**.
- **Cards 역할 추가 vs 객체 불필요**.
- **패키지 구조 의식 (model/view/service)**.
- **`dealerPlay` 함수 분리 (CQS 위반)**.
- **`determineGameResult` 명명 정확성**.
- **템플릿 패턴 vs 단순 List 섞기 + Test 코드 위치**.
- **enum 변환 (GameResult)**.
- **카드 부족 = 카지노 메타포 (새 덱 vs 종료)**.

## 대표 인용

> [malibinYun Thread 2]
> *"ACE 카드가 4장인 경우, `4 * 11 - 3 * 10` 로직을 작성하는 게 적절해보이시나요? 아니면, `4 * 1 + 10` 로직을 작성하는 게 적절해보이시나요?*
>
> *일반적으로 ACE는 어떤 게임에서든 1로 계산하기도 하니까요. 다각도로 볼 수 있는 시각을 연습해보세요."*

→ **수학적 비교 어휘 + 일반화 어휘** — *모델 자연스러움 = 수학식 단순성*.

> [gahyunkim Thread 8]
> *"객체지향의 사실과 오해를 읽으면서, 객체를 단순히 현실의 사물을 모방하는 존재가 아니라 명확한 역할과 책임을 가진 독립적인 행위자로 바라보게 되었습니다."*

→ **학생 「객체지향의 사실과 오해」 자가 인용 + 역할·책임 어휘** — *MCP `oh-my-oop` 핵심 가치 정점*.

> [malibinYun Thread 10]
> *"MVC 패턴이라 Service 라는 이름을 붙이신거라면, 거기에 벗어나서 객체의 이름을 짓는 데 집중해보세요."*

→ **패턴 매몰 vs 객체 이름 집중** — *MCP `pattern-applied-without-need` 강화*.

> [malibinYun Thread 11]
> *"글쎄요~ data class 도 이제는 copy의 영향을 받지 않도록 구성해볼 수 있지요. 코틀린 언어의 버전 업데이트가 있을 떄 어떤 변화점이 생겼는지 관심을 가져보셔도 좋겠어요 :)"*

→ **Kotlin 버전 업데이트 학습 권유** — *Kotlin 어휘 깊이 권유*.

> [malibinYun Thread 18]
> *"더이상 뽑을 카드가 없다면 에러가 발생하겠어요. 카지노에서 나눠줄 카드가 없다면, 바로 게임장 문 닫고 사람들 내쫓는 것은 자연스러운 일일까요?"*

→ **카지노 메타포 + graceful shutdown 어휘**.

## 얻은 교훈 (보편)

1. **수학적 비교 어휘 (4*11-3*10 vs 4*1+10)** — *모델 자연스러움 = 수학식 단순성*. **MCP 룰 `mathematical-naturalness-as-model-criterion` 후보**.

2. **「객체지향의 사실과 오해」 학생 자가 인용** — *MCP `oh-my-oop` 핵심 가치 정점*.

3. **Flyweight 패턴 학생 자가 학습** — **MCP 룰 `flyweight-pattern-card-caching` 후보**.

4. **reviewer 문제 시나리오 코드 예시** — **MCP 룰 `reviewer-scenario-code-example` 후보**.

5. **MVC 패턴 매몰 vs 객체 이름 집중** — *PR #126 (MVC 원칙) 반대 시점*. **MCP 룰 `pattern-vs-object-naming-priority` 후보**.

6. **Kotlin 버전 업데이트 자가 학습 + data class copy 비활성화** — **MCP 룰 `kotlin-version-feature-learning` 후보**.

7. **카드 부족 카지노 메타포 (graceful vs 종료)** — **MCP 룰 `card-shortage-handling-strategy` 후보**.

8. **현실 메타포 vs SRP 트레이드오프** — **MCP 룰 `metaphor-fidelity-vs-srp-tradeoff` 후보**.

9. **reviewer 다년 활동 (말리빈 2024 → 2025)** — **MCP 룰 `multi-year-reviewer-activity` 후보**.

10. **Ace 처리 3 방식 (while vs math vs recursion)** — **MCP 룰 `ace-bonus-while-vs-math-vs-recursion` 후보**.

11. **CQS 위반 자가 정정** — *PR #117·#139 결의 *재발* → PR #118 *자가 정정**.

12. **함수 이름 vs 반환 의미 일치** — 누적 7+ PR.

13. **interface-for-test-substitution** — 누적 7+ PR.

14. **virtual-object-vs-real-domain (CardDistributor)** — 누적 8+ PR.

15. **모든 18 스레드 학생 자가 답변 + commit SHA + 자가 의견 명시** — *2025 코호트 10 PR 정점 유지*.

## 얻은 교훈 (도메인)

1. **Ace 1 기본 + while + 10 추가** — *Ace 1 기본 모델 정공* (PR #36 결).

2. **카드 부족 = 새 덱 추가 (graceful)** — *카지노 메타포 정합*.

3. **블랙잭 결과 enum (WIN/LOSE/DRAW)** — *기본 3 멤버*. PR #129·#126·#113 결.

4. **52 카드 캐싱 (Flyweight)** — *학생 자가 학습*.

## MCP 룰 후보 신규 + 강화 (PR #118)

| 룰 | 신규 / 강화 | 누적 PR |
|---|---|---|
| `mathematical-naturalness-as-model-criterion` | 신규 | 1 (#118) |
| `flyweight-pattern-card-caching` | 신규 | 1 (#118) |
| `reviewer-scenario-code-example` | 신규 | 1 (#118) |
| `pattern-vs-object-naming-priority` | 신규 | 1 (#118) |
| `kotlin-version-feature-learning` | 신규 | 1 (#118) |
| `card-shortage-handling-strategy` | 신규 | 1 (#118) |
| `metaphor-fidelity-vs-srp-tradeoff` | 신규 | 1 (#118) |
| `multi-year-reviewer-activity` | 신규 | 1 (#118) |
| `ace-bonus-while-vs-math-vs-recursion` | 신규 | 1 (#118) |
| `pattern-applied-without-need` | 강화 | 7+ |
| `virtual-object-vs-real-domain` | 강화 | 8+ ⭐⭐⭐ |
| `interface-for-test-substitution` | 강화 | 7+ |
| `model-view-dependency-direction` | 강화 | 12+ ⭐⭐⭐ |
| `command-query-separation` | 강화 | 3+ |
| `function-name-vs-return-semantic-mismatch` | 강화 | 7+ |

## 메타 어휘 (15 reviewer? + MCP 핵심 가치 합류 정점)

- ***말리빈 (malibinYun) 2024 → 2025 reviewer 다년 활동*** — **MCP `multi-year-reviewer-activity` 신규 메타 케이스**.
- ***수학적 비교 어휘 (4*11-3*10 vs 4*1+10)*** — malibinYun PR #118 — *모델 자연스러움 = 수학식 단순성*.
- ***학생 「객체지향의 사실과 오해」 자가 인용*** — gahyunkim PR #118 — *MCP `oh-my-oop` 핵심 가치 정점 학생 자가 인용*.
- ***MVC 패턴 매몰 vs 객체 이름 집중*** — malibinYun PR #118 — *PR #126 (MVC 원칙) 반대 시점*.
- ***reviewer 문제 시나리오 코드 예시*** — malibinYun PR #118 — *학생 자가 답변 유도*.
- ***Kotlin 버전 업데이트 학습 권유*** — malibinYun PR #118.
- ***카지노 메타포 (graceful shutdown)*** — malibinYun PR #118.

→ **14 reviewer + MCP 핵심 가치 어휘 합류 정점 유지** (말리빈 = 기존 reviewer 다년 활동). **2025 코호트 10 PR 코퍼스 정점 도달**.

## 비교 (말리빈 2024 reviewer 시기 vs 2025 reviewer 시기)

- **2024 reviewer (PR #32 krrong 학생 시기 등)**: *"개발에 옳고 그름은 없고, 장단점만이 있는 세계"* + *재구현 권유* + *외부 글*. PR #95·#23·#26 등.
- **2025 reviewer (PR #118 gahyunkim)**: *수학적 비교 어휘 (4*11-3*10)* + *MVC 패턴 매몰 회피* + *Kotlin 2.0 학습 권유* + *카지노 메타포*.

→ **말리빈 어휘 진화** — *2024 어휘 (트레이드오프) + 2025 어휘 (수학적 비교 + 메타 학습)*. **MCP 핵심 가치 정점 어휘 유지 + Kotlin 어휘 진화**.
