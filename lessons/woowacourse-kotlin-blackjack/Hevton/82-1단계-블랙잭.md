# PR #82 — [블랙잭 1단계] 팡태 미션

- **Reviewee:** [@Hevton](https://github.com/Hevton)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/82
- **단계:** 1단계
- **리뷰어:** @KwonDae (제이든)
- **라인 코멘트:** 19 스레드 — *PR 본문 자가 의문 2개 + 의존성 그래프 첨부 + Answer 도메인 vs View 매우 긴 양방향 토론 + TestDeck 자가 정당화*
- **신규 reviewee** (2024 코호트). PR #99·#67·#77·#91 와 동일 reviewer.

## 잘한 점 (설계 관점)

- **PR 본문 자가 의문 2개 + 의존성 그래프 첨부**:
  1. *"최대한 한 방향으로 도메인 간 관계를 작성해보고자 시도했습니다. 의존 관계를 이해하고 있는지, 의존성 그래프를 작성해 보았는데 잘못 이해하고 있는 점이 있는지 피드백을 받고 싶습니다."* + 의존성 그래프 이미지 첨부.
  2. *"이전까지 미션을 진행하면서, 컨트롤러의 역할은 '응답 받고, 이를 처리해줄 도메인 함수 호출' 정도의 짧은 느낌으로만 작성했습니다. (...) 하지만 이번에는 컨트롤러의 역할을 조금 더 두었습니다. 결과적으로 '게임을 진행하는 자'가 현재 도메인이 아니라 컨트롤러가 되었습니다. 현재 구성에 대해, 더 세분화 되는게 좋을지, 구성에 대한 피드백을 솔직하게 받아보고 싶습니다."*
  
  **의존성 그래프 첨부 = 학습 가시화 모범**. PR #98·#99·#77·#107·#46 결의 *시각 자료 첨부* 진화.
- **`abstract Human` 다형성 활용 칭찬** — KwonDae: *"추상클래스를 활용하여 딜러와 플레이어의 공통 로직을 한곳에 구현하고, 다형성을 구현하는 방향 좋습니다 👍"*. PR #74·#108·#133·#132·#136·#46·#77 결.
- **`runCatching + getOrElse` Kotlin 어휘** — KwonDae: *"runCatching + getOrElse 활용 👍 👍"*. PR #93 (junjange *let-run-elvis-pitfall*) / PR #91 (murjune *Result*) 결.
- **`flatMap` Kotlin 어휘 권유 + 학생 자가 의문 + reviewer 코드 예시** — 학생: *"MarkType과 ValueType의 모든 조합을 만들기 위해서 flatMap 활용을 기대할 수가 없지 않나요?"* + KwonDae 코드 예시:

```kotlin
private val cards: MutableList<Card> =
    MarkType.entries.flatMap { mark ->
        associateMark(mark)
    }
    .shuffled()
    .toMutableList()

private fun associateMark(markType: MarkType): List<Card> {
    return ValueType.entries.map { amount -> 
        Card.from(markType, amount) 
    }
}
```

**학생 자가 의문 → reviewer 코드 예시 모범**. PR #77·#91·#99·#46·#107 결.

- **`TestDeck` 자가 정당화 + reviewer 인정** — KwonDae: *"테스트를 위해 Deck을 인터페이스화 하신걸까요? 로또 미션에서 테스트를 위해 어떤 과정들이 있었는지 생각해보면, 해당 모델 자체를 인터페이스로 만들지 않고, 랜덤요소가 들어간 대상을 내부적으로 책임을 갖지 않고 주입받도록 했던 방식도 있습니다."* + 학생: *"테스트 더블을 구현해보고자 전략 패턴 느낌을 활용했습니다. 프로덕션에서는 랜덤한 카드들을 pop 해주는 TrumpDeck, 테스트에서는 지정한 카드들을 순서대로 pop 해주는 TestDeck을 이용하게 했습니다."* + reviewer 메타 질문: *"지금은 Deck 자체를 인터페이스화 한 경우이고 전략패턴이라면, 카드덱을 생성하는 곳에서 전략패턴 사용하는게 자연스럽다는 생각을 했습니다. 1. interface Deck 2. deck.create() 중에서 어느 곳에 전략패턴을 적용하는게 좋을까요?"*. **학생 자가 전략 패턴 의도 + reviewer 적용 위치 메타 질문 모범**.

## 못한 점 (설계 관점)

### 1. `MarkType.mark` View 누수 — 메타 질문 + 학생 자가 학습

KwonDae 1차: *"`val mark: String` 은 도메인 로직일까요? View에 표시되는 문구가 수정된다면 도메인에서 수정되어야 하는걸까요?"*

학생: *"기본적으로 문구를 가지고 있어도 되겠지만, 이를 View에서 when 으로 지정해주는 방법도 있겠다고 생각이 드네요..! 후자의 경우가 제가 더 나아가고자 하는 방향과 일치하는 것이죠?"*

KwonDae 메타 질문: *"기본적으로 문구를 가지고 있어도 되겠지만, UI에 표시되어야 하는 로직이 도메인에 있어도 괜찮다는 걸까요? 🧐 출력에 표시되는 문구를 수정해달라는 기획자의 요구사항이 왔을 때 동료개발자가 어디를 가장 먼저 살펴보게 될까요?"*

학생: *"질문을 굉장히 잘해주시네요.. 단번에 이해되었습니다. 해당 부분 곧바로 수정했습니다. 감사합니다."*

→ **3 가지 학습**:

#### 1.1. *"기획자 요구사항 + 동료 개발자 위치"* — reviewer 메타 어휘

**MCP `oh-my-oop` 의 *유지보수자 시점* 어휘 강화**. PR #107·#145 (lee-ji-hoon *유지보수 관점*) 결.

#### 1.2. *enum 부가 속성 (`mark`) 도메인 X / View O*

**MCP 기존 룰 `enum-with-ui-value`**. PR #137 (HamBeomJoon *Rank.label*) / PR #77 (murjune *Suit 스페이드*) / PR #28·#78·#112·#125·#147·#23·#26·#16·#9·#11·#13 결.

#### 1.3. *학생 자가 *질문 잘해주셨다* 인정*

*"질문을 굉장히 잘해주시네요.. 단번에 이해되었습니다."* — **메타 질문 효과 학생 자가 인정**.

### 2. `TrumpDeck object 싱글톤` 동시 게임 어려움

KwonDae: *"카드덱을 object 로 관리하는 이유가 궁금해요! 게임을 다수, 또는 동시에 여러 게임을 진행한다면 어떤 문제가 발생할 수 있을까요?"*

학생: *"하나의 덱을 여러 Hand가 주입 받아 사용한다는 의미를 두고 싶었습니다. 실제로 게임을 할 때 하나의 덱에서 여러 사람이 카드를 소모하게 되는 것 처럼 구현하고자 했습니다. 하지만 이에 대해 동시 게임 진행이 어려운 문제가 존재할 수도 있겠네요.."*

KwonDae: *"하나의 Deck 인스턴스를 여러 Hand가 주입 받아 사용하면 팡태의 구현하고자 하는 사항이 만족되겠네요 👍"*

→ **싱글톤 (object) vs 단일 인스턴스 (class + DI)**:
- *object* = *프로그램 전체 단일 + 동시 게임 X*.
- *class + DI* = *게임마다 새 인스턴스 + 여러 Hand 공유*.

PR #11 (ki960213 *Deck object 자가 인정*) / PR #46 (krrong *Calculator object*) / PR #16 (BeokBeok) 결.

### 3. `Judge object 책임 vs Participant 자기 판단`

KwonDae: *"Judge라는 승패 판단하는 object 역할에 대해 생각해보면 좋을 것 같아요. 사실 승패를 계산하는 로직은 딜러와 플레이어간 차이점이 있을까요? 팡태가 만들어놓은 `abstract class Human` 에서 아래와 같이 다른 사람과의 비교를 자체적으로 판단할 수 있도록 한다면 Judge object는 역할이 어떻게 될까요?"*

```kotlin
abstract class Human {
   fun judge(otherHumane: Human): Result { ... }
}
```

학생: *"'전체 결과' 에 대한 책임을 갖는 객체를 따로 두어서 역할을 주면, 책임 분리가 조금 더 직관적이지 않을까? 생각했습니다. 하지만 '전체 결과'로 꼭 생각하지 않더라도 승패 로직을 확실히 휴먼에서 다뤄주면 좋을 것 같다는 생각이 드네요.. 감사합니다"*

→ **참가자 vs 참가자 일반화 + Judge object 책임**:

- *Judge object 분리*: *책임 응집 직관* (학생 의도) vs *Participant 가 충분히 자기 판단 가능* (reviewer).
- *Tell-Don't-Ask + 자기 판단* 정공.

PR #77 (murjune *participant.judge*) / PR #46 (krrong *Dealer → Participant*) / PR #95·#23·#148·#151·#49·#132·#74·#98·#43 결의 *참가자 vs 참가자 일반화* 결.

### 4. `Hand` 가 `TrumpDeck` 의존 — 의미 모호

KwonDae 1차: *"Hand 클래스가 전체카드덱(TrumpDeck)을 프로퍼티로 갖고있어야 할까요?"*

학생: *"생성자에서 의존성 주입하는 경우에 대해서만 너무 익숙하게 생각했던 것 같습니다. 설계할 땐 이게 가장 의존성 주입 관점에서 의미있지 않을까 싶었는데, 생각해보니 draw 시에 주입해줘도 되겠다는 생각이 듭니다."*

KwonDae 2차: *"제가 이해한 Hand는 현재 사용자가 들고있는 카드 정보라고 이해했었는데, 프로퍼티 타입은 Deck 이어서 `List<Card>`가 더 어울리지 않을까한 코멘트였습니다 🙂"*

학생: *"제가 동료 개발자가 봤을 때 충분히 헷갈릴만한 코드를 작성하였네요. 이에 대해 코드를 개선해보겠습니다."*

→ **`Hand` 의미 = 현재 손에 든 카드** vs *전체 덱 X*. **MCP 룰 후보 `hand-deck-conflation`** = *Hand 가 Deck 보유 = 의미 혼동*. PR #77 (murjune *Hand 책임*) / PR #99·#46·#43 결.

### 5. `TrumpDeck` 내부 랜덤 = 테스트 불가

KwonDae 1차: *"카드덱 생성시 내부적으로 랜덤요소를 반영하여 카드덱을 생성하고 있습니다. 만약 특정 카드덱을 셋팅해두어 pop() 하는 로직을 테스트 해야 한다면 어떻게 수정해야 할까요? 🧐"*

학생: *"그것을 위한 목적으로 TestDeck을 만들어보게 되었습니다"*

→ *TestDeck 자가 정당화* (위 §잘한점). 다만 *전략 패턴 적용 위치* 가 학생 자가 검토할 만한 영역. PR #74·#23·#95·#99·#26·#39·#46·#77 결.

### 6. `Deck` 카드 부족 시 예외 처리

KwonDae: *"만약 덱에 더이상 카드가 없다면 어떤 일이 발생할까요? 지난 미션에서 논리 오류가 아니라면 어떻게 했었는지 생각해볼 수 있겠네요!"*

학생: *"감사합니다. 해당 예외에 대해서는 고려하지 못했네요..!"*

KwonDae 별도 (이후): *"사실 현실세계에서는 참가인원이 2~8명으로 제한되어 있습니다 ㅎㅎ 그렇기에 덱이 모두 소진되는 일 & 중복되는 카드는 발생할 수 없죠. 다만, 프로그래밍 관점에서 removeAt() 이라는 함수가 exception..."*

→ **이전 미션 학습 자가 연결**. PR #91 (murjune *논리 오류*) / PR #46·#26·#74·#108·#133·#136·#41·#107 결.

### 7. `ValueType.rank` View vs 도메인

KwonDae: *"rank는 도메인 로직일까요~? UI 로직이 수정된다면 여기서 수정이 일어나야 됩니다 🧐 amount를 활용할 수 있겠네요!"*

학생: *"감사합니다. amount와 달리 rank는 도메인 로직이 아니겠네요..!"*

→ **enum 부가 속성 도메인 vs View**. PR #137·#77·#23·#26·#16·#9 결.

### 8. `Card` 상수 (INVALID_RANGE, MAX_NUMBER, DIVIDER) 의미 모호

KwonDae: *"처음에 INVALID_RANGE, MAX_NUMBER, DIVIDER 가 무얼 의미하는지 이해하기 힘들었네요 🥲 만약 숫자 혹은 모양이 추가된다면 다시 계산하여 상수를 바꿔주어야 할까요?"*

학생: *"이 부분이 저도 설계에 어려움을 느꼈습니다 ㅜ..ㅜ 처음에는 '문양' x '숫자' 를 조합하여 카드를 생성하는 과정을 넣으려고 했으나, 그렇게 되면 과제 요구사항에 적혀진 'depth 2 미만' 에 위배되기 때문에 해당 방법을 생각하게 되었습니다.. 이 부분은 더 고민해보겠습니다."*

→ **`depth 2 미만` 요구사항 vs 자연스러운 모델링**:
- *요구사항 (인덴트 depth ≤ 2)* + *문양 × 숫자 조합 (2 중 for 또는 flatMap)* 충돌.
- *MarkType.flatMap { ValueType.map }* — `flatMap` 이 depth 회피 (위 §잘한점).

**MCP 룰 후보 `requirement-vs-natural-modeling-conflict`** = *요구사항 강제 = 부자연스러운 모델링 시그널*. PR #98 (chaehyuns *3 변수 제약*) / PR #146 (yrsel *YAGNI*) 결.

### 9. `Hand vs Cards` 명명 혼동 (위 §4 와 같음)

### 10. `Answer 도메인 vs View` 매우 긴 양방향 토론

KwonDae: *"이 로직들은 도메인 로직과 UI 로직 중 어디에 더 어울릴까요? 🧐"*

학생: *"Answer의 역할로 묶으면 테스트가 용이하지 않을까 싶었습니다..!! '사용자의 응답'에 대해 '정해진 응답의 객체'로 변환해야 하는 과정에서 UI에서도 일부 로직을 갖고, 도메인에서도 일부 로직을 가져서 분리되어있는 방법 대신에 도메인에서 이 로직을 한번에 담당해주면 조금 더 유지보수가 간편하지 않을까 싶었는데, 이에 대해 어떻게 생각하시는지 궁금합니다..!"*

KwonDae 메타 질문 + 유지보수자 시점:

> *"UI에서 도메인에 따라 출력 양식을 결정하는 로직을 갖는게 어울리지 않는다는 걸까요?*
>
> *'정해진 응답의 객체'로 변환 — View에서 하는 역할이 이런 역할이지 않을까요?*
>
> *예를 들어, 출력 양식이 또는 입력 조건이 수정되었을 때 동료 개발자가 어디를 가장 먼저 찾아볼까요?*
>
> *1. Answer 도메인 로직이 어떻게 구현되어 있는지 들어온다.*
> *2. OutputView /InputView 와 Controller 호출부를 찾아본다"*

학생: *"질문을 정말 잘해주시네요.. 되돌아보게 되었습니다. 해당 부분 적용해보겠습니다."*

학생 자가 후속 의문: *"Answer 로직은 도메인이 아닌 View로 빼면서 테스트가 불가능해졌는데, 이러한 것의 이점이 어떤 것이 있을까요??"*

KwonDae: *"이번 수정에서 Answer 모델이 삭제되었네요? 오히려 Answer 로직에서 View에 의존하는 로직이 있었다면 단위테스트가 불가했을텐데 이부분에 대해서는 부연설명을 부탁드려도 될까요?"*

학생: *"Answer 객체는 fromInput(input: String) 이라는 팩토리 메서드로 생성되게 구현하여, View에서의 입력값으로 받을 String의 값에 따른 단위테스트를 진행할 수 있었습니다! 즉 사용자의 모든 입력에 따른 단위테스트가 도메인 로직에서 가능했던 느낌입니다. 제 생각에는 사용자의 응답이 YES 또는 NO 또는 기타 입력으로 왔을 때의 단위테스트를 진행해야 한다고 생각했는데, 지금처럼 InputView에서 진행하는 부분이 있다면 사용자 입력에 대한 단위테스트가 어렵다고 생각했는데, 잘못 이해하고 사용한 부분이 있을까요??"*

KwonDae 마무리: *"이 테스트는 도메인 테스트일까요 View 테스트에 해당할까요? 입력에 따른 단위테스트라면 도메인 로직이 사용자의 입력에 의존하고 있었다는 이야기가 되겠네요..! 그럼 사용자 입력을 Answer 도메인으로 부터 분리하고 Answer 객체가 다른 역할이 없다면 해당 객체의 존재 가치에 대해서 생각해볼 필요가 있을 것 같습니다."*

→ **5 가지 학습**:

#### 10.1. *사용자 입력 의존 = 도메인 객체 X*

- *Answer.fromInput(String)* = *사용자 입력 의존* = *도메인 의도 아닌 View 변환 책임*.
- *Answer 가 다른 역할 없음* = *존재 가치 의문*.

#### 10.2. *테스트 도메인 vs View*

학생이 *Answer 도메인 테스트* 라고 생각한 것이 *실은 View 입력 테스트*. **MCP 룰 후보 `test-classification-domain-vs-view`**.

#### 10.3. *동료 개발자 찾아보는 위치 = 유지보수자 시점*

KwonDae 어휘. PR #107·#145 (lee-ji-hoon) / PR #46·#91 결.

#### 10.4. *학생 자가 *"잘못 이해하고 사용한 부분이 있을까요?"* 자가 의문*

PR #91·#46·#107·#43 결.

#### 10.5. *Answer 객체 자가 삭제 결정*

학생 자가 *불필요 객체 자가 결정 + 삭제*. PR #145·#13·#146·#31 결.

### 11. `GameManager` vs `Controller` 게임 진행자

KwonDae: *"팡태가 말씀하신 것 처럼 `결과적으로 '게임을 진행하는 자'게 현재 도메인이 아니라 컨트롤러` 가 되었는데, 그렇다면 컨트롤러 내에서 BlackJackGame..."*

학생 자가 의문: *"말씀하신 GameManager는 View와 상호간 값, 명령을 진행한다면 MVC에서 도메인의 역할은 아닌 것인가요?? 도메인에서는 뷰를 참조하지 않는다고 생각하고 있기에 제가 KwonDae의 말씀을 잘못 이해한 것 같아서 질문드립니다!"*

KwonDae: *"제가 말씀드리는 GameManager는 도메인 역할이 아닌, Controller - 블랙잭 게임장 / GameManager - 매 게임을 관리감독 하는 관리자 (매 게임이 끝나면 초기화(새로운 인스턴스)가 된다) 에 비유할 수 있겠네요. GameManager가 있어야 한다가 아닙니다. 단순 이해를 돕기위해 예시를 들고와봤습니다. MVC에서 Controller는 현재 블랙잭에서 게임관리자가 맞습니다. 도메인은 해당 '게임에 참여하는 자'가 되겠네요"*

→ **MVC Controller = 게임 관리자 + 도메인 = 게임 참가자**. *학생 자가 *MVC 정의 의문* + reviewer 명확화*. PR #145·#107·#46·#91 결.

### 12. `사용자 응답 모든 입력` Player 비유

KwonDae 별도 *"사용자의 응답이 YES 또는 NO 또는 기타 입력 단위테스트"* 의 마무리: *(jumbled 코드)*. PR #91·#107·#46 결.

### 13. `participant.initGame(cardDeck)` 메시지 던지기

KwonDae: *"딜러와 플레이어를 게임 시작 셋팅을 해주는 부분이군요..! `게임 시작 시 참가자들은 두장의 카드를 갖게된다.` 이부분은 비즈니스 로직에 해당할까요? `participant.initGame(cardDeck)` 식으로 객체가 알아서 하게끔 메세지를 던져보는건 어떨까요?"*

→ **Tell-Don't-Ask + initGame 메시지**. PR #99·#107·#43·#46·#77·#9·#76 결.

### 14. `Test Fixture` 외부 글 추천

KwonDae: *"참고로 테스트에 Test Fixture 라는 개념이 있습니다. 이는 여러 테스트에서 같은 Data를 사용할 때, 코드 중복을 피하면서 가독성을 높이는 방법인데요"* + [tjdtls690 Test Fixture 글](https://tjdtls690.github.io/studycontents/java/2022-10-10-test_fixture/) 추천.

→ **외부 자료 첨부**. PR #91 (KwonDae *팩토리 메서드 글*) / PR #95·#67·#23·#26·#16·#46·#77·#107 결.

### 15. `Player.judge` Dealer 의 코드 vs Player 의 코드 (suggestion)

KwonDae suggestion (코드만 첨부, 본문 잘림):

```kotlin
// in Dealer.kt
override fun judge(other: Participant): ResultType {
    return when {
        other.participantState is ParticipantState.Bust -> ResultType.WIN
        participantState is ParticipantState.Bust -> ResultType.LOSE
        this.getPointWithAce() > other.getPointWithAce() -> ResultType.WIN
        this.getPointWithAce() == other.getPointWithAce() -> ResultType.DRAW
        ...
```

→ **참가자 vs 참가자 일반화 + 자기 Bust 우선**. PR #77 (murjune *블랙잭 룰 비대칭*) / PR #95·#23·#148·#151 결.

### 16. `의존성 그래프` 자가 검토 (위 PR 본문 §1)

KwonDae: *"의존성 그래프를 잘 작성해주셔서 덕분에 프로젝트 이해하는데 아주 큰 도움이 되었습니다 👍 💯 지금 human 이라는 추상클래스를 활용하여 플레이어, 딜러간 공통 로직을 잘 설계해주셨는데요, 조금 더 나아가서 어떤 부분이 공통 로직이 될 수 있고, 어떤 부분이 다른 구현을 가질 수 있어 다형성을 고려할 (수 있는지 ...)"* + 학생 자가 의문: *"의존성 그래프를 그린 뒤에, 의존성이 가장 적은 것부터 구현하는 방향이 구현이 쉽지 않나요??!"* + reviewer 자가 명확화: *"맞습니다. 그래서 의존성 그래프를 그려 구조를 파악하기 쉽게 만들도록 하는 역할과 함께 가장 의존성이 적은 것 부터 구현하는게 맞습니다. 제가 헷갈리게 말씀드린거 같네요 🥲 하고싶었던 의미는 '객체 스스로가 할 수 있는 기능임에도 불구하고 다른 객체에 의존하는 상황에 대해 생각해보자' 였습니다!"*

→ **의존성 그래프 자가 작성 + reviewer 자가 사과 + 의도 명확화**. PR #91·#77·#46·#16·#95·#137·#107 결.

## 리뷰어 의견 요지 (KwonDae / 제이든)

19 코멘트 특징 (PR #99·#67·#77·#91 와 동일 reviewer):

- **메타 질문 + 유지보수자 시점** — *"기획자 요구사항이 왔을 때 동료 개발자가 어디를 가장 먼저 살펴보게 될까요?"* — **유지보수자 시점 강화**. PR #107·#145 결.
- **외부 글 + 코드 예시 첨부** — *Test Fixture 외부 글* + *flatMap 코드 예시*. PR #91·#77·#46 결.
- **`?_?`, `🧐`, `🥲` 친밀한 이모지** — PR #99·#77·#91 일관.
- **reviewer 자가 사과 + 의도 명확화** — *"헷갈리게 말씀드린거 같네요 🥲"* / *"단순 이해를 돕기위해 예시를 들고와봤습니다"*. PR #91·#77·#46·#16·#95·#137·#107 결.
- **이전 미션 학습 자가 연결** — *"지난 미션에서 논리 오류가 아니라면 어떻게 했었는지"* — PR #91 결.
- **칭찬 분포** — *추상클래스 다형성, runCatching, flatMap 권유, 의존성 그래프* 등 칭찬.

## 대표 인용

> *"질문을 굉장히 잘해주시네요.. 단번에 이해되었습니다. 해당 부분 곧바로 수정했습니다."* — Hevton (메타 질문 효과 학생 자가 인정)

> *"기획자의 요구사항이 왔을 때 동료개발자가 어디를 가장 먼저 살펴보게 될까요?"* — KwonDae (유지보수자 시점 메타 질문)

> *"출력 양식이 또는 입력 조건이 수정되었을 때 동료 개발자가 어디를 가장 먼저 찾아볼까요? 1. Answer 도메인 로직이 어떻게 구현되어 있는지 들어온다. 2. OutputView /InputView 와 Controller 호출부를 찾아본다"* — KwonDae (유지보수자 위치 시뮬레이션)

> *"Answer 객체가 다른 역할이 없다면 해당 객체의 존재 가치에 대해서 생각해볼 필요가 있을 것 같습니다."* — KwonDae (불필요 객체 시그널)

> *"제 생각에는 사용자의 응답이 YES 또는 NO 또는 기타 입력으로 왔을 때의 단위테스트를 진행해야 한다고 생각했는데, 지금처럼 InputView에서 진행하는 부분이 있다면 사용자 입력에 대한 단위테스트가 어렵다고 생각했는데, 잘못 이해하고 사용한 부분이 있을까요??"* — Hevton (학생 자가 *잘못 이해* 자가 의문)

> *"제가 헷갈리게 말씀드린거 같네요 🥲 하고싶었던 의미는 '객체 스스로가 할 수 있는 기능임에도 불구하고 다른 객체에 의존하는 상황에 대해 생각해보자' 였습니다!"* — KwonDae (reviewer 자가 사과 + 의도 명확화)

## 얻은 교훈

1. **`유지보수자 시점 (기획자 요구사항 + 동료 개발자 위치)` 메타 어휘** — *"기획자 요구사항이 왔을 때 동료 개발자가 어디를 가장 먼저 살펴보게 될까요?"*. **MCP 룰 후보 `maintainer-perspective-locator`**. PR #107·#145·#46·#91 결.

2. **`Answer 도메인 vs View` 양가성** — *학생이 *Answer 객체* 로 *사용자 입력 변환* 응집 시도* + *reviewer 가 *Answer 가 다른 역할 없음 = 존재 가치 의문* 메타 + 학생 자가 삭제*. **MCP 룰 후보 `intermediate-object-without-role`** = *단순 변환만 담당하는 객체 = 존재 가치 의문*.

3. **`테스트 분류 (도메인 vs View)`** — *입력 변환 테스트 = View 테스트 / 비즈니스 로직 = 도메인 테스트*. **MCP 룰 후보 `test-classification-domain-vs-view`**.

4. **`Hand 가 Deck 보유 = 의미 혼동`** — *Hand = 현재 손에 든 카드 / Deck = 전체 카드*. **MCP 룰 후보 `hand-deck-conflation`**.

5. **`참가자 vs 참가자 일반화 + Judge object 책임`** — *Judge 분리 vs Participant 자기 판단*. PR #77·#46·#95·#23·#148·#151·#49·#132·#74·#98·#43 결.

6. **`요구사항 (인덴트 depth ≤ 2) vs 자연스러운 모델링 충돌`** — *학생 자가 *요구사항 강제 = 부자연스러운 상수 도입* 자가 인정*. **MCP 룰 후보 `requirement-vs-natural-modeling-conflict`**. PR #98 (chaehyuns *3 변수 제약*) / PR #146 (yrsel *YAGNI*) 결.

7. **`의존성 그래프 PR 본문 첨부`** — *학습 가시화 + 의존성 자가 검토*. **MCP 룰 후보 `dependency-graph-self-review`**. PR #98·#99·#77·#107·#46 결의 *시각 자료 강화*.

8. **`flatMap` Kotlin 어휘 = depth 회피 + 자연스러운 모델링** — *2 중 for / nested map → flatMap*. **MCP 룰 후보 `nested-loop-to-flatmap`**.

9. **`reviewer 자가 사과 + 의도 명확화`** — 다수. PR #91·#77·#46·#16·#95·#137·#107·#145 결.

10. **`TestDeck = 전략 패턴 적용 위치` 학생 자가 의문** — *Deck 자체 interface (학생) vs Deck.create() 위치 (reviewer)*. PR #74·#77·#23 결.

11. **`이전 미션 학습 자가 연결`** — *"지난 미션에서 논리 오류가 아니라면"* — PR #91·#95·#23·#46 결의 *KwonDae 어휘*.

12. **`MVC Controller vs GameManager` 비유** — *Controller = 게임장 / GameManager = 매 게임 관리자 (인스턴스화)*. **MCP 룰 후보 `controller-vs-game-manager-distinction`**.

13. **`object 싱글톤 vs class + DI` 동시 게임 시나리오** — PR #11·#46·#16 결.

14. **`PR 본문 자가 의문 2개 + 의존성 그래프 + 시각 자료` = 학습 가시화 풍부** — PR #98·#99·#77·#107·#46·#91 결.
