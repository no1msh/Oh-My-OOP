# PR #122 — [블랙잭 1단계] 뭉치 미션

- **Reviewee:** [@m6z1](https://github.com/m6z1)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/122
- **단계:** 1단계
- **리뷰어:** [@hyemdooly](https://github.com/hyemdooly) (둘리) — 2025 코호트 reviewer (학생 → reviewer 진화)
- **라인 코멘트:** 17 스레드 — *PR 본문 *따끔하게 피드백* 자가 한계 인정 + cards.cards vs value 명명 학생 자가 의문 + reviewer *취향 차이, 정답 없어요!* 메타 어휘 + Dealer Player 상속 → Participant 추상화 + 동명이인 검증 학생 자가 정정 + 분기 테스트 자가 의문 + 학생 자가 *컴파일 상수* 학습 + 블랙잭이면 hit X 룰*
- **신규 reviewee** (2025년 코호트 — 모바일 안드로이드 7기).

## 잘한 점 (설계 관점)

### 1. PR 본문 자가 한계 인정 + *따끔하게 피드백*

PR 본문:

> *"안녕하세요 둘리! (...) 블랙잭 미션을 돌아가게하자! 라는 부분에 초점을 두어, 코드가 질서정연하지 않고 가독성이 떨어지는 것 같습니다.. 따끔하게 피드백 주시면 감사하겠습니다!!"*

→ **학생 자가 한계 인정** = PR #118 *많이 혼내주세요* 결 합류.

### 2. reviewer *취향 차이, 정답 없어요!* 메타 어휘 (Thread 1)

학생 자가 의문 (`cards.cards` vs `cards.value`):

> *"기존의 cards의 생성자 네이밍은 cards 였습니다. 일급컬렉션을 만들어 외부에서 사용하게 되면 , `List<Card>`에 접근하기 위해서는 `cards.cards.~~`로 사용을 했어야 했는데, 미관상으로도 그렇고 카드들의 카드들이라는 이름이 어색하다고 느꼈습니다."*

reviewer:

> *"취향차이라고 생각하는데, 저는 `cards.cards`를 더 많이 사용하긴 합니다. 정답은 없어요!"*

→ **MCP `oh-my-oop` 핵심 가치 정점 어휘 (취향 + 정답 없음)** = PR #19·#95·#23·#26·#129·#36·#142 결 누적 9+ PR.

### 3. Dealer 가 Player 상속 → Participant 추상화 (Thread 3)

학생 자가 학습:

> *"현재 Dealer는 Player를 상속받고 있습니다. 하지만, dealer가 아닌 player들은 `딜러` 라는 이름을 가질 수 없다고 판단했습니다. 하지만, `init`을 통해 이름에 대한 검증을 진행하게 되면, 딜러 또한 이 에러에 막히는 것을 깨달았습니다."*

학생 자가 적용: `Participant` 상위 클래스 생성. PR #126·#129·#118·#138 결 합류 누적 11+ PR.

### 4. ACE 계산 단순화 (Thread 7) — 학생 자가 정정

학생 자가 답변:

> *"ace 카드의 끗수의 값을 기본 1로 두고, 에이스 카드가 있을 경우에 10을 더해도 21을 초과하지 않으면 10을 추가하는 로직으로 수정해보았습니다."*

→ **Ace 1 기본 + 10 추가** = PR #36·#117·#129·#138·#142·#118 결 합류.

### 5. 학생 자가 *컴파일 상수* 학습 (Thread 13)

reviewer: *"Kotlin에서 이렇게 전부 대문자로 이름을 짓는 경우는 어떤 경우일까요?"*

학생:

> *"상수일 경우에만 짓는 것으로 알고 있습니다. DENOMINATIONS / SHAPES / CARDS를 모두 대문자로 둔 이유는 이 앱이 한 번 실행될 경우, 변경되지 않는 값을 가지며, decompile 했을 때 static final 로 디컴파일 되어 상수라고 판단해 대문자로 지었습니다!"*

reviewer:

> *"대문자로 이름을 짓는 경우는 컴파일 상수인 경우입니다! 컴파일, 런타임이 무엇이고, `List`는 [컴파일 상수](https://kotlinlang.org/docs/properties.html#compile-time-constants)일까요? 🤔"*

→ **컴파일 vs 런타임 상수 학생 자가 학습** = **MCP 룰 `compile-time-constant-vs-runtime-value` 후보**.

### 6. 추가 칭찬

- Thread 8: *"방어적 복사 👍"*.

## 못한 점 (설계 관점)

### 1. combine String 함수 도메인 vs view (Thread 2)

reviewer:

> *"`combine` 함수의 로직은 뷰의 관심사가 맞습니다. 그럼 이렇게 생각해보면 어떨까요?*
> *1. 이 도메인 로직을 여러 플랫폼에서 사용한다*
> *2. `combine()` 함수로 뷰에 출력할 String값을 받는다.*
> *3. A플랫폼에서는 `A다이아몬드`, B플랫폼에서는 `B다이아몬드` 라고 출력해야한다.*
> *뷰의 요구사항이 바뀌면 뷰를 수정하는 것이 자연스럽지 않을까요?"*

→ **여러 플랫폼 가정 = 변경 가능성 어휘** = PR #126·#130·#143·#118 결 합류 누적 7+ PR.

### 2. 동명이인 검증 학생 자가 정정 (Thread 3)

학생:

> *"추가로 플레이어 이름의 중복 불가능 하도록 하는 것은 플레이어 중에서 동명이인이 있다고 했을 때 누가 이겼는지 알기 어렵다고 판단하여 중복을 허용하지 않았는데, 카지노 내에서 실제로 동명이인이 있을 수도 있고, 딜러와 플레이어 1대 1로 대결하는 블랙잭의 특성상 중복을 허용해도 될 것 [같다]"*

→ **학생 자가 도메인 룰 학습** = PR #138·#129 결 합류.

### 3. 단위 테스트 분기 (Thread 4·5) — Controller 분기 폭증

학생 자가 의문:

> *"딜러가 블랙잭일 경우에 플레이어가 블랙잭 or 아닐 경우, 딜러가 블랙잭이 아니고, 플레이어가 블랙잭 or 아닐 경우 등등.. 여러 분기에 대해서 테스트를 진행해보고 싶은 갈증이 있습니다."*

reviewer:

> *"플레이어가 블랙잭이고 딜러가 카드 합 일반 21이면 플레이어 승으로 계산되어야 하는데요! 지금 로직 상으로는 push로 계산될 것 같은데 맞을까요? 👀"*

→ **블랙잭 룰 정확성 (플레이어 블랙잭 vs 딜러 21점 = 플레이어 승)** = PR #130 (medAndro 블랙잭 룰 자가 학습) 결 강화.

**MCP 룰 `blackjack-rule-fidelity-comparison` 강화** 누적 18+ PR ⭐⭐⭐.

### 4. Player 가 deck 에서 카드 자가 뽑기 (Thread 6) — Tell-Don't-Ask

reviewer: *"Controller에서 deck에서 카드를 뽑아서 주지말고, player가 직접 deck에서 카드를 뽑는다면 어떨까요?"*

→ **Tell-Don't-Ask + Participant 추상화** = PR #126·#129·#138·#142 결.

### 5. 테스트를 위한 변수 = Dead code (Thread 9)

reviewer: *"이 변수는 테스트를 위함일까요? 🤔"*

학생: *"테스트를 위한 변수는 불필요하다고 판단하여 제거했습니다!"*

→ **Dead code** = PR #153·#118 결.

### 6. result 갱신 시점 (Thread 10)

reviewer: *"`result`는 결과 계산이 필요할 때만 계산해서 보여주면 될 것 같은데, 게임 진행 내내 갱신을 하고 있더라구요."*

학생 자가 적용: *마지막 모두 hit/stay 후 한 번만 갱신*.

→ **lazy 계산 vs 매번 갱신** = PR #87·#143 결 합류.

### 7. enum answer 불필요 + Boolean (Thread 11)

reviewer:

> *"어차피 하드코딩된 \"Y\"와 \"N\"에 따라 결과값을 리턴할거라면 enum 내의 `answer`는 정의하지 않아도 괜찮아보여요. 파라미터 `answer`가 지금 `String`인데, 진행 여부를 `Boolean`으로 받아도 충분하지 않을까요?"*

→ **Boolean vs enum** = PR #113·#117·#126 결 합류.

### 8. Cards.status 자가 업데이트 (Thread 12)

reviewer: *"상태를 활용하겠다면, `Cards`가 상태를 가지고, 카드가 추가될 때마다 상태를 업데이트하는 것이 자연스럽지 않을까요?"*

→ **State 패턴 자가 적용** = 누적 12+ PR.

### 9. 함수 명명 정확성 (Thread 14)

reviewer: *"추가 진행 여부가 boolean 리턴값인건 알겠는데, 코드를 읽기 전까지는 리턴값을 예상할 수가 없네요."*

→ **이름 의미 정확성** = 누적 14+ PR.

### 10. 블랙잭이면 hit X 룰 (Thread 15) — 이미지 첨부

reviewer가 이미지 첨부하여 *블랙잭 시 hit 불가 룰* 표시.

→ **블랙잭 룰 정확성 + 이미지 첨부 reviewer 어휘** = **MCP 룰 `reviewer-image-attachment-as-domain-rule` 후보**.

### 11. Cards.status BlackJack 도달 불가 (Thread 17)

reviewer:

> *"Cards를 처음 생성할 때 첫 턴이니 2장을 넣어줄 것으로 생각하고 이렇게 짜여진 것 같은데요! 실제 코드에서는 Player를 빈 카드 리스트를 지닌 것으로 생성 후 카드를 뽑게 되어있어, 이 status는 BlackJack이 나오는 경우가 없을 것 같은데 맞을까요?"*

→ **Dead state = 도달 불가능 분기** = **MCP 룰 `unreachable-state-branch` 후보**.

## 대표 인용

> [hyemdooly Thread 1]
> *"취향차이라고 생각하는데, 저는 `cards.cards`를 더 많이 사용하긴 합니다. 정답은 없어요!"*

→ **MCP `oh-my-oop` 핵심 가치 (취향 + 정답 없음)** — 누적 9+ PR.

> [hyemdooly Thread 13]
> *"대문자로 이름을 짓는 경우는 컴파일 상수인 경우입니다!"*

→ **컴파일 상수 어휘** — *Kotlin 어휘 자가 학습*.

## 얻은 교훈 (보편)

1. **취향 차이 + 정답 없음 메타 어휘** = 누적 9+ PR ⭐⭐⭐.
2. **컴파일 vs 런타임 상수** — **MCP 룰 `compile-time-constant-vs-runtime-value` 후보**.
3. **블랙잭 룰 정확성 (플레이어 블랙잭 vs 딜러 21점)** = 누적 18+ PR ⭐⭐⭐.
4. **reviewer 이미지 첨부 = 도메인 룰 인용** — **MCP 룰 `reviewer-image-attachment-as-domain-rule` 후보**.
5. **Dead state 도달 불가능 분기** — **MCP 룰 `unreachable-state-branch` 후보**.
6. **여러 플랫폼 가정 = 변경 가능성** = 누적 7+ PR.
