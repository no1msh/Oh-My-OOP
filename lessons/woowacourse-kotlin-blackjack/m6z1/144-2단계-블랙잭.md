# PR #144 — [블랙잭 2단계] 뭉치 미션

- **Reviewee:** [@m6z1](https://github.com/m6z1)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/144
- **단계:** 2단계
- **리뷰어:** [@hyemdooly](https://github.com/hyemdooly) (둘리) — 1단계 #122 동일 reviewer
- **라인 코멘트:** 20 스레드 — *학생이 PR 본문 외에 자가 코드에 4 자리에 의도 명시 (Thread 1-4 학생 자가 셀프 코멘트) + Money 객체 사용 vs 제거 reviewer 의도 + 학생 자가 *명령형 → 함수형* 다회 적용 + 학생 자가 *Participants 생성* vs reviewer *의도 X* 양방향 + 함수 명명 (isTurnOver) 정확성*
- **양 단계 완성 reviewee** (1단계 #122 batch 24 완료 + 2단계 #144 = batch 24 추가, 동시 분석).

## 잘한 점 (설계 관점)

### 1. 학생 자가 코드 의도 명시 4 자리 (Thread 1-4)

학생이 reviewer 외에 자가 코드에 *코멘트* 추가:
- Thread 1: *Money open vs abstract 이유*.
- Thread 2: *카드 여러 장 뽑기 = 참가자 능동*.
- Thread 3: *bettingMoney lateinit 이유*.
- Thread 4: *BettingMoney + Money 분리 이유 + 자가 의문*.

→ **학생 자가 *코드 self-review* 어휘 정점** = *학습 가시화 정점*. **MCP 룰 `student-self-review-as-comment` 후보**.

### 2. 베팅 금액 vs 수익금 객체 분리 (Thread 4)

학생 자가 의문:

> *"`베팅 금액`과 `돈` 이란 객체를 분리한 이유는 (...) 베팅 금액과 수익금은 비슷하지만 다른 개념이라 생각했습니다. 또한, 딜러는 베팅 금액을 가지지 않는다는 점과, 베팅 금액은 조건(0원 이하일 수 없다)이 따로 존재한다는 점에서 베팅 금액을 따로 만들어야겠다고 생각했습니다."*

reviewer:

> *"우선 지금처럼 따로 만들긴 해야한다고 생각해요. 왜냐하면 수익금은 마이너스가 존재할 수 있기 때문에, 같은 검증은 사용하지 못할거기 때문이에요. 🤔"*

→ **invariant 다른 = 별도 value class** = PR #142·#143 결 합류.

### 3. 첫 턴 = 비즈니스 로직 학생 자가 학습 (Thread 5)

reviewer: *"딜러가 첫 턴에 카드를 두 장 뽑고나서 한 장만 보여주는 것은 비즈니스 로직일까요, 뷰의 로직일까요?"*

학생: *"하지만, 다시 생각해보니 카드 한 장은 숨기고 플레이어들에게 딜러의 카드를 유추하며 베팅 하게끔 유도하는 것 자체가 블랙잭의 도메인이라고 생각해서 비즈니스 로직으로 수정해보겠습니다."*

→ **블랙잭 도메인 룰 학생 자가 학습 (베팅 유도 어휘)** = PR #126·#129·#142·#143·#149 결 합류.

### 4. `firstTurn` 상태 변수 vs 카드 사이즈 (Thread 8)

학생 자가 답변:

> *"기존 firstTurn 이란 boolean 값을 가진 변수를 사용할 때 이 값을 업데이트 하는 시점이 애매했습니다. (...) 카드가 2장 일 때 조건을 찾아 boolean 값을 업데이트 하는 것보다 애초에 상태를 찾을 때 카드 사이즈가 2일 때로 명시하는 것이 좋겠단 생각을 했습니다."*

→ **상태 플래그 vs 도메인 상태 판단** = PR #126·#142 (state-flag-vs-domain-state-judgement) 결 합류 누적 3+ PR.

### 5. Money → Profit value class 자가 정정 (Thread 13)

학생 자가:

> *"value를 수익금으로 사용하는 것 때문에 double 로 사용했는데, 생각해보니 money 를 받을 때와 초기값은 double 일 필요가 없다고 판단해서 int 로 수정했습니다! money 객체가 제거됨에 따라 BettingMoney 는 int 형 그대로, Profit 객체는 double 형으로 두었습니다."*

→ **Money → Profit 분리 + 타입 자가 정정** = PR #142·#143·#140 결.

### 6. 추가 칭찬

- Thread 9: *"캐싱 전략을 활용해주셨네요 👍"*.

## 못한 점 (설계 관점)

### 1. 학생 자가 Participants 생성 vs reviewer 의도 X (Thread 11)

학생:

> *"해당 리뷰를 보고, Participants 라는 참가자의 복수형 객체를 생성했습니다. 생성자로 딜러와 플레이어들을 받으며, 해당 객체에서 각 참가자들이 직접 로직을 계산하며, profit 을 업데이트 하도록 수정해보았습니다. 둘리가 의도하신 리뷰에 맞게 수정이 되었는지 궁금합니다!"*

reviewer:

> *"리뷰 의도는 Participants를 만들라는 것이 아니긴 했습니다 😅 이것도 코드에 리뷰 달아드릴게요~!"*

→ **학생 자가 잘못된 자가 적용 + reviewer 의도 다름 인정** = PR #142·#126 (reviewer 자가 의도 명료화) 결 합류.

### 2. `isTurnOver()` 명명 — 함수가 3 역할 (Thread 12)

reviewer:

> *"`isTurnOver()` 함수라고하면, 턴이 오버되었는지(끝났는지) 판단하는 함수일 것 같은데 카드를 뽑고, 출력도 하고, 버스트인지까지 확인 후에 Boolean값을 리턴하네요!"*

학생 자가 분리: *hit 로직을 isHitPlayerBust 로 분리*.

reviewer 자가 후속: *"흐음 이것도 결국 카드를 뽑는 로직이 함수에 포함되어 있어 함수명과 맞지 않은 것 같아요 😭"*

→ **CQS 위반 + reviewer 자가 후속** = PR #117·#139·#130·#141·#94·#118 결 합류.

### 3. emptyList() Kotlin 어휘 (Thread 10)

reviewer: *"빈 리스트는 `emptyList()`를 사용할 수 있어요!"*

→ **Kotlin 어휘 자가 학습**.

### 4. `getXXX()` 명명 (Thread 7)

reviewer: *"보통 `getXXX()` 네이밍을 사용하면 리턴값이 있을 것으로 기대되는 경우가 많습니다. 🤔 다른 네이밍은 어떨까요?"*

학생 자가 적용: `getXXX` (반환) + `updateXXX` (업데이트) 분리.

→ **명명 일관성** = PR #35 (set prefix vs create prefix) 결 합류.

### 5. 네이밍 컨벤션 (Thread 6) + 외부글

reviewer: 외부글 *kotlinlang.org/docs/coding-conventions.html#property-names*.

→ **Kotlin 공식 컨벤션** = PR #19·#142 결.

### 6. 매직 넘버 상수화 (Thread 14)

reviewer: *"여기도 상수화를 해주면 좋겠어요!"*

학생 자가: *눈에 보이는 매직 넘버 모두 상수화*.

→ **상수화** = 누적 다수.

### 7. `pickCard()` index 예외 (Thread 2)

reviewer: *"`pickCard()`할 때 만약 index가 넘어가게 되면 예외가 발생할 수 있어요. check와 같은 검증 로직을 넣어주면 좋을 것 같아요!"*

→ **카드 부족 처리** = PR #113·#118·#129·#138·#141·#130·#71 결 합류 누적 8+ PR.

## 대표 인용

> [hyemdooly Thread 11]
> *"리뷰 의도는 Participants를 만들라는 것이 아니긴 했습니다 😅 이것도 코드에 리뷰 달아드릴게요~!"*

→ **reviewer 자가 의도 다름 인정**.

> [m6z1 Thread 5]
> *"카드 한 장은 숨기고 플레이어들에게 딜러의 카드를 유추하며 베팅 하게끔 유도하는 것 자체가 블랙잭의 도메인이라고 생각해서 비즈니스 로직으로 수정해보겠습니다."*

→ **블랙잭 도메인 룰 학생 자가 *베팅 유도* 어휘**.

> [m6z1 Thread 8]
> *"카드가 2장 일 때 조건을 찾아 boolean 값을 업데이트 하는 것보다 애초에 상태를 찾을 때 카드 사이즈가 2일 때로 명시하는 것이 좋겠단 생각을 했습니다."*

→ **상태 플래그 vs 도메인 상태 판단 학생 자가 학습**.

## 얻은 교훈 (보편)

1. **학생 자가 코드 self-review 어휘 (Thread 1-4)** — **MCP 룰 `student-self-review-as-comment` 후보**.
2. **베팅 유도 = 블랙잭 도메인 룰 학생 자가 학습** — *블랙잭 정확성 어휘*.
3. **상태 플래그 vs 도메인 상태 판단** = 누적 3+ PR.
4. **Money → Profit 분리** = PR #142·#143·#140 결.
5. **reviewer 자가 의도 다름 인정** = 누적 6+ PR.
6. **CQS 위반 isTurnOver 3 역할** = 누적 5+ PR.

## MCP 룰 후보 신규 + 강화 (PR #144)

| 룰 | 신규 / 강화 | 누적 PR |
|---|---|---|
| `student-self-review-as-comment` | 신규 | 1 (#144) |
| `state-flag-vs-domain-state-judgement` | 강화 | 3+ (#126·#142·#144) |
| `reviewer-self-clarification-explicit` | 강화 | 6+ |
| `command-query-separation` | 강화 | 6+ |
