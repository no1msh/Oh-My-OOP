# PR #102 — [블랙잭 2단계] 벼리 미션

- **Reviewee:** [@gaeun5744](https://github.com/gaeun5744)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/102
- **단계:** 2단계
- **리뷰어:** [@vagabond95](https://github.com/vagabond95) — 1단계 #69 동일 reviewer
- **라인 코멘트:** 13 스레드 — *PR 본문 자가 의문 (공통 로직 묶는 방법 + Dealer/Player playTurn 시그니처 차이) + 학생 자가 의문 (factory deck 의존성, abstract vs interface) + reviewer *추상함수 기준보다 추상 클래스와 인터페이스 사용 기준* 메타 어휘 + 람다 named parameter 권유 + API 일관성 어휘 + 람다 네이밍 = 외부 상황 X 어휘*
- **양 단계 완성 reviewee** (1단계 #69 batch 23 완료 + 2단계 #102 = batch 24).

## 잘한 점 (설계 관점)

### 1. PR 본문 자가 의문 + 시그니처 차이 학습

PR 본문:

> *"`Dealer`와 `Player`를 보면 공통적으로 `playTurn` 로직이 있습니다. 둘의 로직은 비슷하지만, 반환값과 파라미터가 약간 다릅니다. (...) 그래서 부득이하게 `Finish`를 반환하기 어려워서 둘의 로직을 공통으로 묶지 못했는데, 이러한 상황에서는 어떻게 대처해야할지 몰라 질문 드립니다."*

→ **시그니처 차이 추상화 한계 학생 자가 의문** = PR #126·#140·#69 결 합류 누적 4+ PR.

### 2. 학생 자가 abstract vs interface 의문 (Thread 2)

학생:

> *"혹시 해당 구현을 `abstract`가 아닌 `interface`로도 바꿔도 무방한 코드일까요? 저는 추상함수를 사용하는 기준이 다음과 같이 두가지 라고 생각합니다.*
> *- 클래스간 공통된 로직을 묶을 때*
> *- 클래스 간 연간관계가 강할 때 (...) 현재의 로직은 구현부가 없고 시그니처만 존재하고 있기 때문에, 인터페이스를 사용하는 것과 큰 차이점을 못 느끼고 있습니다."*

reviewer:

> *"인터페이스를 사용하는 것과 큰 차이가 없다면, 반대로 다중 상속이 가능하여 비교적 제약이 적은 인터페이스를 사용하지 않을 이유가 있을까요? :)*
>
> *추상함수는 말씀하신 것처럼 인터페이스의 함수 시그니쳐와 동일하므로, 추상함수를 사용하는 기준보다는 추상 클래스와 인터페이스를 사용하는 기준을 잡아보는게 좋을 것 같은데요."*

→ **학생 자가 Kotlin 어휘 깊은 학습 + reviewer 메타 어휘 (기준 재정의)** = PR #129·#126·#138·#142·#143·#113·#19·#69 결 합류 누적 9+ PR.

### 3. 학생 자가 *deck 의존성 vs card 전달* 의문 (Thread 1)

학생:

> *"`Players`와 `Dealer`가 `Deck`을 가지고 있는 상황이 어색하여, 팩토리 함수를 이용해 `gamePlayers`를 초기화 하였습니다. 다만, 이렇게 하는 방식이 과연 옳은지에 관해 의문이 들었습니다."*

reviewer:

> *"deck 을 꼭 주입받아야하는지 고민해보면 좋겠어요. card 만 전달받아도 충분하지 않을까요?"*

학생 자가 적용: *고차함수로 받기*.

→ **deck 의존성 제거 학생 자가 학습** = PR #69 1단계 결의 *2단계 학생 자가 정공*. **MCP 룰 `object-ownership-vs-parameter-passing` 강화** 누적 4+ PR.

### 4. 추가 칭찬

- Thread 8: *"enum 활용 👍"*.

## 못한 점 (설계 관점)

### 1. 람다 named parameter 권유 (Thread 6)

reviewer:

> *"람다를 넘길 때는 named parameter 를 습관해주시면 좋습니다~! 현재와 같은 코드는 어떤 람다인지 구분이 불가능해서 가독성이 많이 떨어질 것 같아요."*

→ **Kotlin named parameter 가독성** = **MCP 룰 `kotlin-named-parameter-readability` 후보**.

### 2. API 일관성 어휘 (Thread 7)

reviewer:

> *"times 함수는 하나만 제공해도 되지 않을까요? 혹은 Float 형도 지원해야하지 않을까요? API 는 일관된 규칙을 가지고 있어, 코드를 쉽게 예측할 수 있으면 좋다고 생각합니다 ㅎㅎ"*

→ **API 일관성 어휘** = **MCP 룰 `api-consistency-criteria` 후보** — *함수 1 개 vs 모든 타입 지원 양쪽 일관성*.

### 3. 람다 네이밍 = 외부 상황 X (Thread 13)

reviewer:

> *"앞으로도 람다의 네이밍을 지을 때 항상 명심하면 좋을 부분입니다. 람다는 외부의 상황을 알지 못하고, 철저히 람다를 사용하는 입장에서 네이밍이 지어져야합니다!"*

→ **람다 네이밍 = 사용 입장 어휘** = *PR #36 (Domain/View 비용 메타포) 결 합류*. **MCP 룰 `lambda-naming-from-user-perspective` 후보**.

### 4. show 알기 model = view 책임 (Thread 9)

reviewer: *"\"show\" 할 것이라는 것을 model 이 알고 있는 것이 어색하지 않을까요?"*

→ **Model→View 분리** = 누적 15+ PR ⭐⭐⭐.

### 5. setResult vs getResult 명명 (Thread 10)

reviewer: *"해당 함수는 gameResult 를 반환하는 것인지 Result 를 Set 하는 것인지 코드를 직접 보기 전까지는 알기가 어려울 것 같아요."*

→ **이름 의미 정확성** = 누적 13+ PR.

### 6. Result 일급 컬렉션 (Thread 11)

reviewer: *"Result 도 별도 클래스로 묶어봐도 좋겠습니다~!"*

→ **일급 컬렉션** = 누적 다수.

### 7. require init 검증 중복 (Thread 12)

reviewer: *"require 문이 중복되는데, 클래스의 init 블록에서 한번에 검증해도 좋겠습니다~!"*

→ **검증 중복 자가 정리** = PR #69·#109 결 합류.

### 8. players turn + dealer turn 함수 명명 (Thread 4)

reviewer: *"players trun 인데 dealer turn 도 진행되는게 어색한 것 같아요. 함수를 분리하거나 네이밍을 수정해야하지 않을까요~?"*

→ **함수 명명 의미 정확성**.

### 9. 매직넘버 BLACKJACK_NUMBER 재활용 (Thread 3)

학생 자가 *매직넘버 제거 까먹음* 인정.

→ **매직넘버 상수화** = 누적 다수.

### 10. 상태 = 파라미터 받지 말고 멤버 (Thread 5)

reviewer: *"상태로 가지고 있으므로, 파라미터로 받을 필요가 없지 않을까요?"*

→ **parameter vs self state access** = PR #140·#143 결 합류.

## 대표 인용

> [vagabond95 Thread 2]
> *"추상함수를 사용하는 기준보다는 추상 클래스와 인터페이스를 사용하는 기준을 잡아보는게 좋을 것 같은데요."*

→ **reviewer 메타 어휘 (기준 재정의)**.

> [vagabond95 Thread 13]
> *"람다는 외부의 상황을 알지 못하고, 철저히 람다를 사용하는 입장에서 네이밍이 지어져야합니다!"*

→ **람다 네이밍 사용 입장 어휘**.

> [vagabond95 Thread 7]
> *"API 는 일관된 규칙을 가지고 있어, 코드를 쉽게 예측할 수 있으면 좋다고 생각합니다"*

→ **API 일관성 어휘**.

## 얻은 교훈 (보편)

1. **시그니처 차이 추상화 한계** = 누적 4+ PR.
2. **추상 클래스 vs 인터페이스 기준** = 누적 9+ PR.
3. **람다 named parameter 가독성** — **MCP 룰 `kotlin-named-parameter-readability` 후보**.
4. **API 일관성 어휘** — **MCP 룰 `api-consistency-criteria` 후보**.
5. **람다 네이밍 = 사용 입장** — **MCP 룰 `lambda-naming-from-user-perspective` 후보**.
6. **deck 의존성 제거 학생 자가 학습 시간성 (PR #69 → #102)** = *학습 시간성 정공*.
