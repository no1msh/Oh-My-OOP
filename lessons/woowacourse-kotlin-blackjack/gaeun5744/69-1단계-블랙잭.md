# PR #69 — [블랙잭 1단계] 벼리 미션

- **Reviewee:** [@gaeun5744](https://github.com/gaeun5744)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/69
- **단계:** 1단계
- **리뷰어:** [@vagabond95](https://github.com/vagabond95) — 2023 코호트 (racingcar 인용 reviewer) + 2024 reviewer
- **라인 코멘트:** 20 스레드 — *Domain ↔ View 분리 5+ 자리 + 학생 자가 *type-safe enum class* 의문 + CardMachineManager 인터페이스 학생 자가 *확장성* 명시 + handle 이름 정확성 + Judge 객체 vs Dealer 책임 4 라운드 깊은 양방향 토론 + reviewer *현실 세계 투영 = 필수 X, 필요할 때 사용 도구* 메타 어휘 + 상태 패턴 자가 적용*
- **신규 reviewee** (2024 코호트, 새 1단계 분석).

## 잘한 점 (설계 관점)

### 1. 학생 자가 *type-safe enum class* 의문 (Thread 1)

학생:

> *"그렇다면 문자열이 포함된 `enum class`들은 따로 util 패키지에 두는게 맞을까요...? 문자열을 없앨까 라는 생각도 들었지만, 이는 상수를 `type-safe`하게 다루는 enum class의 장점을 활용하지 못한다는 생각이 들어서 패키지를 옮기는 쪽으로 생각했습니다!!"*

→ **학생 자가 *type-safe* + *enum class 장점 활용* 어휘** = *Kotlin 어휘 깊은 자가 학습*. **MCP 룰 `student-self-type-safe-vocabulary` 후보**.

reviewer 자가 정정:

> *"CompetitionResult 를 enum 으로 구현하고, 이것이 model 패키지에 있는 것은 자연스럽다고 생각해요. 제가 코멘트를 남겼던 것은 enum 자체가 문제가 된다기보다 model 에서 view 에서 사용될 값을 알고 있는 것이 어색해보였어요."*

→ **reviewer 자가 의도 명료화** = PR #142 결 합류.

### 2. CardMachineManager 인터페이스 + 학생 자가 *확장성* 명시 (Thread 2)

학생 자가 답변:

> *"`ShuffleCardMachine` 및 `BlackjackCardMachine` 등의 상태를 싱글톤으로 만드는게 문맥상 옳다고 생각해서 해당 `CardMachineManager object`를 만들었습니다. 만약 예시의 클래스들을 그냥 object로 만들 경우, 해당 클래스에 직접 접근해서 다른 Deck 클래스를 만든 후, 수정해야 하기 때문에 Deck 객체의 확장성이 저해된다고 생각했습니다."*

학생 자가 후속: `shuffle` → `handle` 이름 자가 변경.

reviewer 칭찬: *"handle 로 수정되니, 구현의도가 명확해진 것 같아요. 카드를 분배하는 방식을 인터페이스로 유연하게 다루는 것은 좋은 시도 같습니다 👍"*

→ **학생 자가 *유연성* 의식 + 자가 명명 정정** + **인터페이스 = 카드 분배 방식 유연** = PR #138·#142·#143 결 합류.

### 3. Judge 객체 vs Dealer 책임 — 4 라운드 깊은 양방향 토론 (Thread 12)

학생 자가 답변:

> *"따로 `Judge`라는 객체를 둘까라는 생각도 했지만, 현실 세계에 빗대어 봤을때 문맥이 어색해 보여서 `Dealer` 객체에 두었습니다! 왜냐하면 실제로 승패를 판단하는건 딜러이며, 딜러가 승패 결과에 따라 칩을 직접 놔눠주기 때문에 추후 리팩토링을 위해서 `Dealer` 객체에 두었습니다.*
>
> *또한, 원래부터 딜러라는 역할 자체가 책임이 많은 역할이라는 생각에 무의식중에 괜찮지 않을까? 라는 생각이 들었습니다.*
>
> *제리는 책임이 많은 역할에 대해 어떻게 생각하시나요? 현실 세계에서는 책임이 많은 역할이더라도, 프로그래밍 세계에서는 역할을 책임을 쪼개서 새로운 객체를 탄생시키는게 옳다고 생각하시나요?"*

reviewer (메타 어휘):

> *"벼리의 말도 일리가 있다고 생각해요. 도메인을 어떻게 해석하느냐에 따라 관점을 다르게 볼 수 있겠네요!*
>
> *좋은 질문이고, 굉장히 어려운 내용이라고 생각해요. 저도 잘 지키고 있는지 모르겠네요 ㅎㅎ;*
> *저 같은 경우 현실세계의 도메인을 잘 투영하려고 노력하지만, 읽고 유지보수하기 쉬운 코드인가? 를 더 중요하게 생각하고 구현하는 것 같아요.*
>
> *읽고 유지보수하기 쉬운 코드는 기본적으로 수행하는 역할이 명확하고, 잘 분리되어야 한다고 생각해서 위와 같은 코멘트를 남기게 되었네요 ㅎㅎ*
>
> *벼리의 명확한 기준이 있다면, 우선 그 방향으로 진행해보고 추후에 구현이 막힐"*

학생 자가 후속 의문:

> *"그렇다면 제리는 역할과 책임이 명확하고 읽기 쉬운 코드를 작성하는데 있어서, 현실 세계의 도메인을 투영하는 것은 필수가 아니라 필요할때 사용하는 도구라 생각하시나요?*
>
> *사실 현실 세계를 투영한다고 해도, 다양한 정렬 방식을 제공하는 `CardMachine`이 실제로는 존재 하지 않지만 유지보수성을 위해 추가한 것처럼, 어디까지 현실 세계를 의인화할지 아직도 고민입니다."*

→ **현실 메타포 vs 유지보수성 트레이드오프 깊은 메타 어휘** = *PR #117·#129·#130·#143·#109·#113·#118·#71·#96 (현실 메타포 정합) 결의 *반대 시점 (학생 자가 메타 의문)***. **MCP 룰 `metaphor-fidelity-vs-maintenance-tradeoff` 후보** — *현실 메타포 = 도구 vs 필수* 학생 자가 메타 의식.

PR #118 (현실 메타포 vs SRP 트레이드오프) 결의 *학생 자가 깊은 의문* 어휘 합류.

### 4. 상태 패턴 자가 적용 (Thread 17)

reviewer: *"결과를 판단하는 조건식이 복잡한데요, 조건식의 순서도 중요하고 조금만 요구사항이 추가되어도 이해하기가 쉽지 않을 것 같아요 😢 객체지향의 다형성을 이용하여 코드를 좀 더 간결하게 만들어보는 방법은 없을까요?"*

학생: *"상태 패턴을 이용해서 코드를 좀 더 간결하게 해봤습니다...!!"*

→ **State 패턴 자가 적용** = PR #93·#136·#152·#98·#36·#87·#142·#143·#138·#153 결 합류 누적 11+ PR.

### 5. 추가 칭찬

- Thread 18: *"확장함수 활용 👍 패키지도 view 에 잘 선언해주셨네요!"*.
- Thread 19: *"이전보다 훨씬 코드가 간결해졌네요 👍"*.

## 못한 점 (설계 관점)

### 1. Domain ↔ View 분리 5+ 자리 (Thread 1·3·6·7·8)

reviewer 가 5+ 자리에서 *view 값 (result/value/shape/이름 출력 함수)* 도메인 외부 권유.

→ **MVC 원칙 정공** = PR #126·#143·#149·#19·#35·#68 결 합류 누적 15+ PR ⭐⭐⭐.

### 2. Pattern 명명 = 정규식 충돌 (Thread 6)

reviewer: *"Pattern 은 정규식에 사용되는 클래스의 이름이기도 해서 좀 더 구체적인 네이밍이 들어가면 좋겠습니다 :)"*

→ **Kotlin/Java 표준 라이브러리 명명 충돌** = PR #130 (Number vs kotlin.Number), PR #142 (Result vs kotlin.Result) 결 강화 누적 3+ PR.

### 3. HandCards 가 Deck 가짐 자연성 X (Thread 5·16)

reviewer: *"HandCards 는 플레이어가 소지하고있는 카드를 표현하는 도메인으로 보이는데요, 전체 카드 덱을 같이 소지하고 있는 부분이 어색한 것 같아요. 필요할 때만 카드를 전달받는것이 도메인의 흐름상 좀 더 자연스럽지 않을까요?"*

→ **카드 전달 vs Deck 소유** = PR #96·#138 결의 *손이 덱과 카드를 동시에 아는가* 어휘 합류.

### 4. Player.deck 멤버 → 카드 전달 (Thread 13)

reviewer:

> *"Player, Dealer 가 deck 을 가지고 있는 이유는 card 를 추가해주기 위함인데요. deck 을 멤버로 가지고 있기보다, 필요시 card 만 전달받는 것은 어떨까요?*
>
> *현 구조에서는 테스트시에도 deck 을 매번 주입해줘야하는데, deck 에 대한 의존성을 줄여봐도 좋을 것 같습니다."*

학생 자가 적용: *팩토리 함수 통해 deck 전달*.

→ **객체 소유 vs 매개변수 전달** = PR #138 (rosemin928 *deck 매개변수 전달 = 소유 X*) 결 합류.

### 5. isWantAddCard 외부 의존 (Thread 14)

reviewer:

> *"isWantAddCard 여부를 Player 가 꼭 알 필요가 있을까요? 해당 구현은 외부에 의한 조건인데, `카드가 주어지면 더한다.` 의 역할이 더 간결하고 명확할 것 같아요."*

학생 자가 답변:

> *"메시지는 최대한 추상적이고 명료해야 하는데, 그 법칙을 제대로 지키지 못한것 같네요ㅠㅠ 지적해주셔서 감사합니다!!"*

→ **메시지 추상화 어휘 + Tell-Don't-Ask** = PR #129·#142·#143 결 합류.

### 6. GameParticipant.addCard 추상화 미적용 (Thread 15)

reviewer: *"addCard 도 필수적인 함수로 보이는데 상위 클래스에서는 선언되지 않은 이유가 무엇일까요?"*

학생: *"`Dealer` 객체와 `Player` 객체의 시그니처가 달라서 넣지 않았습니다ㅠ"*

→ **시그니처 차이 추상화 한계** = PR #126·#140 결 합류. 학생 자가 수정.

### 7. Players init 검증 중복 (Thread 10)

reviewer: *"해당 검증은 내부의 init 블록이 아니라 별도함수로 분리해서 검증하는 이유가 있을까요?"*

학생: *"`validateDuplicateNames`을 제거하면, `names.toSet()`에서 저절로 중복이 제거된 채 객체가 생성되기 때문에 추가했습니다! 지금 다시 보니 init 블록 내 검증과 겹치는거 같아서 이쪽 검증을 제거하겠습니다!"*

→ **검증 중복 자가 인지** = PR #109 (validation-function-vs-require-inline) 결 합류.

### 8. Deck.draw 리턴 List<Card> 확장성 (Thread 4)

reviewer: `removeFirst()` 활용 권유.

학생 자가 답변:

> *"해당 `draw`메서드의 반환 타입이 `List<Card>`라서 어쩔 수 없이 여러 줄로 작성했습니다ㅠ 처음 블랙잭 게임을 시작할 때 카드를 두장 뽑는데, 이때 같은 메서드를 두번 사용하기 보다는 파라미터로 뽑고자 하는 카드의 개수를 전달하는 것이 확장성에 좋을것 같아서 이렇게 구현했습니다!!"*

→ **학생 자가 *확장성* 의식** = *PR #138·#142 결의 *유연성 vs 현재 룰* 어휘 합류*.

### 9. InputView 분리 기준 (Thread 11)

학생 자가 답변:

> *"다음과 같이 두가지 기준으로 뷰를 분리했습니다.*
> *1. 객체를 생성해서 던질 수 있는가 (도메인 객체를 단위로 해서 뷰를 분리했습니다.)*
> *2. 비슷한 함수끼리 엮을 수 있는가"*

→ **학생 자가 분리 기준 명시** = *PR #126·#130·#117 결의 *학생 자가 분류* 합류*.

### 10. `playerInput` 모호 명명 (Thread 9)

reviewer: *"playerInput 은 너무 이름이 모호한 것 같아요. 좀 더 구체적인 네이밍으로 지어볼 수 있을까요?"*

→ **이름 의미 정확성** = 누적 13+ PR.

### 11. Player 의 상태 자가 결정 (Thread 20)

reviewer: *"상태가 스스로 다음 상태를 결정하도록 리펙토링 해봐도 좋을 것 같습니다!"*

→ **State 패턴 자식 자가 결정** = PR #93·#136·#152·#98 결 합류.

## 대표 인용

> [vagabond95 Thread 12]
> *"저 같은 경우 현실세계의 도메인을 잘 투영하려고 노력하지만, 읽고 유지보수하기 쉬운 코드인가? 를 더 중요하게 생각하고 구현하는 것 같아요."*

→ **현실 메타포 vs 유지보수성 메타 어휘**.

> [gaeun5744 Thread 12]
> *"그렇다면 제리는 역할과 책임이 명확하고 읽기 쉬운 코드를 작성하는데 있어서, 현실 세계의 도메인을 투영하는 것은 필수가 아니라 필요할때 사용하는 도구라 생각하시나요?"*

→ **학생 자가 *현실 메타포 = 도구* 메타 의문**.

> [gaeun5744 Thread 1]
> *"이는 상수를 `type-safe`하게 다루는 enum class의 장점을 활용하지 못한다는 생각이 들어서"*

→ **학생 자가 *type-safe* Kotlin 어휘**.

> [gaeun5744 Thread 14]
> *"메시지는 최대한 추상적이고 명료해야 하는데, 그 법칙을 제대로 지키지 못한것 같네요ㅠㅠ"*

→ **학생 자가 *메시지 추상화* 메타 학습**.

## 얻은 교훈 (보편)

1. **현실 메타포 vs 유지보수성 트레이드오프** — **MCP 룰 `metaphor-fidelity-vs-maintenance-tradeoff` 후보**.

2. **학생 자가 *type-safe* / *확장성* / *메시지 추상화* Kotlin/OOP 어휘** — **MCP 룰 `student-self-type-safe-vocabulary` 후보**.

3. **Kotlin/Java 표준 라이브러리 명명 충돌 (Pattern)** — 누적 3+ PR.

4. **카드 전달 vs Deck 소유** — PR #96·#138 결 누적 3+ PR.

5. **시그니처 차이 추상화 한계 → 학생 자가 수정** = 누적.

6. **검증 중복 자가 인지** = PR #109 결.

7. **상태 패턴 자가 적용 (조건식 복잡 → 다형성)** = 누적 11+ PR.

8. **InputView 분리 기준 학생 자가 명시** = 누적.

## MCP 룰 후보 신규 + 강화 (PR #69)

| 룰 | 신규 / 강화 | 누적 PR |
|---|---|---|
| `metaphor-fidelity-vs-maintenance-tradeoff` | 신규 | 1 (#69) |
| `student-self-type-safe-vocabulary` | 신규 | 1 (#69) |
| `class-name-vs-stdlib-collision` | 강화 | 3+ (#130·#142·#69) |
| `model-view-dependency-direction` | 강화 | 15+ ⭐⭐⭐ |
| `state-children-method-vs-external-when` | 강화 | 11+ |
| `object-ownership-vs-parameter-passing` | 강화 | 3+ |
| `function-name-vs-return-semantic-mismatch` | 강화 | 13+ |
