# PR #149 — [블랙잭 2단계] 오이 미션

- **Reviewee:** [@cucumber99](https://github.com/cucumber99)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/149
- **단계:** 2단계
- **리뷰어:** [@hyemdooly](https://github.com/hyemdooly) (둘리) — 1단계 #109 동일 reviewer
- **라인 코멘트:** 13 스레드 — *PR 본문 자가 의문 (수익 계산 책임) + 학생 자가 의견 + Sequence Kotlin 어휘 자가 학습 + reviewer 자가 *뭉치 코드와 같은 의도* + reviewer 자가 *제가 작성한 코드는 ... 문제가 없었던거였어요* 자가 학습 정점 + lateinit var 자가 학습 + 첫 턴 딜러 카드 비즈니스 로직 (학생 자가 *명확한 답 힘들다* 인정)*
- **양 단계 완성 reviewee** (1단계 #109 batch 21 분석 완료 + 2단계 #149 = batch 23 추가).

## 잘한 점 (설계 관점)

### 1. reviewer 자가 학습 어휘 정점 (Thread 12)

reviewer:

> *"두 패를 비교하는 것을 의도했고, 제가 생각한대로 되는지 코드도 작성해봤었는데요! (현재 뭉치의 코드가 제 리뷰 의도와 똑같아요. 궁금하시면 구경가보셔도..?! 👀)*
>
> *제가 작성한 코드는, 딜러의 수익금을 계산할 때 플레이어의 결과 수익금의 합에 -1를 곱한 형태라 문제가 없었던거였어요. 😮 플레이어의 결과만 계산에 사용하는 형태라, Hand 로직이 플레이어 기준이었네요. Hand만 놓고보면 플레이어 기준 로직이기에, 어떻게 보면 어색한 코드였겠군요..?!*
>
> *오이 덕분에 놓친 부분을 알았네요. 😅 의견 나누어주셔서 감사합니다!! 👍"*

→ **reviewer 자가 학습 + 학생에게 감사 어휘 + 다른 학생 코드 cross-reference (뭉치)** = *PR #126·#130·#142·#109 (reviewer 자가 한계 인정) 결 정점*. **MCP 룰 `reviewer-self-learning-from-student` 후보** — *reviewer 가 학생 의견에서 자가 학습*.

### 2. Sequence Kotlin 어휘 학생 자가 학습

reviewer (Thread 5): *"Sequence로 변환해주셨네요! Sequence는 어떨 때 사용하면 좋을까요?"*

학생:

> *"수업에서 배운 것처럼, 상태가 없는 연산(map, filter 등)이 자주 일어날 때 사용하면 좋습니다. 위 코드에서는 map, flatMap이 사용되고 있기 때문에 한 번 적용해보고자 했습니다!"*

→ **Sequence 자가 적용** = *Kotlin 어휘 깊은 학습*. **MCP 룰 `sequence-vs-collection-criteria` 후보**.

### 3. value class BettingAmount 칭찬 + ResultState.profitRate 정의 (Thread 3·13)

reviewer: *"원시값들을 모두 포장해주었네요 👍"* + *"ResultState에 profitRate를 정의해주었군요 👍"*.

→ **value class + enum 책임 강화** = PR #138·#142·#143 결.

### 4. 모든 13 스레드 학생 자가 답변 + 자가 의문 4+ 회 (2025 코호트 정점)

## 못한 점 (설계 관점)

### 1. 수익 계산 책임 = 외부 객체 vs 참가자 자가 (PR 본문 자가 의문)

학생 자가 답변:

> *"`List<Card>`를 플레이어와 딜러가 갖고 있으니, 수익금 계산이나 승패 결정도 내부에서 할 수 있겠다고 생각했어요. 다만, 그렇게 되면 플레이어와 딜러가 너무 많은 책임을 갖게 되는 것이 아닌가 고민되는데요🤔"*

reviewer:

> *"단일책임원칙(SRP)에 대해 공부해보시면 좋을 것 같아요! 어떻게 설계하면 한 클래스가 변경되었을 때 다른 클래스가 같이 변경을 안해도 될지 고민해보세요😊"*

→ **SRP + 변경 영향 어휘** = PR #126 (change-impact-as-locus-criteria) 결 합류.

### 2. GameRule 상수 분리 vs 도메인 분산 (PR #109 결 재발)

학생 자가 의문 + reviewer 답변 — *PR #109 1단계 결과 같은 의문 재발*. **MCP 룰 `constant-aggregation-vs-domain-distribution` 강화** 누적 2+ PR (#109·#149).

### 3. 첫 턴 딜러 카드 = 비즈니스 로직 (PR #126·#142 결 재발)

학생: *"`카드 한 장을 보여주지 않는 것은 누구의 책임일까?`에 대해서 크루들과 얘기를 나눈 적이 있어요! 사실 아직도 명확한 답을 내리기가 힘든 것 같은데, 제가 생각하기에는 비즈니스 로직에 조금 더 가까운 것 같아요."*

→ **학생 자가 *명확한 답 힘들다* 메타 학습** = *학습 시간성*. PR #126·#142·#129 결 합류.

### 4. `canHit() = !isBust()` 블랙잭 처리 X (Thread 7)

reviewer: *"블랙잭일 때도 false여야하지 않나요?"*

학생: *"`Game` 객체를 사용하고 있을 때는 블랙잭인 경우를 처리해주고 있었기 때문에 차마 신경쓰지 못한 부분인 것 같아요."*

→ **1단계 → 2단계 회귀 자가 인지** = PR #139 (abs 자가 정정) 결 합류.

### 5. BettingAmount.calculateProfit(ResultState/profitRate) (Thread 8) — Tell-Don't-Ask

reviewer: *"BettingAmount한테, ResultState 또는 비율을 받아 계산하고 ProfitAmount을 리턴하게 하는 것은 어떨까요?"*

→ **value class 책임 강화** = PR #143·#142·#140 결 합류.

### 6. `Participant.visibleCard()` 추상화 + `this is Dealer` 제거 (Thread 9)

reviewer: *"`Participant`의 `visibleCard()`를 추상화해서, 플레이어와 딜러 각각 따로 구현해주고 `this is Dealer` 로직을 삭제할 수 있어보여요."*

→ **다형성 + `is` 연산자 회피** = PR #87 (is-operator-polymorphism-avoidance) 결 합류 누적 7+ PR.

### 7. BettingInfo vs Player.bettingAmount 양방향 토론 (Thread 10)

reviewer:

> *"플레이어가 베팅 금액을 가지고 있지 않은 이유가 있으신가요? 코멘트로 이야기했던대로 내부에서 처리하면 더 객체가 일한다는 느낌을 줄 수 있을 것 같아서요. 저는 플레이어가 베팅 금액을 가지고 있어도 괜찮다는 의견인데(BettingInfo를 묶어두고 하는 일이 없기도 하구요), 오이의 생각은 어떠신지 궁금합니다!"*

학생 자가 답변:

> *"`lateinit var` 키워드를 사용하면, 이름을 다 입력받고 난 후 베팅 금액을 따로 받을 수 있겠다고 생각했는데요. 다만 `lateinit` 키워드를 사용해서 발생할 수 있는 문제점도 생각했어요. (초기화되지 않은 값에 접근 등)"*

→ **BettingInfo 분리 vs Player 멤버 + lateinit 자가 학습** = PR #138 (BlackJackTable 형식 규칙 부산물 자가 비판) 결 합류.

### 8. Dealer.profitOfPlayer 명명 부적합 (Thread 11)

reviewer: *"Dealer 클래스만 봤을 때 이 함수는 딜러의 수익을 리턴할 것 같았는데 속은 플레이어의 수익이군요 🤔"*

→ **이름 의미 정확성** = 누적 11+ PR.

### 9. Hand 책임 vs Participant 책임 — reviewer 자가 학습 (Thread 12)

§1 참조. **MCP 룰 `reviewer-self-learning-from-student` 후보** — *reviewer 의 자가 학습 정점*.

### 10. `lateinit` 학생 자가 학습 + 단점 인지

학생이 *`lateinit` 단점 (초기화되지 않은 값 접근)* 자가 인지 = **MCP 룰 `lateinit-var-call-order-invariant` 강화** 누적 12+ PR.

## 대표 인용

> [hyemdooly Thread 12]
> *"오이 덕분에 놓친 부분을 알았네요. 😅 의견 나누어주셔서 감사합니다!! 👍"*

→ **reviewer 자가 학생 의견 학습** — *MCP 핵심 가치 정점*.

> [hyemdooly Thread 1]
> *"단일책임원칙(SRP)에 대해 공부해보시면 좋을 것 같아요! 어떻게 설계하면 한 클래스가 변경되었을 때 다른 클래스가 같이 변경을 안해도 될지 고민해보세요😊"*

→ **SRP + 변경 영향 어휘**.

> [cucumber99 Thread 6]
> *"사실 아직도 명확한 답을 내리기가 힘든 것 같은데, 제가 생각하기에는 비즈니스 로직에 조금 더 가까운 것 같아요."*

→ **학생 자가 *명확한 답 힘들다* 메타 학습**.

## 얻은 교훈 (보편)

1. **reviewer 자가 학습 (학생 의견 → reviewer 학습)** — **MCP 룰 `reviewer-self-learning-from-student` 후보**.
2. **Sequence vs Collection (Kotlin 어휘)** — **MCP 룰 `sequence-vs-collection-criteria` 후보**.
3. **`lateinit var` 단점 자가 인지** — PR #138·#142·#141 결 강화 누적 12+ PR.
4. **GameRule 상수 분리 1단계 → 2단계 재발** — PR #109·#149 결 합류.
5. **Hand 책임 양방향 토론 + reviewer 자가 학습** — *학생 ↔ reviewer 양방향 정점*.
6. **첫 턴 딜러 카드 비즈니스 로직 = 학생 자가 *명확한 답 X* 메타 학습** — *학습 시간성*.

## MCP 룰 후보 신규 + 강화 (PR #149)

| 룰 | 신규 / 강화 | 누적 PR |
|---|---|---|
| `reviewer-self-learning-from-student` | 신규 | 1 (#149) |
| `sequence-vs-collection-criteria` | 신규 | 1 (#149) |
| `lateinit-var-call-order-invariant` | 강화 | 12+ |
| `constant-aggregation-vs-domain-distribution` | 강화 | 2+ (#109·#149) |
| `is-operator-polymorphism-avoidance` | 강화 | 7+ |
| `function-name-vs-return-semantic-mismatch` | 강화 | 11+ |
