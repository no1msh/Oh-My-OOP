# PR #50 — [코비] 2단계 블랙잭

- **Reviewee:** [@rhkrwngud445](https://github.com/rhkrwngud445) (코비)
- **PR:** https://github.com/woowacourse/kotlin-blackjack/pull/50
- **단계:** 2단계
- **리뷰어:** [@malibinYun](https://github.com/malibinYun) (말리빈)
- **라인 코멘트:** 8 스레드 — *학생 자가 답변 3 thread + Effective Kotlin Item 4·14 외부 서적 인용 + 1단계 리뷰 연장선 + Deck 전략 생성자 주입 + BlackJack 승리 vs 일반 승리 책임 분리 + assertAll 효율*
- **rhkrwngud445 양 단계 완성** (1단계 #17).

## 잘한 점 (설계 관점)

### 1. PR 본문 — 1단계 피드백 미이해 자가 질문

PR 본문:

> *"이전 피드백 반영하였습니다! 다만 해당 피드백에서 \"다른 인스턴스를 만들 수 있는 구조\"를 잘 이해 못하였습니다😥 **어떻게 접근해야 할지 참고할 예시나 키워드 부탁드려도 괜찮으실까요..?!**"*

→ **`student-self-vocabulary-keyword-request`** ⭐ — 학생 자가 *이해 못한 부분 키워드 요청* 어휘.

### 2. assertAll 효율 자가 답변 (Thread 7)

reviewer: *"assert 구문을 나열하는 것과, assertAll로 감싸는 것은 어떤 차이점이 있을까요?"*

학생 자가 답변: *"assertAll은 이전 테스트가 틀렸어도 어디서 값이 또 다른지 알 수 있습니다! 테스트 수가 많아지면 효율적일 것 같습니다!"*

→ **MCP 룰 `assertall-vs-sequential-assert-tradeoff`** ⭐⭐ 신규 — *assertAll = 다른 검증 계속 + 어디서 또 틀렸는지 동시 확인*. PR #42 (`assertj-soft-assertion-for-long-tests`) 결 합류 누적 2 PR.

## 못한 점 (설계 관점)

### 1. Effective Kotlin Item 4·14 외부 서적 인용 (Thread 1) — 정점

reviewer:

> *"멤버변수와 메서드의 리턴타입은 모두 명시해주는 것은 어떨까요? 명시해주는 것과 그렇지 않은 것은 어떤 차이점이 생길까요?*
>
> *먼저 고민해보시고, 아래 Effective Kotlin 서적의 챕터를 참고해서 스스로 생각해본 것과 비교해보아요 😀*
>
> *- **Item 4 : `Inferred Type으로 리턴하지 말라`***
> *- **Item 14 : `변수 타입이 명확하지 않은 경우 확실하게 지정하라`***"*

학생 자가 답변:

> *"Inferred Type일 때 super 혹은 interface 형태로 리턴되지 않기 때문에, 제 코드를 직접 조작하지 않는 이상 외부에서 코드를 조작하기 어렵습니다. 또한 코드 내부로 들어가서 타입을 확인을 해주어야 하는 비용이 발생하기 때문에, 코드를 짧게 작성하는 것보다 이점이 많다고 생각됩니다!!"*

→ **MCP 룰 `effective-kotlin-inferred-type-reference`** ⭐⭐⭐ **신규** — *Effective Kotlin Item 번호 명시 + 학생 자가 학습 비교 어휘*. PR #154 (조영호)·PR #150 (로버트 마틴)·PR #62 (LISP)·PR #136 (엘레강트 오브젝트) 결 합류 → ***외부 서적 인용 누적 5 PR*** ⭐⭐⭐ 정점 강화.

→ **`reviewer-book-chapter-citation`** ⭐⭐⭐ — *reviewer 서적 챕터 명시 + 학생 자가 스스로 생각 후 비교* 어휘.

### 2. Deck 전략 패턴 생성자 주입 (Thread 2)

reviewer:

> *"deck을 변수로 두어 계속 새 List를 할당하는 것 보다, 한 MutableList에 카드를 추가/삭제하게 구현해보세요. **makeRandomDeck()은, 모든 덱은 랜덤하다는 전제가 깔려있어 보여요.** Deck 객체를 생성할 때, **카드를 채우는 전략을 생성자로부터 받아보게** 만들어보세요 :)"*

→ **MCP 룰 `deck-fill-strategy-via-constructor`** ⭐⭐⭐ 신규 — *랜덤 가정 X + 카드 채우기 전략 = 생성자 주입*. PR #30 (Generator interface)·PR #154 (`factory-with-test-only-variant`) 결 합류 누적 3 PR.

### 3. 딜러 = 생성자 / 플레이어 = 실행 시 주입 (Thread 0)

reviewer:

> *"사이드 이팩트를 발생시키기 보다, 게임을 실행할 때 딜러와 플레이어를 받는 것은 어떨까요? **딜러는 변하지 않으니, 여전히 생성자에 놔둬도 괜찮아 보이네요** :)"*

→ **MCP 룰 `dealer-constructor-vs-player-runtime-injection`** ⭐⭐⭐ 신규 — *불변 의존 = 생성자 / 변경 가능 = 실행 시 주입*. PR #38 (`mutable-member-var-state-dependent-result`)·PR #154 (`construction-time-vs-mutation-time-invariant`) 결 합류 누적 3 PR.

### 4. 일급 컬렉션 + Mutable 내부 + toList 반환 (Thread 5) — 1단계 연장선

reviewer (PR #17 리뷰 연장):

> *"현재 리스트를 반환할 수 있습니다. **mutableList.toList()로 복사해서 반환이 가능합니다.** 컬렉션에 담기는 객체가 불변이라면 반환하는 객체는 안전하겠지요 🙂*
>
> *일급 컬렉션 내부에서 관리하는 컬렉션을 Mutable로 해도, 외부에서의 수정 등을 막을 수 있습니다. 어떤 이유로 불가능하다고 생각하셨을까요?_?"*

학생 자가 답변:

> *"프로퍼티로 둘 생각을 못했었네요.. **위의 덱 리뷰와 같이 프로퍼티로 두어 외부적으로 불변한 객체를 생성할 수 있다는 것을 간과했었습니다..**😭"*

→ **MCP 룰 `firstclass-collection-mutable-inner-with-tolist-return`** ⭐⭐ 신규 — *일급 컬렉션 = Mutable 내부 + `toList()` 외부 = 캡슐화 + 성능*. PR #42·#47 결 합류.

→ **MCP 룰 `student-cross-pr-feedback-continuity`** ⭐⭐ — *1단계 PR 리뷰 링크 + 2단계 연장선* — PR #97·#154·#101 결 합류.

### 5. BlackJack 승리 vs 일반 승리 책임 분리 (Thread 6)

reviewer:

> *"BlackJack으로 승리한 것과, 일반적으로 승리한 것은 정말 다른 종류의 승리일까요? **요구사항으로 규칙이 여러 추가된다면, 여러 종류의 DRAW, WIN 등이 더 생겨날 수 있어보여요.** 그렇다면 이들은 정말로 Result 하나만을 나타내는 걸까요? 다른 책임을 가지고 있는 것은 아닐지 고민해보아요 🙂"*

→ **MCP 룰 `bjackjack-vs-normal-win-different-responsibility`** ⭐⭐⭐ 신규 — *Result enum = 미래 규칙 추가 시 책임 분리 시그널*. PR #134 (`score-vs-blackjack-score-locus`)·PR #97 (`branch-multiplication-as-test-burden-signal`) 결 합류 — *enum 책임 분리 어휘*.

### 6. public vs val 가시성 (Thread 3)

reviewer: *"현재 players의 가시성을 public으로 공개하는 것과 크게 다르지 않아보이네요 :)"*

→ **가시성 = 의도 명시** = 누적.

### 7. 총합 중복 계산 (Thread 4)

reviewer: *"총합을 중복으로 계산하네요 :) 한 번 만 계산하게 변경해보세요."*

→ **중복 계산 = 캐싱/지역변수** = 누적.

### 8. reviewer 마무리 — 기한 초과 머지

reviewer:

> *"미션 기한이 지나 아쉽게도 바로 머지해야해요 ㅠ_ㅠ. 다음 미션에는 기한을 꼭 지켜서 리뷰 받으시길 바라요 😥. 그래도 저와 함께 미션을 진행하면서 고민했던 것들이 미래에 도움이 되셨으면 좋겠어요!!"*

→ **`reviewer-deadline-miss-merge-vocabulary`** ⭐⭐ — *기한 초과 = 머지 + 미래 도움 어휘*. PR #134·#97 결 합류 누적 3 PR — *reviewer 시간 한계 어휘 정점*.

## 대표 인용

> [malibinYun Thread 1]
> *"아래 Effective Kotlin 서적의 챕터를 참고해서 스스로 생각해본 것과 비교해보아요 😀. Item 4 : `Inferred Type으로 리턴하지 말라`. Item 14 : `변수 타입이 명확하지 않은 경우 확실하게 지정하라`"*

→ **Effective Kotlin 챕터 명시 + 스스로 생각 후 비교 어휘**.

> [malibinYun Thread 0]
> *"딜러는 변하지 않으니, 여전히 생성자에 놔둬도 괜찮아 보이네요"*

→ **불변 = 생성자 / 가변 = 실행 시 주입 어휘**.

> [malibinYun Thread 6]
> *"BlackJack으로 승리한 것과, 일반적으로 승리한 것은 정말 다른 종류의 승리일까요?"*

→ **enum 책임 분리 시그널 어휘**.

> [rhkrwngud445 Thread 5]
> *"프로퍼티로 둘 생각을 못했었네요.. 위의 덱 리뷰와 같이 프로퍼티로 두어 외부적으로 불변한 객체를 생성할 수 있다는 것을 간과했었습니다.."*

→ **학생 자가 *간과* 자가 자각 어휘**.

## 얻은 교훈 (보편)

1. **Effective Kotlin Item 챕터 인용 (외부 서적 누적 5 PR)** — **MCP 룰 `effective-kotlin-inferred-type-reference` + `reviewer-book-chapter-citation` 후보** ⭐⭐⭐ 정점.
2. **불변 = 생성자 / 가변 = 실행 시 주입** — **MCP 룰 `dealer-constructor-vs-player-runtime-injection` 후보** ⭐⭐⭐.
3. **Deck 전략 생성자 주입** — **MCP 룰 `deck-fill-strategy-via-constructor` 후보** ⭐⭐⭐.
4. **enum 책임 분리 시그널** — **MCP 룰 `bjackjack-vs-normal-win-different-responsibility` 후보** ⭐⭐⭐.
5. **일급 컬렉션 + Mutable 내부 + `toList()` 반환** — **MCP 룰 `firstclass-collection-mutable-inner-with-tolist-return` 후보** ⭐⭐.
6. **assertAll vs sequential assert** — **MCP 룰 `assertall-vs-sequential-assert-tradeoff` 후보** ⭐⭐.
7. **reviewer 기한 초과 머지 어휘 (누적 3 PR)** — **MCP 룰 `reviewer-deadline-miss-merge-vocabulary` 후보** ⭐⭐.

## 메타 노트

- **rhkrwngud445 양 단계 완성 (1단계 #17 → 2단계 #50)**.
- **외부 서적 인용 누적 5 PR** (PR #154 조영호 + PR #150 로버트 마틴 + PR #62 LISP + PR #136 엘레강트 오브젝트 + PR #50 Effective Kotlin) — *외부 권위 인용 정점*.
- **reviewer 시간 한계 어휘 누적 3 PR** (PR #134 + #97 + #50) — *reviewer 자가 한계 인정* 어휘 정점.
